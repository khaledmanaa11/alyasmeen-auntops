---
phase: 03-agent-dependability-safety
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified: [scripts/eval_agent.py, app/services/processor.py]
autonomous: true
requirements: [REQ-prod-eval-gate, REQ-bot-ai-fallback]
must_haves:
  truths:
    - "Evaluation script reports accuracy, tool precision, and cost."
    - "Bot provides a friendly Arabic/English fallback message when AI fails."
  artifacts:
    - path: "scripts/eval_agent.py"
      provides: "AI performance metrics"
---

<objective>
Establish a performance baseline for the AI agent using the existing test dataset and implement robust error handling to ensure the bot remains polite even during AI service outages.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/03-agent-dependability-safety/03-02-SUMMARY.md
@tests/data/whatsapp_agent_dataset.json
@app/services/processor.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AI evaluation script</name>
  <files>scripts/eval_agent.py</files>
  <action>
    Create 'scripts/eval_agent.py' that:
    1. Loads the 75-case dataset from 'tests/data/whatsapp_agent_dataset.json'.
    2. Runs each 'raw_input' through 'handle_message' (mocking database and WhatsApp senders).
    3. Compares the triggered tool/intent with 'expected_intent'.
    4. Calculates and prints summary metrics: Accuracy %, Average Latency, and Estimated Cost (based on Claude Haiku pricing).
  </action>
  <verify>
    <automated>python scripts/eval_agent.py --limit 5</automated>
  </verify>
  <done>Evaluation script provides actionable performance data.</done>
</task>

<task type="auto">
  <name>Task 2: Implement hard-coded AI fallbacks</name>
  <files>app/services/processor.py</files>
  <action>
    Update 'handle_message' in 'app/services/processor.py' to wrap the 'generate_reply' call in a try/except block.
    In case of any exception (AI timeout, service down, rate limit):
    1. Log the error with high severity.
    2. Send a hard-coded fallback message to the user: "عذراً، أواجه مشكلة تقنية بسيطة حالياً. سأقوم بإبلاغ حنان لتعود إليكِ في أقرب وقت. شكراً لصبرك 🙏"
    3. Trigger a human handoff.
  </action>
  <verify>
    <automated>python -c "import app.services.processor as p; print('fallback' in open('app/services/processor.py').read())"</automated>
  </verify>
  <done>Bot handles AI failures gracefully with localized fallback messages and handoffs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| AI Service (Anthropic) | External dependency. Availability is not guaranteed. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Denial of Service | AI Service Outage | mitigate | Hard-coded fallbacks + Human Handoff. |
</threat_model>

<verification>
- Run the full evaluation script and record the baseline.
- Simulate an AI failure (e.g., invalid API key) and verify fallback message delivery.
</verification>

<success_criteria>
1. Evaluation baseline established.
2. Error fallbacks implemented and verified.
</success_criteria>

<output>
Create .planning/phases/03-agent-dependability-safety/03-03-SUMMARY.md when done
</output>
