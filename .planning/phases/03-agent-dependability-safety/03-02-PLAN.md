---
phase: 03-agent-dependability-safety
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: [app/services/processor.py]
autonomous: true
requirements: [REQ-prod-policy-gate, REQ-prod-handoff, REQ-ai-tools, REQ-ai-no-hallucination]
must_haves:
  truths:
    - "Bot ignores messages when session is paused."
    - "Tool calls are gated by PolicyEngine."
    - "Unsupported media (voice/image) triggers human handoff."
    - "Handoff keywords ('human', 'agent', 'حنان') trigger handoff."
  artifacts:
    - path: "app/services/processor.py"
      provides: "Integrated safety logic"
---

<objective>
Integrate the PolicyEngine and HandoffService into the core message processor to ensure all AI actions are validated and human intervention is triggered when necessary.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/03-agent-dependability-safety/03-01-SUMMARY.md
@app/services/processor.py
@app/services/policy.py
@app/services/handoff.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate PolicyEngine into tool executor</name>
  <files>app/services/processor.py</files>
  <action>
    Update '_make_tool_executor' in 'app/services/processor.py' to use 'PolicyEngine'.
    Before executing any tool, call 'policy_engine.validate(name, args, context)'.
    If validation fails, instead of executing the tool, return a message indicating that the request requires human review and trigger a handoff.
  </action>
  <verify>
    <automated>python -c "from app.services.processor import _make_tool_executor; print('Integrated' if 'PolicyEngine' in open('app/services/processor.py').read() else 'Missing')"</automated>
  </verify>
  <done>AI tool calls are blocked by the PolicyEngine if they violate rules.</done>
</task>

<task type="auto">
  <name>Task 2: Implement human handoff triggers and session gate</name>
  <files>app/services/processor.py</files>
  <action>
    Update 'handle_message' in 'app/services/processor.py':
    1. Check if 'st.get("paused")' is TRUE. If so, exit early (do not respond).
    2. Before calling AI, check the user message for handoff keywords (e.g., "human", "agent", "حنان", "تحويل").
    3. If a keyword is found, trigger 'HandoffService.trigger()' and send a confirmation message to the user.
  </action>
  <verify>
    <automated>grep -C 5 "paused" app/services/processor.py</automated>
  </verify>
  <done>Bot stops responding when paused and honors explicit handoff requests.</done>
</task>

<task type="auto">
  <name>Task 3: Detect and handle unsupported media</name>
  <files>app/services/processor.py</files>
  <action>
    Update 'process_event' or 'handle_message' to detect incoming media types other than 'text'.
    Specifically handle 'audio' (voice notes) and 'image'.
    If unsupported media is received, trigger 'HandoffService.trigger(reason="unsupported_media")' and notify the user: "عذراً، لا أستطيع معالجة الرسائل الصوتية أو الصور حالياً. سأقوم بتحويلك لخدمة العملاء."
  </action>
  <verify>
    <automated>grep "audio" app/services/processor.py</automated>
  </verify>
  <done>Voice notes and images automatically trigger human handoff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Meta Webhook -> processor.py | Media type field is trusted to identify non-text inputs. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Information Disclosure | AI Hallucinations | mitigate | System prompt forbids making up order statuses; PolicyEngine validates against real DB. |
</threat_model>

<verification>
- Manually test keyword triggers via mock WhatsApp interface.
- Mock a 'paused' session and verify bot silence.
- Mock an 'audio' message payload and verify handoff record creation.
</verification>

<success_criteria>
1. Tool calls are gated.
2. Handoff triggers are functional.
3. Bot respects 'paused' state.
</success_criteria>

<output>
Create .planning/phases/03-agent-dependability-safety/03-02-SUMMARY.md when done
</output>
