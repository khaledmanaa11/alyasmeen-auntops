---
phase: 03-agent-dependability-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [supabase/migrations/20260615000000_add_paused_to_sessions.sql, app/services/policy.py, app/services/handoff.py, tests/unit/test_policy.py, tests/unit/test_handoff.py]
autonomous: true
requirements: [REQ-prod-policy-gate, REQ-prod-handoff, REQ-bot-aunt-notification]
must_haves:
  truths:
    - "Sessions table has a 'paused' boolean column."
    - "PolicyEngine can validate tool calls against rules."
    - "HandoffService can create active handoff records and update session state."
  artifacts:
    - path: "app/services/policy.py"
      provides: "Deterministic tool validation"
    - path: "app/services/handoff.py"
      provides: "Handoff state management"
---

<objective>
Establish the data and service foundation for agent safety, including a session pause mechanism, a deterministic policy engine for tool calls, and a handoff management service.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/research/RESEARCH-3.md
@app/db/schema.sql
@supabase/migrations/20260614000001_durable_messaging.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 'paused' column to 'sessions' table</name>
  <files>supabase/migrations/20260615000000_add_paused_to_sessions.sql</files>
  <action>
    Create a new migration to add a 'paused' BOOLEAN column (default FALSE) to the 'sessions' table. This column will be used to gate bot responses when a human takes over.
    Execute the migration against the database.
  </action>
  <verify>
    <automated>python -c "from app.db.database import query; print(query('SELECT paused FROM sessions LIMIT 0'))"</automated>
  </verify>
  <done>Column 'paused' exists in 'sessions' table with default FALSE.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement PolicyEngine for deterministic tool validation</name>
  <files>app/services/policy.py, tests/unit/test_policy.py</files>
  <behavior>
    - Rule 1: 'add_to_cart' must not allow price overrides (args should match catalog prices).
    - Rule 2: 'get_order_status' is always allowed.
    - Rule 3: Any tool call for a session with a 'delivered' or 'done' order should be flagged for handoff (simulated by returning False).
  </behavior>
  <action>
    Create 'app/services/policy.py' with a PolicyEngine class. It should have a 'validate(tool_name, args, context)' method that returns a boolean.
    The context should include the latest order status and cart.
  </action>
  <verify>
    <automated>pytest tests/unit/test_policy.py</automated>
  </verify>
  <done>PolicyEngine correctly approves/denies tool calls based on defined rules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement HandoffService for human intervention</name>
  <files>app/services/handoff.py, tests/unit/test_handoff.py</files>
  <behavior>
    - Triggering a handoff must:
      1. Create a record in 'handoffs' table with status='active'.
      2. Update 'sessions.paused' to TRUE for that phone number.
      3. Log the event in 'audit_logs'.
  </behavior>
  <action>
    Create 'app/services/handoff.py' with a HandoffService class. It should provide a 'trigger(phone, reason, metadata)' method.
    Integrate with 'app.db.database' for transactions.
  </action>
  <verify>
    <automated>pytest tests/unit/test_handoff.py</automated>
  </verify>
  <done>HandoffService successfully persists handoff state and pauses the bot session.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| AI -> PolicyEngine | Untrusted tool calls from Claude are intercepted and validated. |
| Database -> Session | Session 'paused' state must be authoritative to stop bot loops. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Tool Arguments | mitigate | Pydantic validation + PolicyEngine checks. |
| T-03-02 | Denial of Service | Bot Loops | mitigate | 'paused' state blocks 'handle_message' entry. |
</threat_model>

<verification>
- Run all unit tests for policy and handoff services.
- Verify DB schema manually using Supabase dashboard or CLI.
</verification>

<success_criteria>
1. 'sessions.paused' column exists.
2. PolicyEngine and HandoffService classes are implemented and tested.
</success_criteria>

<output>
Create .planning/phases/03-agent-dependability-safety/03-01-SUMMARY.md when done
</output>
