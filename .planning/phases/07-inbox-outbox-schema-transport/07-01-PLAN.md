---
phase: 07-inbox-outbox-schema-transport
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "supabase/migrations/20260616000000_inbox_outbox.sql",
  "app/db/database.py",
  "app/services/transport.py",
  "app/routers/whatsapp.py",
  "tests/integration/test_transport.py"
]
autonomous: true
requirements: [REQ-M2-01, REQ-M2-02]

must_haves:
  truths:
    - "Meta webhooks are rejected with 403 if X-Hub-Signature-256 is missing or invalid."
    - "Valid Meta webhooks are persisted to webhook_events with status 'pending'."
    - "Duplicate wamid events do not create new database entries (idempotency)."
    - "The webhook responds with 200 OK immediately after persistence."
    - "Outbox schema exists and supports multi-transport jobs."
  artifacts:
    - path: "supabase/migrations/20260616000000_inbox_outbox.sql"
      provides: "Durable inbox/outbox tables"
    - path: "app/services/transport.py"
      provides: "HMAC verification and Meta envelope parsing"
    - path: "app/routers/whatsapp.py"
      provides: "Refactored 'ingest-and-ack' webhook"
  key_links:
    - from: "app/routers/whatsapp.py"
      to: "app/services/transport.py"
      via: "Dependency/Helper call"
    - from: "app/routers/whatsapp.py"
      to: "webhook_events table"
      via: "execute_returning('INSERT INTO ...')"
---

<objective>
Implement durable inbox/outbox schema and Meta transport security for ALYASMEEN AuntOps.

Purpose: Transition from synchronous request-response handling to a reliable asynchronous "ingest-and-ack" architecture, ensuring no Meta messages are lost and security is enforced at the edge.
Output: Database tables for inbox/outbox, a security layer for signature verification, and a refactored webhook router.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-inbox-outbox-schema-transport/RESEARCH.md
@app/db/database.py
@app/routers/whatsapp.py
@app/services/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema Migration & Database Validation</name>
  <files>supabase/migrations/20260616000000_inbox_outbox.sql, app/db/database.py</files>
  <action>
    1. Create a new Supabase migration creating `webhook_events` and `outbox_jobs` tables.
       - `webhook_events`: id (UUID/Serial), wamid (Text, UNIQUE), payload (JSONB), status (Text, default 'pending'), error (Text), created_at (Timestamptz), processed_at (Timestamptz).
       - `outbox_jobs`: id (UUID/Serial), transport (Text), recipient (Text), payload (JSONB), status (Text, default 'pending'), attempts (Int, default 0), max_attempts (Int, default 3), error (Text), created_at (Timestamptz), processed_at (Timestamptz).
    2. Update `REQUIRED_SCHEMA` in `app/db/database.py` to include these new tables and columns so that `validate_schema()` passes on startup.
  </action>
  <verify>
    <automated>python -c "from app.db.database import validate_schema; validate_schema()"</automated>
  </verify>
  <done>Schema migration applied and application starts with valid schema check.</done>
</task>

<task type="auto">
  <name>Task 2: Transport Security & Parsing Layer</name>
  <files>app/services/transport.py</files>
  <action>
    1. Create `app/services/transport.py` to house transport-specific logic.
    2. Implement `verify_meta_signature(body: bytes, signature: str)` using `hmac` and `hashlib.sha256` with `Config.WA_META_APP_SECRET`. Use `hmac.compare_digest` for timing-safe comparison.
    3. Implement `parse_meta_envelope(body: dict)` to extract a list of messages/events, focusing on extracting the `wamid` and basic metadata (from, text, timestamp) for each.
  </action>
  <verify>
    <automated>pytest tests/unit/test_transport_logic.py (To be created in this task)</automated>
  </verify>
  <done>Security and parsing logic implemented and unit-tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Refactor Webhook to Ingest-and-Ack</name>
  <files>app/routers/whatsapp.py, tests/integration/test_transport.py</files>
  <behavior>
    - POST /whatsapp/webhook with valid signature saves to DB and returns 200.
    - POST /whatsapp/webhook with invalid signature returns 403.
    - POST /whatsapp/webhook with duplicate wamid returns 200 (idempotent) and does not insert again.
    - Webhook acknowledgment occurs within < 1 second.
  </behavior>
  <action>
    1. Create `tests/integration/test_transport.py` with the behaviors above.
    2. Refactor `app/routers/whatsapp.py`:
       - Update `webhook_post` to read raw body `await request.body()`.
       - Verify signature using `verify_meta_signature`.
       - Parse payload using `parse_meta_envelope`.
       - For each extracted message:
         - Attempt `INSERT INTO webhook_events (wamid, payload) VALUES (%s, %s) ON CONFLICT (wamid) DO NOTHING`.
       - Return `{"ok": True}` immediately.
       - Note: For now, we temporarily comment out or bypass the old `_handle_message` inline processing to fulfill the "ingest-only" requirement of this phase. (Phase 08 will re-enable it via a worker).
  </action>
  <verify>
    <automated>pytest tests/integration/test_transport.py</automated>
  </verify>
  <done>Webhook successfully refactored to durable ingestion pattern.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Meta -> FastAPI | Untrusted webhook payload enters here. Must be verified via HMAC. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | /whatsapp/webhook | mitigate | Verify `X-Hub-Signature-256` header using `Config.WA_META_APP_SECRET` and timing-safe HMAC. |
| T-07-02 | Denial of Service | /whatsapp/webhook | mitigate | Fast 403 for unverified requests; offload processing to worker to keep request time minimal. |
| T-07-03 | Repudiation | Messaging | mitigate | Persist every verified event to `webhook_events` before acknowledgment. |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
### Integration Test Cases
1. **Signature Verification**:
   - `test_webhook_rejects_missing_signature`: Send POST without header -> 403.
   - `test_webhook_rejects_invalid_signature`: Send POST with wrong HMAC -> 403.
   - `test_webhook_accepts_valid_signature`: Send POST with correct HMAC -> 200.
2. **Durable Ingestion**:
   - `test_webhook_persists_to_db`: Verify row exists in `webhook_events` after 200 OK.
   - `test_webhook_idempotency`: Send same `wamid` twice -> Second call returns 200 but no new row.
3. **Performance**:
   - `test_webhook_latency`: Verify response time < 1000ms.
</verification>

<success_criteria>
- Schema migration applied and validated.
- HMAC verification enforced on Meta webhooks.
- Webhook events persisted to `webhook_events` for later worker processing.
- All integration tests in `tests/integration/test_transport.py` pass.
</success_criteria>

<output>
Create `.planning/phases/07-inbox-outbox-schema-transport/07-01-SUMMARY.md` when done
</output>
