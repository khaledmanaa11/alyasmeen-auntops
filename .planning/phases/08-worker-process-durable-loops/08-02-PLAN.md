---
phase: 08-worker-process-durable-loops
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified: [app/services/worker_tasks.py, app/routers/whatsapp.py, worker.py]
autonomous: true
requirements: [REQ-M2-01]
must_haves:
  truths:
    - "AI processing logic runs in the worker process, not the web thread"
    - "webhook_events are correctly handled by migrating _handle_message to worker_tasks"
  artifacts:
    - path: "app/services/worker_tasks.py"
      provides: "Handlers for background tasks (Inbox/AI)"
---

<objective>
Migrate the core business logic (AI processing and message handling) from the FastAPI request thread to the dedicated worker process.

Purpose: Offload computationally heavy and network-dependent AI reasoning to the background, allowing the web server to acknowledge Meta webhooks instantly.
Output: A worker capable of processing WhatsApp messages using Claude AI and updating session state.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/routers/whatsapp.py
@app/services/ai_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate _handle_message to worker_tasks.py</name>
  <files>app/services/worker_tasks.py, app/routers/whatsapp.py</files>
  <action>
    1. Create 'app/services/worker_tasks.py'.
    2. Move '_handle_message' and all its dependent helpers (_AR_ALIASES, tool handlers, tool executor factory) from 'app/routers/whatsapp.py' to 'app/services/worker_tasks.py'.
    3. Ensure 'app/services/worker_tasks.py' has all necessary imports (logging, re, json, database helpers, ai_service).
    4. Clean up 'app/routers/whatsapp.py' by removing the migrated logic. It should now only contain the webhook endpoints (GET and POST) which perform ingestion.
  </action>
  <verify>
    <automated>pytest tests/unit/test_ai_service.py (ensure logic still works in isolation)</automated>
  </verify>
  <done>Business logic is successfully decoupled from the API router.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Inbox Loop to handle_inbox_event</name>
  <files>worker.py, app/services/worker_tasks.py</files>
  <action>
    1. In 'app/services/worker_tasks.py', implement 'handle_inbox_event(event: dict)':
       - Extract 'payload' from event.
       - Extract 'from_number' (phone) and 'text' from payload (handling Meta envelope shape).
       - Call the migrated '_handle_message(phone, text, wa_name)'.
    2. Update 'worker.py' to call 'handle_inbox_event' when an event is claimed in 'inbox_loop'.
    3. Ensure exceptions in 'handle_inbox_event' are caught, logged, and the event status updated to 'failed' in the database.
  </action>
  <verify>
    <automated>python worker.py --test-inbox (verify processing of a simulated event)</automated>
  </verify>
  <done>Worker successfully processes incoming messages from the inbox table.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker → AI Service | Worker sends user messages to Claude API; trusts response but handles timeouts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | Information Disclosure | AI Logs | mitigate | Ensure PII (phone numbers) is masked or handled per project conventions in worker logs. |
| T-08-02-02 | Denial of Service | AI API Quota | mitigate | Implement error handling and stop retrying 'failed' AI calls to preserve quota. |
</threat_model>

<verification>
1. POST a mock message to '/whatsapp/webhook' (it should return 200 immediately).
2. Check 'webhook_events' table; verify status is 'pending'.
3. Run 'worker.py'.
4. Verify worker logs show processing and AI reply generation.
5. Verify 'webhook_events' status becomes 'processed'.
</verification>

<success_criteria>
- AI-driven replies are generated by the worker process.
- No business logic remains in 'app/routers/whatsapp.py'.
- Database sessions and history are updated correctly by the worker.
</success_criteria>

<output>
Create `.planning/phases/08-worker-process-durable-loops/08-02-SUMMARY.md` when done
</output>
