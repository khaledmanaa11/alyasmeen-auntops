---
phase: 08-worker-process-durable-loops
plan: 03
type: execute
wave: 2
depends_on: [08-01]
files_modified: [app/services/outbox.py, app/services/whatsapp_meta.py, app/services/pdf_invoice.py, worker.py, app/services/worker_tasks.py]
autonomous: true
requirements: [REQ-M2-01, REQ-M2-03]
must_haves:
  truths:
    - "Outbound messages are queued in outbox_jobs before delivery"
    - "PDF generation occurs in the background via the worker"
    - "worker.py executes outbox jobs by calling Meta API"
  artifacts:
    - path: "app/services/outbox.py"
      provides: "Unified helper for queuing outbound jobs"
---

<objective>
Standardize outbound communications using the Transactional Outbox Pattern and move PDF generation to the background worker.

Purpose: Ensure that every outbound message or document is reliably delivered, even if the primary transaction or worker process is interrupted.
Output: A system where all WhatsApp messages and PDF invoices are processed through the outbox queue.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/services/whatsapp_meta.py
@app/services/pdf_invoice.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Outbox helper and execution logic</name>
  <files>app/services/outbox.py, worker.py</files>
  <action>
    1. Create 'app/services/outbox.py' with 'enqueue_outbox(recipient, payload, transport="whatsapp_meta")'.
    2. In 'worker.py', implement 'execute_outbox_job(job: dict)':
       - Identify transport (e.g., 'whatsapp_meta', 'whatsapp_dev', 'pdf_generation').
       - If 'whatsapp_*', call 'send_text' or 'send_buttons' from the respective service.
       - If 'pdf_generation', call logic to generate and save/send PDF.
    3. Update 'worker.py' outbox loop to call 'execute_outbox_job'.
  </action>
  <verify>
    <automated>python worker.py --test-outbox (verify execution of a queued job)</automated>
  </verify>
  <done>Outbox jobs are correctly picked up and executed by the worker.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor message senders and PDF generation</name>
  <files>app/services/worker_tasks.py, app/services/pdf_invoice.py</files>
  <action>
    1. In 'app/services/worker_tasks.py', update '_handle_message' and its tools to use 'enqueue_outbox' instead of calling 'send_text'/'send_buttons' directly.
    2. Move PDF generation trigger (usually after order confirmation) to queue an outbox job of type 'pdf_generation'.
    3. Ensure 'app/services/pdf_invoice.py' functions can be called by the worker in a headless/background context.
  </action>
  <verify>
    <automated>pytest tests/integration/test_transport.py (verify messages still reach the simulated transport)</automated>
  </verify>
  <done>All outbound communication and heavy document generation are now routed through the worker via the outbox.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker → Meta API | Worker sends outbound messages; must handle API errors and rate limits. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-01 | Repudiation | Outbox Logs | mitigate | Retain 'outbox_jobs' records (sent/failed) for auditing and troubleshooting. |
| T-08-03-02 | Denial of Service | Meta API | mitigate | Implement exponential backoff in outbox loop for 429/5xx responses from Meta. |
</threat_model>

<verification>
1. Trigger a message flow (e.g., "cart").
2. Verify worker logs show:
   - Inbox event processed.
   - Outbox job queued.
   - Outbox job executed (sending WhatsApp).
3. Confirm an order.
4. Verify 'outbox_jobs' contains a PDF generation task.
5. Verify worker processes the PDF and updates status.
</verification>

<success_criteria>
- No direct 'whatsapp_meta' calls exist in 'app/services/worker_tasks.py' (all go through outbox).
- PDF generation no longer blocks any message processing loop.
- Outbox jobs have retry tracking (attempts incrementing on failure).
</success_criteria>

<output>
Create `.planning/phases/08-worker-process-durable-loops/08-03-SUMMARY.md` when done
</output>
