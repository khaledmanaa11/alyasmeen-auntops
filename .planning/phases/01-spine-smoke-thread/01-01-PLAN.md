---
phase: 01-spine-smoke-thread
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/routers/whatsapp.py
  - tests/data/meta_webhook_text.json
  - tests/data/meta_webhook_button_reply.json
  - tests/data/meta_webhook_list_reply.json
  - tests/data/meta_webhook_status.json
  - tests/unit/test_whatsapp_meta_envelope.py
  - tests/integration/test_bot_flow.py
autonomous: true
requirements:
  - SPINE-01
  - SPINE-02
nyquist_compliant: true

must_haves:
  truths:
    - "D-04: A real Meta Cloud API webhook payload (nested entry[].changes[].value.messages[] envelope) is parsed into (from_number, text, wa_name) and reaches the existing handler without HTTP 422 — minimal seam change only, order/confirm logic untouched"
    - "D-05: A Meta interactive button_reply id (e.g. 'confirm') is mapped to the command text the existing dispatcher branches on"
    - "D-05/D-08: A Meta status callback (sent/delivered/read) and unsupported message types return 200 with the handler never called"
    - "D-04: The flat dev/mock Msg shape ({from_number,text,wa_name}) still routes through the existing handler — no regression (the flat path is preserved, not removed)"
    - "D-08: A malformed/garbage POST is a harmless 200 no-op, never a crash"
    - "D-06: An order confirmed from a parsed Meta envelope (cart→confirm path) writes orders+order_lines and fires the aunt notification (proven against mocked DB/sender; the AI path is not exercised)"
  artifacts:
    - path: "app/routers/whatsapp.py"
      provides: "Meta-envelope-aware POST seam + pure _parse_meta_envelope helper + extracted _handle_message"
      contains: "_parse_meta_envelope"
    - path: "tests/data/meta_webhook_text.json"
      provides: "Documented-schema text-message fixture (D-03)"
      contains: "whatsapp_business_account"
    - path: "tests/data/meta_webhook_button_reply.json"
      provides: "Documented-schema interactive button_reply fixture"
      contains: "button_reply"
    - path: "tests/data/meta_webhook_list_reply.json"
      provides: "Documented-schema interactive list_reply fixture (defensive)"
      contains: "list_reply"
    - path: "tests/data/meta_webhook_status.json"
      provides: "Documented-schema status-callback fixture (no messages[])"
      contains: "statuses"
    - path: "tests/unit/test_whatsapp_meta_envelope.py"
      provides: "Pure-function parser regression tests (SPINE-01)"
      contains: "_parse_meta_envelope"
    - path: "tests/integration/test_bot_flow.py"
      provides: "Meta-envelope→confirm flow test (SPINE-02 automated, mocked)"
      contains: "test_meta_envelope_confirm_flow"
  key_links:
    - from: "app/routers/whatsapp.py POST /webhook"
      to: "_parse_meta_envelope"
      via: "branch on body.get('object') == 'whatsapp_business_account'"
      pattern: "object.*whatsapp_business_account"
    - from: "_parse_meta_envelope"
      to: "_handle_message"
      via: "normalized (from_number, text, wa_name) tuple feeds the existing dispatcher"
      pattern: "_handle_message"
    - from: "tests/unit/test_whatsapp_meta_envelope.py"
      to: "tests/data/meta_webhook_*.json"
      via: "json.loads of committed fixtures"
      pattern: "meta_webhook_.*\\.json"
---

<objective>
Fix the single broken inbound parse seam so the EXISTING ALYASMEEN order spine works against
the real Meta Cloud API envelope, while keeping the flat dev/mock path intact. This is the
thin vertical slice over existing brownfield code (NOT a greenfield build): the cart→confirm→
order-insert→aunt-notify flow already exists and is untouched; the only new code is ~40 lines
of defensive dict traversal plus committed fixtures and regression tests.

Purpose: Today `webhook_post(msg: Msg)` binds a flat Pydantic body, so FastAPI 422s every real
Meta payload BEFORE any handler code runs (CONCERNS 🔴 #1). This plan moves the seam to a raw
`Request`, detects payload shape, normalizes the nested Meta envelope to the tuple the existing
handler already consumes, and proves it with fixture-backed tests — closing SPINE-01 and the
automated (mocked) half of SPINE-02.

Output: A Meta-aware POST seam, a pure `_parse_meta_envelope` helper, an extracted
`_handle_message`, four documented-schema JSON fixtures, a new unit test file, and a new
integration test — all green under `python -m pytest`. The live, token-gated proof of SPINE-02
is the separate human gate in plan 01-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-spine-smoke-thread/01-CONTEXT.md
@.planning/phases/01-spine-smoke-thread/01-RESEARCH.md
@.planning/phases/01-spine-smoke-thread/01-PATTERNS.md
@.planning/phases/01-spine-smoke-thread/01-VALIDATION.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase this session — use directly, do NOT re-explore. -->

The seam being changed (app/routers/whatsapp.py:165-176, VERIFIED this session):
- `class Msg(BaseModel)` with fields `from_number: str`, `text: str`, `wa_name: str | None = None` (line 165-168) — KEEP this model; the flat dev/mock path and existing tests depend on it.
- `@router.post("/webhook")` then `def webhook_post(msg: Msg):` (line 171-172) — THIS is the bug. FastAPI validates the body against `Msg` before the function runs, so nested Meta payloads 422.
- The handler body runs lines 173-486. Its first 4 lines derive the only locals the rest of the body uses: `phone = msg.from_number`, `text = (msg.text or "").strip()`, `low = text.lower()`, `wa_name = (msg.wa_name or "").strip()`. The body ends at line 486 with `return send_text(phone, reply)`.
- `from fastapi import APIRouter, Request` is ALREADY imported (line 6) — no new import needed for the raw-Request seam.
- The sibling GET route (line 154-162) is the in-file precedent for `async def ...(request: Request)` raw-Request access.

The in-file raw-Request precedent (app/routers/whatsapp.py:154-162):
```
@router.get("/webhook")
async def webhook_get(request: Request):
    params = dict(request.query_params)
    ...
```

The confirm→order→notify flow that MUST stay byte-for-byte unchanged (app/routers/whatsapp.py:262-323):
- `execute_returning("INSERT INTO orders ... RETURNING id", (phone, fulfillment, address, total))` → `order_row["id"]` (line 278-286)
- `execute("UPDATE orders SET order_name = %s WHERE id = %s", ...)` and order_lines inserts (line 288-297) — `%s` placeholders only.
- Aunt notification block wrapped in try/except, only fires when `Config.AUNT_PHONE` is set (line 303-321).
- `return {"ok": True, "order_id": order_id, "order_name": order_name}` (line 323).

Interactive reply id → command identity map (VERIFIED: send_buttons emits these exact ids in this file):
| Reply id | Handler branch that consumes it |
|----------|----------------------------------|
| `confirm`  | `if low == "confirm"` (line 262) |
| `clear`    | `if low == "clear"` (line 232) |
| `pickup`   | `if low in ("pickup","delivery")` (line 239) |
| `delivery` | same (line 239) |
| `cart`     | `if low == "cart"` (line 206) |
The mapping is an identity pass-through: `text = interactive.{type}.id`. No new command vocabulary.

Test isolation (tests/conftest.py:11-52, autouse — VERIFIED this session):
- `mock_db` monkeypatches names ON THE `wa` MODULE: `wa.execute`, `wa.execute_returning`, `wa.load_session`, `wa.save_session`, `wa.clear_session`, `wa.upsert_customer`, `wa.get_customer_name`, etc. → no test touches live Supabase (satisfies D-07).
- `fake_execute_returning` returns an incrementing `{"id": n}` so `confirm` yields `ORD-000n`.
- IMPLICATION: keep `_handle_message` and ALL DB calls defined IN `whatsapp.py` so the module-level monkeypatches still bind. Do NOT move DB calls to another module.

Integration test boilerplate already present (tests/integration/test_bot_flow.py:8-38, VERIFIED):
- Sets `os.environ["USE_MOCK_WHATSAPP"] = "1"`, imports `app.routers.whatsapp as wa` and `app.main.app`.
- `FAKE_CATALOG` + a `client` fixture that monkeypatches `wa._CATALOG` and returns `TestClient(app)`.
- Existing flat-shape regression anchor (line 66): `client.post("/whatsapp/webhook", json={"from_number": phone, "text": "menu"})` then `assert r1.status_code == 200` and `assert "كريم اليدين" in r1.json().get("text", "")`. The dev/flat path MUST keep returning the handler's dict so `.json().get("text")` still works.
- This file is NOT marked `@pytest.mark.integration`, so it runs in the default `python -m pytest` suite.

Verified Meta envelope wire shapes (from 01-RESEARCH.md "Code Examples", snake_case wire fields):
- TEXT: `entry[0].changes[0].value.messages[0]` with `.from`, `.type=="text"`, `.text.body`; name at `value.contacts[0].profile.name`. Expect parse → `("972599123456", "menu", "فاطمة")`.
- BUTTON: `messages[0].type=="interactive"`, `.interactive.type=="button_reply"`, `.interactive.button_reply.id=="confirm"`. Expect → text `"confirm"`.
- LIST: `.interactive.type=="list_reply"`, `.interactive.list_reply.id=="prod_1"`. Expect → text `"prod_1"` (defensive).
- STATUS: `value.statuses[]` (status sent/delivered/read), NO `messages` key. Expect parse → `[]`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the four documented-schema Meta webhook fixtures (Wave 0)</name>
  <read_first>
    - .planning/phases/01-spine-smoke-thread/01-RESEARCH.md (section "Code Examples", lines ~419-501 — the four VERIFIED envelope shapes; copy field names verbatim, snake_case: `phone_number_id`, `whatsapp_business_account`)
    - .planning/phases/01-spine-smoke-thread/01-PATTERNS.md (section "tests/data/meta_webhook_*.json" — expected parse results per fixture)
    - tests/data/whatsapp_agent_dataset.json (the in-repo precedent that committed JSON test data lives under tests/data/)
  </read_first>
  <action>
    Create four static JSON fixtures under tests/data/ using the Write tool (NOT heredoc, NOT a Pydantic model). Build each verbatim from the VERIFIED schema in 01-RESEARCH.md "Code Examples":
    - tests/data/meta_webhook_text.json — full envelope with `object: "whatsapp_business_account"`, one `entry[].changes[]` where `field == "messages"`, `value.contacts[0].profile.name == "فاطمة"`, `value.messages[0]` with `from == "972599123456"`, `type == "text"`, `text.body == "menu"`. Must parse to `("972599123456", "menu", "فاطمة")`.
    - tests/data/meta_webhook_button_reply.json — same envelope shape, `messages[0].type == "interactive"`, `interactive.type == "button_reply"`, `interactive.button_reply.id == "confirm"`, title `"✅ تأكيد الطلب"`. Must parse to text `"confirm"`.
    - tests/data/meta_webhook_list_reply.json — same shape, `interactive.type == "list_reply"`, `interactive.list_reply.id == "prod_1"`. Must parse to text `"prod_1"` (defensive path).
    - tests/data/meta_webhook_status.json — envelope with `value.statuses[]` (one entry: `status` "delivered", `id`, `timestamp`, `recipient_id`) and NO `messages` key. Must parse to `[]`.
    Use snake_case wire fields throughout (`phone_number_id`, not camelCase — RESEARCH stale-flag confirms wire is snake_case). Keep Arabic text intentional and UTF-8 encoded.
  </action>
  <verify>
    <automated>python -c "import json,glob; [json.load(open(f,encoding='utf-8')) for f in glob.glob('tests/data/meta_webhook_*.json')]; print('4 fixtures valid JSON')"</automated>
  </verify>
  <acceptance_criteria>
    - All four files exist under tests/data/ and are valid JSON (the verify command exits 0 and prints "4 fixtures valid JSON").
    - meta_webhook_text.json, meta_webhook_button_reply.json, meta_webhook_list_reply.json each contain a top-level `"object": "whatsapp_business_account"` and a `value.messages` array.
    - meta_webhook_status.json contains a `value.statuses` array and has NO `messages` key anywhere under `value`.
    - All envelope fields use snake_case wire names (e.g. `phone_number_id`).
  </acceptance_criteria>
  <done>Four documented-schema fixtures committed under tests/data/, all valid JSON, matching the VERIFIED RESEARCH shapes (D-03).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the pure _parse_meta_envelope helper, change the POST seam, extract _handle_message</name>
  <read_first>
    - app/routers/whatsapp.py (read the whole file — you are extracting the handler body lines 173-486 into a function and changing the route at 171-172; you MUST NOT alter the confirm/order/notify logic at 262-323)
    - .planning/phases/01-spine-smoke-thread/01-PATTERNS.md (section "app/routers/whatsapp.py — MODIFY the POST seam" — the exact recommended seam shape, the pure parser helper, and the DO-NOT-TOUCH list)
    - .planning/phases/01-spine-smoke-thread/01-RESEARCH.md (Pattern 1 shape-detection, Pattern 2 interactive identity map, Pattern 3 status no-op, Pitfalls 1-2, "Reference parser skeleton")
    - tests/conftest.py (so you understand the monkeypatch binds on the `wa` module — keep _handle_message and all DB calls IN whatsapp.py)
  </read_first>
  <behavior>
    - `_parse_meta_envelope({})` returns `[]` (no crash on garbage — V5 input validation).
    - `_parse_meta_envelope(meta_webhook_text.json)` returns `[("972599123456", "menu", "فاطمة")]`.
    - `_parse_meta_envelope(meta_webhook_button_reply.json)`: the single tuple's text element is `"confirm"`.
    - `_parse_meta_envelope(meta_webhook_list_reply.json)`: the single tuple's text element is `"prod_1"`.
    - `_parse_meta_envelope(meta_webhook_status.json)` returns `[]` (status callback, no messages[]).
    - An envelope whose only message has `type == "image"` returns `[]` (unsupported type ignored).
    - A flat dev POST `{"from_number": p, "text": "menu"}` to `/whatsapp/webhook` still returns 200 with the handler's dict body (regression).
    - A Meta text envelope POST to `/whatsapp/webhook` returns 200, NOT 422 (the core SPINE-01 fix).
  </behavior>
  <action>
    Add a pure module-level helper `_parse_meta_envelope(body: dict) -> list[tuple[str, str, str | None]]` to whatsapp.py. It must: iterate `body.get("entry", []) or []` → `entry.get("changes", []) or []`; `continue` when `change.get("field") != "messages"`; read `value = change.get("value") or {}`; `continue` when `value.get("messages")` is falsy (the `"messages" in value` guard — status callbacks have no messages[]); derive `wa_name` from `value.get("contacts")[0].get("profile", {}).get("name")` defensively (None if no contacts); for each message, branch on `m.get("type")`: `"text"` → `(m.get("text") or {}).get("body", "")`, `"interactive"` → `inter = m.get("interactive") or {}; sub = inter.get("type"); text = (inter.get(sub) or {}).get("id", "") if sub else ""` (the identity map for button_reply/list_reply), any other type → `continue`; append `(frm, text, wa_name)` only when both `frm` and `text` are non-empty. Defensive `.get()` everywhere — no bare indexing.

    Extract the current handler body (lines 173-486) into `def _handle_message(phone: str, text: str, wa_name: str | None)`. Move the first 4 derived locals inside it: `text = (text or "").strip()`, `low = text.lower()`, `wa_name = (wa_name or "").strip()` (keep `phone` as the param). The remainder (lines 178-486) is verbatim — do NOT change order/confirm/notify logic (D-04/D-06). It still returns the existing dict / send_text result.

    Change the route to `async def webhook_post(request: Request):`. Read `body = await request.json()`. Branch on `body.get("object") == "whatsapp_business_account"`: loop `for from_number, text, wa_name in _parse_meta_envelope(body): _handle_message(from_number, text, wa_name)` and return `{"ok": True}` (200 even when the list is empty — status callbacks / unsupported). Else (flat dev/mock shape): `_handle_message(body.get("from_number", ""), body.get("text", ""), body.get("wa_name"))` and return its result (so existing tests' `.json().get("text")` still works). KEEP the `Msg` Pydantic model defined in the file even though the route no longer binds it — tests and the mock contract reference it.

    Per RESEARCH open-question 1 (A1): the handler body is sync and calls blocking `requests.post`/Supabase. Wrap the dispatch in `run_in_threadpool` (`from starlette.concurrency import run_in_threadpool`, `await run_in_threadpool(_handle_message, ...)`) to keep the event loop responsive — this is the cleaner minimal choice and satisfies D-08's "inline processing acceptable". Do NOT add HMAC/signature verification, rate limiting, idempotency, or an inbox table (all M2). Leave the dormant signature branch in whatsapp_meta.verify_get dormant. Leave the global 200 handler in main.py unchanged (D-08).
  </action>
  <verify>
    <!-- Self-contained smoke check against the Task 1 fixtures (which already exist) + the freshly
         implemented helper. Deliberately does NOT run tests/unit/test_whatsapp_meta_envelope.py —
         that file is authored in Task 3, so depending on it here would fail on a missing file when
         tasks run in order T1→T2→T3. The formal pytest regression suite is Task 3's gate. -->
    <automated>python -c "import json; from app.routers.whatsapp import _parse_meta_envelope, _handle_message, Msg; L=lambda n: json.load(open('tests/data/'+n, encoding='utf-8')); assert _parse_meta_envelope(L('meta_webhook_text.json')) == [('972599123456','menu','فاطمة')]; assert _parse_meta_envelope(L('meta_webhook_button_reply.json'))[0][1]=='confirm'; assert _parse_meta_envelope(L('meta_webhook_list_reply.json'))[0][1]=='prod_1'; assert _parse_meta_envelope(L('meta_webhook_status.json'))==[]; assert _parse_meta_envelope({})==[]; print('parser smoke ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: whatsapp.py defines `_parse_meta_envelope` and `_handle_message` as module-level functions, and the route is `async def webhook_post(request: Request)`.
    - Source assertion: the `Msg` BaseModel is still present in whatsapp.py.
    - Source assertion: the confirm/order/notify block (the `INSERT INTO orders ... RETURNING id`, the order_lines loop, the `🛍️ طلب جديد!` aunt notification) is unchanged from the original.
    - Source assertion: no `X-Hub-Signature-256`, HMAC, rate-limit, or inbox/outbox code was added.
    - Behavior assertion: the self-contained parser smoke check passes (exits 0, prints "parser smoke ok") — proving text/button/list/status/garbage envelopes normalize correctly against the committed fixtures. (Route-level no-422 and flat-shape regression are formally proven by Task 3's pytest suite.)
  </acceptance_criteria>
  <done>The POST seam accepts a raw Request, normalizes Meta envelopes via the pure parser into the unchanged handler, keeps the flat path working, and never adds M2 hardening. Parser smoke check green; full route-level + regression coverage lands in Task 3.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write parser unit tests + extend the integration flow test (SPINE-01 + SPINE-02 automated)</name>
  <read_first>
    - .planning/phases/01-spine-smoke-thread/01-PATTERNS.md (sections "tests/unit/test_whatsapp_meta_envelope.py" and "tests/integration/test_bot_flow.py — EXTEND" — copy the fixture-loader idiom and the test skeletons; note the filename caution)
    - .planning/phases/01-spine-smoke-thread/01-VALIDATION.md ("Per-Task Verification Map" — the exact test names and the requirement→behavior rows)
    - tests/unit/test_whatsapp_meta.py (the no-TestClient, direct-import, class-grouped unit style to mirror — and the reason the new file is named test_whatsapp_meta_envelope.py to avoid collision)
    - tests/integration/test_bot_flow.py (read in full — reuse the existing `client` fixture, the flat-shape regression anchor, and the aunt-notification capture pattern in TestAuntNotification)
  </read_first>
  <action>
    Create tests/unit/test_whatsapp_meta_envelope.py (NOT test_whatsapp_meta.py — that exists and covers the sender). Add a `pathlib` fixture loader: `DATA = Path(__file__).resolve().parents[1] / "data"` and `_load(name)` reading the JSON with `encoding="utf-8"`. Import `from app.routers.whatsapp import _parse_meta_envelope`. Group tests in a class with these exact method names (per VALIDATION map): `test_text_message_parses` (asserts `== [("972599123456", "menu", "فاطمة")]`), `test_button_reply_maps_to_command` (asserts `out[0][1] == "confirm"`), `test_list_reply_parses` (asserts `out[0][1] == "prod_1"`), `test_status_callback_noop` (asserts `== []`), `test_unsupported_type_noop` (inline image-type envelope → `== []`), and `test_malformed_payload_is_safe` (asserts `_parse_meta_envelope({}) == []`).

    Extend tests/integration/test_bot_flow.py with a new class `TestMetaEnvelopeFlow` containing `test_meta_envelope_confirm_flow`. Reuse the existing `client` fixture. Seed the cart via the flat dev shape (`menu`, then `1`, then `pickup` — shape detection is per-request so seeding flat is allowed), then POST the FINAL `confirm` step as a real Meta `button_reply` envelope (`object: "whatsapp_business_account"`, `messages[0].type=="interactive"`, `interactive.button_reply.id=="confirm"`). Assert `r.status_code == 200` (the core no-422 proof). For the SPINE-02 aunt-notification assertion, reuse the TestAuntNotification capture pattern: monkeypatch `config_mod.Config.AUNT_PHONE` to a test number and monkeypatch `wa.send_text` to append `to` into a list, then assert the aunt number is in the captured sends after the Meta-envelope confirm. Note the Meta-loop route returns `{"ok": True}` (not order_id) — assert on the captured aunt send, not on the response body's order_id.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_whatsapp_meta_envelope.py tests/integration/test_bot_flow.py::TestMetaEnvelopeFlow -x</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/test_whatsapp_meta_envelope.py exists with all six named test methods, loading the four committed fixtures via `_load`.
    - tests/integration/test_bot_flow.py contains `class TestMetaEnvelopeFlow` with `test_meta_envelope_confirm_flow`.
    - Behavior assertion: `python -m pytest tests/unit/test_whatsapp_meta_envelope.py::TestMetaEnvelopeParser::test_status_callback_noop -x` passes (handler not reachable for status callbacks).
    - Behavior assertion: `test_meta_envelope_confirm_flow` proves a Meta button_reply confirm returns 200 (not 422) AND the aunt number appears in captured `send_text` recipients.
    - Full-suite regression: `python -m pytest` is green (the flat dev path and all prior tests still pass).
  </acceptance_criteria>
  <done>SPINE-01 parser behaviors and the SPINE-02 mocked confirm-flow are covered by green automated tests; the full suite has no regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → `POST /whatsapp/webhook` | Untrusted JSON from Meta (and anyone who finds the URL) crosses here. This phase's only new attack surface. |
| Parser → existing handler | Parsed `text` flows into the existing dispatcher and, on `confirm`, into parameterized SQL. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Denial of Service | `_parse_meta_envelope` on malformed/oversized/missing-field payload | mitigate | Defensive `.get()` everywhere, `"messages" in value` presence guard before indexing, no bare indexing; `test_malformed_payload_is_safe` + `test_status_callback_noop` prove `{}` and status callbacks return `[]`/200 without raising (Pitfall 2). |
| T-01-02 | Tampering | SQL injection via inbound message `text` | mitigate | Parsed text flows only through the existing `%s`-placeholder + `_escape()` path in database.py (the confirm branch is unchanged); the parser does no SQL. Already enforced; preserved. |
| T-01-03 | Tampering | Tests writing throwaway rows to live Supabase | mitigate | Autouse `mock_db` in tests/conftest.py patches `wa.execute*`/session helpers to in-memory fakes; keep `_handle_message` + DB calls in whatsapp.py so the patch binds (D-07). |
| T-01-04 | Spoofing / Elevation | Unauthenticated webhook POST triggers an order | accept (deferred to M2) | HMAC `X-Hub-Signature-256` verification, rate limiting, and `wamid` idempotency are EXPLICITLY M2 per the scope fence. The dormant signature branch in whatsapp_meta.verify_get stays dormant. Documented as known-deferred; do NOT build now. |
| T-01-05 | Tampering | Customer text treated as instruction (prompt injection) | accept (deferred to M3) | The AI/tool path is out of the Phase 1 proof (D-06); AI hardening is M3. The confirm proof path is deterministic, not AI-driven. |

No high-severity threat is left unmitigated WITHIN Phase 1's actual scope (the new parser surface).
T-01-04 and T-01-05 are deliberate, documented deferrals to M2/M3 per the binding scope fences — not Phase 1 gaps.
</threat_model>

<verification>
- `python -m pytest tests/unit/test_whatsapp_meta_envelope.py -x` — parser regression (SPINE-01) green.
- `python -m pytest` — full suite green, proving the flat dev/mock path is not regressed and the
  Meta-envelope→confirm flow (SPINE-02, mocked) passes. Excludes the `integration` marker by default;
  test_bot_flow.py is unmarked so it runs.
- Source check: `_parse_meta_envelope` and `_handle_message` exist in whatsapp.py; route is
  `async def webhook_post(request: Request)`; `Msg` model retained; confirm/notify block unchanged;
  no HMAC/rate-limit/inbox code added.
</verification>

<success_criteria>
- A Meta text/button/list envelope POSTed to `/whatsapp/webhook` returns 200 (not 422) and reaches
  the existing handler with the correct normalized tuple.
- Status callbacks and unsupported message types return 200 with the handler never called.
- The flat dev/mock `{from_number,text,wa_name}` shape still works (no regression).
- A `confirm` from a parsed Meta button_reply writes orders+order_lines and fires the aunt
  notification under the mocked DB/sender harness.
- No M2 hardening (signature verification, rate limiting, idempotency, inbox/outbox) was introduced.
</success_criteria>

<output>
Create `.planning/phases/01-spine-smoke-thread/01-01-SUMMARY.md` when done.
</output>
