---
phase: 01-spine-smoke-thread
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified: []
autonomous: false
requirements:
  - SPINE-02
nyquist_compliant: true

user_setup:
  - service: meta-whatsapp-cloud-api
    why: "Live outbound send to the aunt + inbound delivery to the webhook (D-01 requires a REAL message, not mock)"
    env_vars:
      - name: WA_META_TOKEN
        source: "Meta App Dashboard → WhatsApp → API Setup (temporary 24h token) OR Business Settings → Users → System Users → Generate token with whatsapp_business_messaging + whatsapp_business_management (recommended — non-expiring)"
      - name: WA_META_PHONE_ID
        source: "Meta App Dashboard → WhatsApp → API Setup → 'Phone number ID'"
      - name: WA_META_VERIFY_TOKEN
        source: "Self-chosen string; must match the value typed into Meta's webhook 'Verify token' field"
      - name: USE_MOCK_WHATSAPP
        source: "Set to 0 in the runtime env, then RESTART the process (sender binds at import — Pitfall 4)"
    dashboard_config:
      - task: "Add the aunt's number AND Khaled's number to the ≤5 test-recipient allow-list (each must accept the WhatsApp opt-in)"
        location: "Meta App Dashboard → WhatsApp → API Setup → 'To' recipient list"
      - task: "Set webhook callback URL https://alyasmeen.org/whatsapp/webhook (or a tunnel URL) + matching verify token, and subscribe the WABA to the 'messages' field"
        location: "Meta App Dashboard → WhatsApp → Configuration → Webhook"

must_haves:
  truths:
    - "A working Meta Cloud API token and WA_META_PHONE_ID are set in the runtime env (System User token preferred so the 24h temp-token clock does not gate the proof)"
    - "The aunt's number and Khaled's number are registered in Meta's ≤5 test-recipient allow-list"
    - "USE_MOCK_WHATSAPP=0 is set and the process restarted before the proof (sender binds at import)"
    - "A real WhatsApp message from a registered test number completes cart→confirm with no HTTP 422 in the logs"
    - "The resulting order appears in the live Supabase orders+order_lines tables and is visible in the /orders dashboard"
    - "The aunt's phone actually receives the 🛍️ طلب جديد! notification via the real Meta sender"
    - "The one smoke-test order is deleted afterward (order_lines → orders → sessions for the test phone) so prod is not polluted"
  artifacts:
    - path: ".planning/phases/01-spine-smoke-thread/01-02-SUMMARY.md"
      provides: "Recorded live-proof evidence (no-422 confirmation, /orders screenshot reference, aunt-phone screenshot reference, captured wamid) and D-07 cleanup confirmation"
  key_links:
    - from: "Meta Cloud API (real inbound)"
      to: "POST /whatsapp/webhook (the 01-01 parser)"
      via: "configured webhook callback URL + subscribed 'messages' field"
      pattern: "whatsapp/webhook"
    - from: "confirm handler"
      to: "aunt phone"
      via: "whatsapp_meta.send_text with USE_MOCK_WHATSAPP=0"
      pattern: "AUNT_PHONE"
---

<objective>
Close SPINE-02 with the binding D-01 live proof: a REAL WhatsApp message from a registered test
phone, parsed by the 01-01 seam, creates a REAL order in live Supabase visible in /orders, and the
aunt's phone ACTUALLY receives the new-order notification through the real Meta sender. Green tests
alone do NOT satisfy this phase (D-01) — this is the human-gated gate that can block phase completion.

Purpose: D-02 makes the Meta token an in-phase prerequisite, not an M5 deferral ("we do the token
when we need it" = we need it here). This plan surfaces that blocking human setup loudly, then runs
the live proof and cleans up the prod smoke order (D-07).

Output: A working live spine proven once end-to-end, evidence recorded, and the throwaway prod row
deleted. No code changes — this plan is human action + verification on top of the 01-01 code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-spine-smoke-thread/01-CONTEXT.md
@.planning/phases/01-spine-smoke-thread/01-RESEARCH.md
@.planning/phases/01-spine-smoke-thread/01-VALIDATION.md
@.planning/phases/01-spine-smoke-thread/01-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: ⛔ BLOCKING — Khaled provisions the Meta Cloud API token + test recipients (D-02)</name>
  <read_first>
    - .planning/phases/01-spine-smoke-thread/01-RESEARCH.md ("Environment Availability" → "D-02 — Concrete ordered human steps for Khaled", lines ~610-631; Pitfalls 3, 5, 6 for the failure modes)
    - CLAUDE.md ("Env Vars Reference" table — the WA_META_* vars and USE_MOCK_WHATSAPP)
  </read_first>
  <what-built>
    No code is built here. This is the in-phase human prerequisite (D-02) that gates the live proof.
    Claude CANNOT do this — it requires Khaled's Meta Business account access. Phase 1 is NOT done
    until this is complete, so it is flagged blocking and loud.
  </what-built>
  <action>
    HALT and surface this as a loud, blocking human-action checkpoint — Claude cannot perform Meta
    Business portal steps. Present Khaled the ordered click-path: (1) read WA_META_PHONE_ID from
    WhatsApp → API Setup; (2) generate a non-expiring System User token (whatsapp_business_messaging
    + whatsapp_business_management) → WA_META_TOKEN, preferred over the 24h temp token (Pitfall 3);
    (3) add BOTH the aunt's and Khaled's numbers to the ≤5 test-recipient allow-list (Pitfall 5);
    (4) set the webhook callback URL + matching WA_META_VERIFY_TOKEN and subscribe the WABA to the
    "messages" field (Pitfall 6); (5) set WA_META_TOKEN/WA_META_PHONE_ID/WA_META_VERIFY_TOKEN and
    USE_MOCK_WHATSAPP=0 in the runtime env and RESTART (sender binds at import, Pitfall 4). Wait for
    the "token ready" resume signal before proceeding to the live proof.
  </action>
  <how-to-verify>
    Khaled performs these ordered steps (full click-path in 01-RESEARCH.md lines 610-631):
    1. Meta App Dashboard → WhatsApp → API Setup. Note the "Phone number ID" → set WA_META_PHONE_ID.
    2. (Recommended) Generate a NON-EXPIRING System User token (Business Settings → Users → System
       Users → admin system user → assign the WABA → Generate token with
       whatsapp_business_messaging + whatsapp_business_management) → set WA_META_TOKEN. This avoids the
       24h temp-token expiry that would otherwise put the proof on a stopwatch (Pitfall 3).
    3. WhatsApp → API Setup → 'To' recipient list → add BOTH the aunt's number AND Khaled's number
       (≤5 allowed pre-business-verification); each accepts the WhatsApp opt-in prompt (Pitfall 5).
    4. WhatsApp → Configuration → Webhook: set callback URL https://alyasmeen.org/whatsapp/webhook
       (or a tunnel URL for local), paste the SAME string as env WA_META_VERIFY_TOKEN into "Verify
       token", and subscribe the WABA to the "messages" field (Pitfall 6).
    5. Set runtime env WA_META_TOKEN, WA_META_PHONE_ID, WA_META_VERIFY_TOKEN, USE_MOCK_WHATSAPP=0
       → RESTART the process (the sender binds at import time, whatsapp.py:29-32 — Pitfall 4).
    Confirm: the webhook GET handshake succeeds in Meta (subscription saves), and both numbers show
    in the recipient list.
  </how-to-verify>
  <verify>Human-gated: confirmed by the "token ready" resume signal (no automated check — Meta portal state).</verify>
  <acceptance_criteria>
    - WA_META_TOKEN and WA_META_PHONE_ID are set in the runtime env (System User token preferred).
    - The aunt's number AND Khaled's number both appear in Meta's test-recipient allow-list and have accepted the opt-in.
    - The Meta webhook subscription saved successfully (GET verify handshake passed) for the "messages" field.
    - USE_MOCK_WHATSAPP=0 is set and the process has been restarted.
  </acceptance_criteria>
  <done>Token, phone ID, both test recipients, webhook subscription, and USE_MOCK_WHATSAPP=0 (restarted) are all confirmed live.</done>
  <resume-signal>Type "token ready" once the token, phone ID, both test recipients, webhook subscription, and USE_MOCK_WHATSAPP=0 (restarted) are all confirmed. This task BLOCKS the live proof.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live spine proof — real message → real order in /orders → aunt's phone buzzes (D-01)</name>
  <read_first>
    - .planning/phases/01-spine-smoke-thread/01-VALIDATION.md ("Manual-Only Verifications" table + "Live-proof checkpoint" steps)
    - .planning/phases/01-spine-smoke-thread/01-RESEARCH.md (Pitfalls 3-6 — the failure modes to watch during the proof)
  </read_first>
  <what-built>
    The 01-01 parser fix is now deployed and the D-02 token/recipients are live. This checkpoint
    verifies the REAL end-to-end spine — the automated tests deliberately do NOT prove this (they
    must not write to prod, D-07). Per D-01, mock/green-tests do NOT count; the aunt's phone must
    actually buzz from a real message.
  </what-built>
  <action>
    With Task 1 confirmed live, drive the real end-to-end proof: from a REGISTERED test number send a
    real WhatsApp message and complete the existing cart → confirm path (text or confirm button — the
    AI path is NOT required per D-06). Then capture three pieces of evidence: no HTTP 422 in the logs
    for the inbound POST, the new order visible on the /orders dashboard, and the aunt's phone
    actually receiving the 🛍️ طلب جديد! notification via the real Meta sender (not the [DEV] mock
    console). Capture the inbound `wamid` from the logs. Watch for the three failure modes (expired
    token / unregistered recipient / mock-not-flipped). Wait for the "proven" resume signal.
  </action>
  <how-to-verify>
    1. Confirm Task 1 prerequisites are still live (token not expired if temp; USE_MOCK_WHATSAPP=0; restarted).
    2. From a REGISTERED test number, send a real WhatsApp message to the business number and complete
       the existing cart → confirm flow (text or the confirm button — D-06; the AI path is not required).
    3. Verify all three, and capture evidence:
       (a) No HTTP 422 in the application logs for the inbound POST (the SPINE-01 fix holding live).
       (b) The order appears in the live Supabase orders+order_lines tables and is visible on the
           /orders dashboard page — screenshot it.
       (c) The aunt's phone ACTUALLY receives the 🛍️ طلب جديد! notification (real Meta sender, not
           [DEV] console) — screenshot it. Capture the inbound message `wamid` from the logs.
    Failure modes to watch (from RESEARCH): outbound 401/OAuthException = expired/invalid token
    (Pitfall 3); send returns 200 but nothing arrives = recipient not registered (Pitfall 5);
    messages print [DEV] to console = USE_MOCK_WHATSAPP not flipped or no restart (Pitfall 4).
  </how-to-verify>
  <verify>Human-gated: confirmed by the "proven" resume signal with captured wamid + screenshots (cannot be automated — token-gated, writes prod, D-07).</verify>
  <acceptance_criteria>
    - No HTTP 422 logged for the real inbound webhook POST.
    - A real orders row (+ its order_lines) exists in live Supabase and is visible on /orders.
    - The aunt's phone received the 🛍️ طلب جديد! notification via the real Meta sender (not the [DEV] mock).
    - Evidence recorded: /orders screenshot reference, aunt-phone screenshot reference, and the captured inbound `wamid`.
  </acceptance_criteria>
  <done>The real message produced a no-422 inbound, a real order visible on /orders, and an actual aunt-phone notification; evidence (wamid + screenshots) recorded.</done>
  <resume-signal>Type "proven" with the captured wamid and confirmation of all three (no 422, order in /orders, aunt notified). Describe any failure instead if a Pitfall fired.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: D-07 cleanup — delete the smoke-test order from live Supabase</name>
  <read_first>
    - .planning/phases/01-spine-smoke-thread/01-RESEARCH.md ("Runtime State Inventory" + Open Question 2 / Assumption A4 — FK-safe deletion order and the customers/chat_history decision)
    - app/db/database.py (the single DB adapter — `execute` with `%s` placeholders; HTTPS-RPC only, no psycopg2)
  </read_first>
  <what-built>
    The live proof wrote one real order to prod (D-01). D-07 requires deleting it so the smoke test
    does not pollute production data. No app code changes — this is a one-off data cleanup using the
    existing DB adapter / Supabase SQL editor against the test phone's rows.
  </what-built>
  <action>
    Delete the one smoke-test order from live Supabase in FK-safe order — `order_lines` (by the smoke
    order_id) → `orders` (the smoke order_id) → `sessions` (the test phone) — using the existing
    `%s`-placeholder DB adapter or the Supabase SQL editor (NEVER psycopg2/DATABASE_URL). Decide with
    Khaled whether to also delete the test phone's `customers`/`chat_history` rows (default per A4:
    leave, record the decision). Confirm the smoke order no longer appears on /orders. Wait for the
    "cleaned" resume signal.
  </action>
  <how-to-verify>
    Delete in FK-safe order for the test phone: `order_lines` (by the smoke order_id) → `orders`
    (the smoke order_id) → `sessions` (the test phone). Use the existing DB adapter (`execute` with
    `%s` placeholders) or the Supabase SQL editor — never psycopg2/DATABASE_URL. Decide the
    `customers`/`chat_history` rows for the test phone WITH Khaled (default per A4: leave them, note
    in the summary; delete if Khaled wants a fully clean slate). After deletion, confirm the smoke
    order no longer appears on /orders.
  </how-to-verify>
  <verify>Human-gated: confirmed by the "cleaned" resume signal (manual data cleanup against live Supabase; no automated check).</verify>
  <acceptance_criteria>
    - The smoke-test order_lines, orders, and sessions rows for the test phone are deleted from live Supabase.
    - The smoke order no longer appears on the /orders dashboard.
    - The customers/chat_history retention decision (leave vs delete) is recorded in the summary (A4).
    - No psycopg2 / DATABASE_URL was used — deletion went through the existing adapter or Supabase SQL editor.
  </acceptance_criteria>
  <done>The throwaway smoke order_lines/orders/sessions rows are deleted, /orders no longer shows the smoke order, and the customers/chat_history decision is recorded.</done>
  <resume-signal>Type "cleaned" once the smoke order_lines/orders/sessions are deleted and /orders no longer shows it. Note the customers/chat_history decision.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Meta portal / runtime env → secrets | WA_META_TOKEN and the phone ID are live credentials entering the runtime env. |
| Live proof → production data | This plan intentionally writes ONE real row to live Supabase, then deletes it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Information Disclosure | WA_META_TOKEN handling | mitigate | Token lives only in the runtime env via Config.WA_META_TOKEN; never hardcoded, never committed (.env stays out of git). System User token preferred over temp to avoid re-minting churn. |
| T-01-07 | Tampering | Smoke order pollutes production data | mitigate | D-07 cleanup task deletes the throwaway order_lines→orders→sessions rows in FK-safe order via the existing `%s`-placeholder adapter after the proof. |
| T-01-08 | Repudiation | Live proof claimed but not actually delivered to the aunt | mitigate | Evidence required: /orders screenshot, aunt-phone screenshot, captured inbound `wamid`. Mock/[DEV] output explicitly does NOT satisfy D-01. |
| T-01-09 | Spoofing | Unauthenticated webhook POST (no HMAC) accepts the live inbound | accept (deferred to M2) | Signature verification is EXPLICITLY M2 per the scope fence. The ≤5 test-recipient allow-list naturally limits exposure during the proof window; full hardening is M2. |

No high-severity threat is left unmitigated within Phase 1's scope. T-01-09 is a documented M2 deferral.
</threat_model>

<verification>
- This plan is human-gated; its verification is the three checkpoints above (token ready → proven → cleaned).
- The automated suite from 01-01 must already be green before this plan runs (it is the Wave 1 dependency).
- Phase gate (per VALIDATION.md): full suite green AND this live-proof checkpoint signed off (D-01).
</verification>

<success_criteria>
- A real WhatsApp message from a registered test number created a real order in live Supabase,
  visible on /orders, with no 422.
- The aunt's phone actually received the 🛍️ طلب جديد! notification via the real Meta sender.
- The smoke-test order was deleted (D-07); production is not polluted.
- The Meta token/phone-ID/recipients/webhook were provisioned as an in-phase prerequisite (D-02),
  not deferred.
</success_criteria>

<output>
Create `.planning/phases/01-spine-smoke-thread/01-02-SUMMARY.md` when done — record the live-proof
evidence (no-422 confirmation, /orders + aunt-phone screenshot references, captured wamid) and the
D-07 cleanup confirmation incl. the customers/chat_history retention decision.
</output>
