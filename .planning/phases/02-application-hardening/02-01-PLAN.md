---
phase: 02-application-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [app/main.py, app/routers/whatsapp.py, app/services/whatsapp_meta.py, app/shared/logging.py, requirements.txt]
autonomous: true
requirements: [REQ-prod-struct-log, REQ-prod-metrics, REQ-prod-raw-hmac, REQ-prod-inbox, REQ-nfr-latency]
must_haves:
  truths:
    - "Application logs are in structured JSON format"
    - "GET /health returns 200 OK with database connectivity status"
    - "POST /whatsapp/webhook rejects requests with invalid HMAC signatures"
    - "POST /whatsapp/webhook returns 200 OK immediately after persisting event to DB"
  artifacts:
    - path: "app/shared/logging.py"
      provides: "structlog configuration"
    - path: "app/main.py"
      provides: "Health check endpoint and logging init"
    - path: "app/routers/whatsapp.py"
      provides: "Hardened webhook entry point"
  key_links:
    - from: "app/routers/whatsapp.py"
      to: "webhook_events table"
      via: "db.rpc('ingest_webhook')"
---

<objective>
Harden the application's external boundary and improve observability.
Purpose: Ensure all incoming data is authentic, logged correctly, and persisted durably before processing.
Output: Integrated structured logging, health monitoring, and a hardened webhook inbox.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/RESEARCH-2.md
@app/main.py
@app/routers/whatsapp.py
@app/services/whatsapp_meta.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Structured Logging & Health Check</name>
  <files>requirements.txt, app/shared/logging.py, app/main.py</files>
  <action>
    1. Add 'structlog' and 'orjson' to requirements.txt.
    2. Create app/shared/logging.py to configure structlog with JSON renderer for production (env-based switch).
    3. Initialize structlog in app/main.py and replace standard 'logging' basicConfig.
    4. Add a GET /health endpoint to app/main.py that calls db.ping() and returns 200 if OK.
  </action>
  <verify>
    <automated>curl http://localhost:8000/health returns {"ok": true}</automated>
  </verify>
  <done>Logs are JSON formatted and health endpoint is functional.</done>
</task>

<task type="auto">
  <name>Task 2: HMAC Webhook Verification</name>
  <files>app/services/whatsapp_meta.py, app/routers/whatsapp.py</files>
  <action>
    1. Implement signature verification in app/services/whatsapp_meta.py using hmac.compare_digest and WA_META_APP_SECRET.
    2. Update app/routers/whatsapp.py to enforce signature verification on the POST /webhook route. 
    3. Ensure verification works for both mock (skipped if USE_MOCK_WHATSAPP=1) and real Meta requests.
  </action>
  <verify>
    <automated>pytest tests/unit/test_whatsapp_meta.py (Create if needed to test verification logic)</automated>
  </verify>
  <done>Invalid signatures return 403 Forbidden; valid signatures proceed.</done>
</task>

<task type="auto">
  <name>Task 3: Durable Webhook Ingestion (Inbox Pattern)</name>
  <files>app/routers/whatsapp.py</files>
  <action>
    1. Update the POST /whatsapp/webhook endpoint to extract the raw request body.
    2. Extract 'wamid' (WhatsApp Message ID) from the Meta payload.
    3. Use db.execute to insert the raw JSON, sender, and wamid into the 'webhook_events' table (status='pending').
    4. Return a 200 OK response immediately after successful insertion.
    5. Note: This will temporarily stop message processing until Plan 02-02 implements the worker.
  </action>
  <verify>
    <automated>Send mock webhook; verify it appears in 'webhook_events' table with 'pending' status.</automated>
  </verify>
  <done>Webhooks are persisted to DB and acknowledged fast (< 1s).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public Internet → API | Untrusted webhook data enters the system here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | /whatsapp/webhook | mitigate | Validate X-Hub-Signature-256 using HMAC-SHA256 with Meta App Secret. |
| T-02-02 | Denial of Service | /whatsapp/webhook | mitigate | Immediate persistence to DB and 200 OK return to prevent worker exhaustion. |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
Check health endpoint: `curl -I http://localhost:8000/health`
Check logs: `tail -f logs/app.log` (should be JSON)
Check DB: `SELECT count(*) FROM webhook_events WHERE status = 'pending';`
</verification>

<success_criteria>
- All 5 requirements (REQ-prod-struct-log, REQ-prod-metrics, REQ-prod-raw-hmac, REQ-prod-inbox, REQ-nfr-latency) are satisfied.
- The system is ready for worker-based processing.
</success_criteria>

<output>
Create .planning/phases/02-application-hardening/02-01-SUMMARY.md when done
</output>
