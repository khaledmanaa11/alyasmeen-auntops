---
phase: 02-application-hardening
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: [app/main.py, app/worker.py, app/services/processor.py, app/services/config.py, Procfile, requirements.txt]
autonomous: true
requirements: [REQ-bot-webhook, REQ-prod-outbox, REQ-prod-idempotency, REQ-prod-cicd, REQ-prod-pinned-model, REQ-nfr-uptime]
must_haves:
  truths:
    - "Web and Worker processes run independently"
    - "APScheduler runs exactly once in the Worker process"
    - "Duplicate webhooks (same wamid) are ignored by the worker"
    - "Outgoing messages are sent via the outbox processing logic"
  artifacts:
    - path: "app/worker.py"
      provides: "Worker processing loop and scheduler"
    - path: "app/services/processor.py"
      provides: "Decoupled bot logic for worker consumption"
    - path: "Procfile"
      provides: "Process orchestration for Railway"
  key_links:
    - from: "app/worker.py"
      to: "webhook_events table"
      via: "Polling status='pending'"
    - from: "app/worker.py"
      to: "outbox_jobs table"
      via: "Polling status='queued'"
---

<objective>
Separate the application into Web and Worker roles and implement the core processing logic.
Purpose: Ensure long-running tasks (AI, PDF) don't block webhooks and provide horizontal scalability.
Output: Dedicated worker process, decoupled bot logic, and deployment configuration.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-application-hardening/02-01-SUMMARY.md
@app/main.py
@app/routers/whatsapp.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Process Separation & Scheduler Migration</name>
  <files>requirements.txt, app/main.py, app/worker.py</files>
  <action>
    1. Add 'SQLAlchemy' to requirements.txt (required for APScheduler JobStore).
    2. Create app/worker.py using BlockingScheduler.
    3. Configure SQLAlchemyJobStore for APScheduler in app/worker.py using SUPABASE_URL (postgres connection string).
    4. Move the @app.on_event("startup") scheduler jobs from app/main.py to app/worker.py.
    5. Remove scheduler initialization from app/main.py.
  </action>
  <verify>
    <automated>Run 'python app/worker.py' and verify it connects to DB and starts without error.</automated>
  </verify>
  <done>Scheduler is moved to a dedicated process with persistent state.</done>
</task>

<task type="auto">
  <name>Task 2: Worker Processing Loop & Idempotency</name>
  <files>app/worker.py, app/services/processor.py, app/services/config.py</files>
  <action>
    1. Create app/services/processor.py and move the core message handling logic from app/routers/whatsapp.py to it (the part that calls AI and tools).
    2. Implement a polling loop in app/worker.py that:
       - Fetches 'pending' records from 'webhook_events'.
       - Checks 'processed_events' or 'webhook_events' (status='processed') for existing 'wamid' to ensure idempotency.
       - Processes the event using processor.py.
       - Marks record as 'processed' (or 'failed' on error).
    3. Implement a similar loop for 'outbox_jobs' to send outgoing messages.
    4. Ensure Config.CLAUDE_MODEL is explicitly set and validated in app/services/config.py (REQ-prod-pinned-model).
  </action>
  <verify>
    <automated>pytest tests/integration/test_bot_flow.py (Should now work via worker processing)</automated>
  </verify>
  <done>Worker successfully processes webhooks and jobs with deduplication.</done>
</task>

<task type="auto">
  <name>Task 3: Process Orchestration & CI/CD</name>
  <files>Procfile</files>
  <action>
    1. Create a Procfile in the project root.
    2. Define the 'web' process: 'uvicorn app.main:app --host 0.0.0.0 --port $PORT'.
    3. Define the 'worker' process: 'python app/worker.py'.
    4. Ensure nixpacks.toml or other deployment configs are compatible with multiple roles.
  </action>
  <verify>
    <automated>Verify Procfile syntax.</automated>
  </verify>
  <done>Railway-compatible process definition is present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Web Process → Worker Process | Communicates via the Database (webhook_events/outbox_jobs tables). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Repudiation | app/worker.py | mitigate | Log every event processing attempt with status and duration in audit_logs. |
| T-02-04 | Information Disclosure | logs | mitigate | Ensure structlog renderer masks PII (phone numbers) in logs if necessary, or secure log storage. |
</threat_model>

<verification>
Check worker logs for processing signals.
Check DB: `SELECT count(*) FROM webhook_events WHERE status = 'processed';`
Verify outbound messages are being sent (check outbox_jobs table).
</verification>

<success_criteria>
- All remaining requirements (REQ-bot-webhook, REQ-prod-outbox, REQ-prod-idempotency, REQ-prod-cicd, REQ-prod-pinned-model, REQ-nfr-uptime) are satisfied.
- The system is fully separated and durable.
</success_criteria>

<output>
Create .planning/phases/02-application-hardening/02-02-SUMMARY.md when done
</output>
