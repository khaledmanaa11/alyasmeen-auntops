---
phase: 02-schema-correctness-integrity
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [app/main.py, tests/unit/test_schema_validation.py, tests/integration/test_schema_sync.py]
autonomous: true
requirements: [SCH-03]

must_haves:
  truths:
    - "FastAPI uses the lifespan context manager for startup/shutdown"
    - "validate_schema() is called at application startup"
    - "Application fails to start if validate_schema() raises RuntimeError"
    - "Unit tests prove validation logic correctly identifies missing columns"
    - "Integration tests prove live Supabase schema is in sync"
  artifacts:
    - path: "app/main.py"
      provides: "Application entry point with schema validation on startup"
    - path: "tests/unit/test_schema_validation.py"
      provides: "Validation logic verification (mocks)"
    - path: "tests/integration/test_schema_sync.py"
      provides: "Live schema verification"
  key_links:
    - from: "app/main.py"
      to: "validate_schema()"
      via: "lifespan startup call"
      pattern: "validate_schema\\(\\)"
---

<objective>
Integrate schema validation into the application lifecycle using FastAPI lifespan and provide comprehensive test coverage to ensure ongoing schema integrity.

Purpose: Fail fast on schema drift at startup and verify correctness against live Supabase.
Output: Wired startup check and new test suites.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-schema-correctness-integrity/02-RESEARCH.md
@app/main.py
@app/db/database.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate app/main.py to lifespan and wire validate_schema()</name>
  <files>app/main.py</files>
  <behavior>
    - Ensure `@app.on_event("startup")` and `@app.on_event("shutdown")` are removed.
    - Ensure `lifespan` async context manager is used instead.
    - Ensure `validate_schema()` is called at the very beginning of the lifespan startup.
    - Ensure all existing startup tasks (APScheduler) and shutdown tasks are preserved within the lifespan.
  </behavior>
  <action>
    Refactor `app/main.py` per SCH-03 and 02-RESEARCH.md.
    - Import `lifespan` from `contextlib` (if needed) or use `FastAPI(lifespan=...)`.
    - Create the `lifespan` function:
      ```python
      @asynccontextmanager
      async def lifespan(app: FastAPI):
          # Startup
          validate_schema()
          # ... existing start_scheduler logic ...
          yield
          # Shutdown
          # ... existing stop_scheduler logic ...
      ```
    - Pass the `lifespan` function to the `FastAPI` constructor.
    - Verify that `uvicorn app.main:app` fails if `validate_schema` is forced to fail.
  </action>
  <verify>
    <automated>python -c "from app.main import app; print(app.router.lifespan_context)"</automated>
  </verify>
  <done>Startup schema validation is wired and application uses modern lifespan handlers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create unit and integration tests for schema validation</name>
  <files>tests/unit/test_schema_validation.py, tests/integration/test_schema_sync.py</files>
  <behavior>
    - Unit Test: Mock `query` to return a subset of columns and assert `validate_schema` raises `RuntimeError`.
    - Unit Test: Mock `query` to return all required columns and assert `validate_schema` passes.
    - Integration Test: Call `validate_schema()` and assert no exception is raised (requires `SUPABASE_KEY`).
  </behavior>
  <action>
    Implement testing suite per Phase Requirements.
    - Create `tests/unit/test_schema_validation.py` using `pytest` and `unittest.mock`.
    - Create `tests/integration/test_schema_sync.py`.
    - The integration test should use the real `database.query` to talk to Supabase.
    - This provides the final verification (SCH-01, SCH-02) that the live database is correct.
  </action>
  <verify>
    <automated>pytest tests/unit/test_schema_validation.py tests/integration/test_schema_sync.py</automated>
  </verify>
  <done>Full test coverage for schema integrity is in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Lifespan Startup | Critical failure point — if validation fails, the entire process terminates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Denial of Service | main.py | accept | Intentional DoS at startup if schema is drift; this is a safety feature to prevent mid-run crashes and data corruption. |
</threat_model>

<verification>
Run `pytest` on the new test files.
</verification>

<success_criteria>
1. `app/main.py` uses `lifespan`.
2. Startup fails if schema is missing `monthly_snapshots` or other columns.
3. Tests prove validation logic works and live schema is correct.
</success_criteria>

<output>
Create `.planning/phases/02-schema-correctness-integrity/02-02-SUMMARY.md` when done
</output>
