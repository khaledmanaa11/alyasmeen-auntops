---
phase: 02-schema-correctness-integrity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [app/db/schema.sql, app/db/database.py]
autonomous: true
requirements: [SCH-01, SCH-02, SCH-03]

must_haves:
  truths:
    - "schema.sql contains the monthly_snapshots table definition"
    - "schema.sql uses ALTER TABLE ... ADD COLUMN IF NOT EXISTS for all columns"
    - "database.py has a REQUIRED_SCHEMA constant matching all tables/columns"
    - "database.py has a validate_schema() function that checks the information_schema"
  artifacts:
    - path: "app/db/schema.sql"
      provides: "Idempotent database schema source of truth"
    - path: "app/db/database.py"
      provides: "Schema validation logic"
  key_links:
    - from: "validate_schema()"
      to: "information_schema.columns"
      via: "query()"
      pattern: "information_schema\\.columns"
---

<objective>
Harden the database schema by making schema.sql idempotent and adding the missing monthly_snapshots table. Implement the core validation logic in database.py to prepare for startup checks.

Purpose: Fix missing-table crashes and ensure the codebase has a verified source of truth for the database structure.
Output: Updated schema.sql and new validate_schema() library.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-schema-correctness-integrity/02-RESEARCH.md
@app/db/schema.sql
@app/db/database.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update app/db/schema.sql for idempotency and missing table</name>
  <files>app/db/schema.sql</files>
  <behavior>
    - Ensure `monthly_snapshots` table is defined (year, month, data, created_at).
    - Ensure all `CREATE TABLE` statements use `IF NOT EXISTS`.
    - For EVERY column in EVERY table, add an `ALTER TABLE ... ADD COLUMN IF NOT EXISTS` statement.
    - This ensures that re-running the script against an existing Supabase instance adds any missing columns without failing on existing ones.
  </behavior>
  <action>
    Modify `app/db/schema.sql` per SCH-01 and SCH-02.
    - Add the `monthly_snapshots` table (as researched in 02-RESEARCH.md).
    - Add primary key and index on `(year, month)` for `monthly_snapshots`.
    - Append `ALTER TABLE ... ADD COLUMN IF NOT EXISTS ...` blocks for products, customers, orders, order_lines, sessions, chat_history, follow_ups, and retry_queue.
    - Ensure all constraints and types match the existing definitions but in an idempotent way.
  </action>
  <verify>
    <automated>grep -q "monthly_snapshots" app/db/schema.sql && grep -q "ADD COLUMN IF NOT EXISTS" app/db/schema.sql</automated>
  </verify>
  <done>schema.sql is fully idempotent and includes the monthly_snapshots table.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement validate_schema() in app/db/database.py</name>
  <files>app/db/database.py</files>
  <behavior>
    - Test 1: `REQUIRED_SCHEMA` contains all 9 tables (products, customers, orders, order_lines, sessions, chat_history, follow_ups, retry_queue, monthly_snapshots).
    - Test 2: `validate_schema()` queries `information_schema.columns`.
    - Test 3: `validate_schema()` raises `RuntimeError` if a table or column is missing.
  </behavior>
  <action>
    Implement `REQUIRED_SCHEMA` and `validate_schema()` per SCH-03 and 02-RESEARCH.md Pattern 2.
    - Define `REQUIRED_SCHEMA` as a dictionary of table names to sets of column names.
    - Include all columns currently defined in `schema.sql`.
    - Implement `validate_schema()`:
      1. Call `query("SELECT table_name, column_name FROM information_schema.columns WHERE table_schema = 'public'")`.
      2. Group columns by table.
      3. Compare against `REQUIRED_SCHEMA`.
      4. Raise `RuntimeError` with a clear message if drift is detected.
  </action>
  <verify>
    <automated>python -c "from app.db.database import validate_schema; print('Function exists')"</automated>
  </verify>
  <done>Schema validation logic is implemented and ready for wiring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend→Supabase | Schema metadata is fetched from PostgreSQL information_schema. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | database.py | mitigate | Use existing `query()` helper which uses parameterized RPC calls to avoid SQL injection if table names were ever dynamic (though they are hardcoded here). |
| T-02-02 | Information Disclosure | database.py | accept | Querying `information_schema` reveals table names/columns to the app; this is expected behavior for validation. |
</threat_model>

<verification>
Check that `app/db/schema.sql` can be parsed and that `validate_schema` is exported by `database.py`.
</verification>

<success_criteria>
1. `schema.sql` includes `monthly_snapshots`.
2. `schema.sql` uses idempotent `ADD COLUMN IF NOT EXISTS` for all columns.
3. `database.py` contains the `REQUIRED_SCHEMA` map and `validate_schema()` function.
</success_criteria>

<output>
Create `.planning/phases/02-schema-correctness-integrity/02-01-SUMMARY.md` when done
</output>
