---
phase: 04-reliability-operations-completion
plan: 07
type: execute
wave: 4
depends_on: ["04-01", "04-02", "04-03", "04-04", "04-05", "04-06"]
files_modified: [supabase/migrations/20260825000004_revert_anon_grants.sql, CLAUDE.md, docs/BACKUP_DRILL.md]
autonomous: false
user_setup:
  - service: supabase
    why: "Switch the app's DB credential to service_role, apply all pending migrations to the live project, and run a real backup/restore drill — all require live Supabase project access this environment does not have."
    env_vars:
      - name: SUPABASE_KEY
        source: "Supabase Dashboard → Project Settings → API → service_role (secret) key, for project ppwcfmuetgczclmnzvqr"
    dashboard_config:
      - task: "Get the Postgres database password for `supabase link`"
        location: "Supabase Dashboard → Settings → Database"
      - task: "Get the Session Pooler connection string for DATABASE_URL"
        location: "Supabase Dashboard → Settings → Database → Connection string → Session pooler"
  - service: railway
    why: "SUPABASE_KEY and DATABASE_URL must both be updated on the live deployment, not just locally/documented — SUPABASE_KEY for the service_role switch (web + worker), DATABASE_URL for the worker's persistent APScheduler job store."
    env_vars:
      - name: SUPABASE_KEY
        source: "Same service_role key as above — set in Railway's Variables tab for this service"
      - name: DATABASE_URL
        source: "Supabase Session Pooler connection string (see supabase dashboard_config above) — set on the Railway WORKER service specifically, postgresql:// scheme, not the Direct connection string"
requirements: [REQ-prod-backup-restore, REQ-nfr-uptime]
must_haves:
  truths:
    - "The deployed app (Railway) and local dev both run on the Supabase service_role key, verified end-to-end before any grant migration ships"
    - "anon/authenticated can no longer execute run_query/run_exec after the revert migration is applied"
    - "All files in supabase/migrations/ are applied to the live Supabase project"
    - "A real backup was taken and a real restore was performed into a fresh project, with a truthful result recorded in docs/BACKUP_DRILL.md's Drill Log"
    - "The worker's APScheduler job store is backed by DATABASE_URL in production (Railway) and next_run_time for a real job survives an actual worker restart, verified against the live apscheduler_jobs table"
  artifacts:
    - path: "supabase/migrations/20260825000004_revert_anon_grants.sql"
      provides: "Revokes run_query/run_exec EXECUTE from anon/authenticated again, undoing 20260825000001"
    - path: "docs/BACKUP_DRILL.md"
      provides: "Real no-Docker CLI drill procedure + a Drill Log row with a genuine result"
  key_links:
    - from: "Railway + local .env SUPABASE_KEY"
      to: "app/db/database.py:_get_client()"
      via: "Config.SUPABASE_KEY passed to create_client()"
      pattern: "service_role"
    - from: "Railway worker service DATABASE_URL"
      to: "app/worker.py:SQLAlchemyJobStore(url=Config.DATABASE_URL)"
      via: "Supabase Session Pooler connection string"
      pattern: "SQLAlchemyJobStore"
---

<objective>
Execute Phase 4 decision 3 (switch to the Supabase `service_role` key, in the exact safe
sequence the research verified) and close out the three remaining success criteria that
require live Supabase/Railway access: making the worker's job store persistent in production
and proving it survives a real restart, applying every migration to the live project, and
running a real backup/restore drill. This plan gates on every other Phase 4 plan being
complete and the full test suite green — the service_role switch is the highest-risk step
(flipping production DB credentials and grants) and should only run once everything else is
verified working.

Purpose: the app must actually run on the credential CLAUDE.md claims it does, the worker's
scheduler must actually survive a restart in production (not just in 04-03's local proof),
every migration in the repo must actually be applied to the live database, and the
backup/restore drill must have actually happened at least once — none of this may remain
merely documented-as-pending.
Output: the revert-anon-grants migration + updated docs (automatable), followed by three
operator checkpoints that make the job store persistent in production, perform the live
credential switch, and run the migration rollout + backup drill for real.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-reliability-operations-completion/04-CONTEXT.md
@.planning/phases/04-reliability-operations-completion/04-RESEARCH.md
@.planning/phases/04-reliability-operations-completion/04-03-SUMMARY.md
@.planning/STATE.md
@supabase/migrations/20260614000003_security_rls.sql
@supabase/migrations/20260825000001_fix_rpc_grants.sql
@app/worker.py
@docs/BACKUP_DRILL.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the revert migration and update docs for the live rollout</name>
  <files>supabase/migrations/20260825000004_revert_anon_grants.sql, CLAUDE.md, docs/BACKUP_DRILL.md</files>
  <action>
    Create `supabase/migrations/20260825000004_revert_anon_grants.sql`, mirroring
    `20260825000001_fix_rpc_grants.sql`'s self-documenting header style and its `IF EXISTS`
    guard pattern:
    ```sql
    DO $$
    BEGIN
      IF EXISTS (SELECT 1 FROM pg_proc WHERE proname = 'run_query') THEN
        REVOKE EXECUTE ON FUNCTION run_query(TEXT) FROM anon, authenticated;
      END IF;
      IF EXISTS (SELECT 1 FROM pg_proc WHERE proname = 'run_exec') THEN
        REVOKE EXECUTE ON FUNCTION run_exec(TEXT) FROM anon, authenticated;
      END IF;
    END $$;
    ```
    In the header comment, explain: this undoes `20260825000001`'s temporary anon re-grant now
    that the app runs on `service_role` — which was never affected by either grant/revoke,
    since `20260614000003_security_rls.sql`'s original `REVOKE` only targeted
    `anon, authenticated, public`. State explicitly, in bold/caps in the comment: "APPLY THIS
    MIGRATION ONLY AFTER confirming SUPABASE_KEY=service_role is live in BOTH Railway and local
    .env and the app has been verified working end-to-end (see Task 3 of this plan) —
    applying it first reproduces the exact outage 20260825000001 was written to fix (the app
    had no way to reach the database at all)."

    Update `CLAUDE.md`:
    - In the "Database Connection (Supabase via HTTPS)" section, change the documented key from
      anon to service_role, and add a short security note: service_role bypasses RLS and must
      never be sent to any client-side/browser code — it is only ever read via
      `Config.SUPABASE_KEY` inside `database.py`, which runs exclusively in the FastAPI/worker
      server processes on Railway, never shipped to the dashboard's HTML/JS.
    - In "Env Vars Reference", update the `SUPABASE_KEY` row's description from an anon-key
      implication to: "Supabase **service_role** key (server-side only — never expose to the
      dashboard's client-side code)".

    Update `docs/BACKUP_DRILL.md`:
    - Replace the stale header `**Status:** Documented (Drill pending local environment setup)`
      — the installed Supabase CLI does NOT require Docker for the commands this drill actually
      needs (`supabase link`, `db push`, `db dump` all work without Docker; only local-dev-stack
      commands like `supabase start`/`db diff` need it, and this drill doesn't use those).
    - Rewrite "Step 1: Initialize a clean local environment" through "Step 2: Apply the backup"
      to the verified no-Docker sequence: `npx supabase link --project-ref <ref>` (prompts for
      the DB password from Supabase Dashboard → Settings → Database) → `npx supabase db push
      --linked` (applies pending migrations — this is also how Success Criterion 2's "all
      migrations applied to the live project" gets satisfied) → `npx supabase db dump --linked
      -f backup_<date>.sql` + `npx supabase db dump --linked --role-only -f roles_<date>.sql`
      → create a throwaway **free-tier** Supabase project as the restore target → `npx supabase
      link --project-ref <new-ref>` → `npx supabase db push --linked` (schema only) →
      `psql "<new-project-session-pooler-url>" -f backup_<date>.sql` (data — note: `psql`/
      `pg_dump` come from the official PostgreSQL Windows installer, typically under
      `C:\Program Files\PostgreSQL\<version>\bin\`, not on PATH by default).
    - Keep the existing "Step 3: Verification Check" queries as-is (already correct).
    - Leave the existing 2026-06-14 Drill Log row in place (historical) and add a note that a
      new row will be added once the drill is actually executed for real (Task 4 of this plan).
  </action>
  <verify>Visual review of the migration SQL and both docs. `python -m pytest -q` unaffected (docs/SQL only, no code touched).</verify>
  <done>The revert migration exists with a clear ordering warning in its header; CLAUDE.md and BACKUP_DRILL.md accurately describe the service_role decision and the real no-Docker drill procedure.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Set DATABASE_URL on Railway and verify the job store survives a worker restart</name>
  <files>Railway worker service Variables (DATABASE_URL)</files>
  <action>
    `app/worker.py` already does the right thing when `DATABASE_URL` is set
    (`SQLAlchemyJobStore(url=Config.DATABASE_URL)`) and falls back to an in-memory job store
    with a warning log when it isn't — 04-03 proved that mechanism generically against a local
    sqlite file. This step performs the equivalent check against the real production database,
    which needs live Railway/Supabase access this environment does not have. Perform it
    yourself; this has no ordering dependency on the service_role switch in Task 3 below, so it
    can be done independently:
    1. Get the Supabase Session Pooler connection string: Supabase Dashboard → Settings →
       Database → Connection string → **Session pooler** (host
       `aws-<region>.pooler.supabase.com`, port `5432`, user `postgres.<project-ref>`) — NOT
       the "Direct connection" string (`db.<ref>.supabase.co`), which is IPv6-only and will not
       resolve on Railway's default IPv4-only outbound networking. Confirm it starts with
       `postgresql://` (not `postgres://` — SQLAlchemy 2.0 rejects the old scheme).
    2. Set it as `DATABASE_URL` on the **worker** Railway service's Variables tab — this repo's
       `Procfile` defines separate `web` (`uvicorn app.main:app ...`) and `worker`
       (`python app/worker.py`) processes; only the worker process constructs the
       scheduler/job store, so setting this on the web service does nothing.
    3. Wait for the worker's next deploy, then check its logs for the line
       `"Using SQLAlchemyJobStore"` (already logged by `app/worker.py`). If you instead see
       `"DATABASE_URL not set, using MemoryJobStore (non-persistent)"`, the variable didn't
       reach the worker service — fix and redeploy before continuing.
    4. Verify persistence across a real restart: in the Supabase SQL editor (or via `psql`),
       run `SELECT id, next_run_time FROM apscheduler_jobs WHERE id = 'monthly_report';` and
       note the `next_run_time` value. Restart the worker service from the Railway dashboard.
       Once it's back up, run the same query again — `next_run_time` must be unchanged. This is
       the same assertion 04-03's sqlite test makes, now checked against the live table.
    5. Record the dated result (worker log line seen, next_run_time before/after) in this
       plan's SUMMARY, the same way Task 4's backup drill result gets recorded there.
  </action>
  <verify>
    <human-check>User confirms the worker's logs show "Using SQLAlchemyJobStore" (not the MemoryJobStore warning) and that apscheduler_jobs.next_run_time for 'monthly_report' is identical before and after a real worker restart.</human-check>
  </verify>
  <done>DATABASE_URL is set on the Railway worker service with the Session Pooler connection string; a real worker restart is proven not to lose the scheduler's next_run_time, verified against the live apscheduler_jobs table and recorded in the SUMMARY.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Switch to service_role and verify end-to-end</name>
  <files>Railway service Variables (SUPABASE_KEY), local .env (SUPABASE_KEY)</files>
  <action>
    Everything automatable for the service_role switch is already done: the code path
    (`app/db/database.py`) just reads whatever key is in `Config.SUPABASE_KEY`, and the revert
    migration (Task 1) is written and waiting — it must NOT be applied yet. This step is a
    human-only credential rotation + live verification that this environment cannot perform
    (no Railway/Supabase dashboard access). Perform it yourself:
    1. Get the `service_role` (secret) key: Supabase Dashboard → Project Settings → API, for
       project `ppwcfmuetgczclmnzvqr`.
    2. Set it as `SUPABASE_KEY` in Railway → this service's Variables tab (change nothing
       else). Railway will auto-redeploy.
    3. Set the same value as `SUPABASE_KEY` in your local `.env`, restart `uvicorn` locally.
    4. Verify Railway: `curl https://<your-railway-domain>/health` — expect
       `{"ok": true, "db": {"ok": true, ...}}`. If `{"ok": false}`, STOP — do not proceed to
       Task 4. The anon re-grant migration is still live at this point, so the app is not at
       risk of an outage while you debug.
    5. Verify a full order flow on Railway: send a real WhatsApp test message (or, if
       `USE_MOCK_WHATSAPP=1` is temporarily set for this check, `POST /dev/test_order`) and
       confirm the order appears on `/orders`.
    6. Repeat steps 4-5 against `http://localhost:8000` for local dev.
  </action>
  <verify>
    <human-check>User confirms /health returns ok=true on BOTH Railway and local, and a test order round-trips successfully on both, before Task 4 proceeds.</human-check>
  </verify>
  <done>SUPABASE_KEY is service_role in both Railway and local .env; /health and a full order flow are confirmed working end-to-end on both, while the anon re-grant migration is still live as a safety net.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Apply all migrations and run the backup/restore drill for real</name>
  <files>supabase/migrations/, docs/BACKUP_DRILL.md</files>
  <action>
    The revert-anon-grants migration (Task 1) and every other not-yet-applied migration in
    `supabase/migrations/` are ready to ship. `docs/BACKUP_DRILL.md` now documents the exact
    no-Docker CLI sequence for the drill. This step requires live Supabase CLI credentials this
    environment does not have — perform it yourself, only after Task 3 is confirmed:
    1. `npx supabase link --project-ref ppwcfmuetgczclmnzvqr` — enter the database password
       (Supabase Dashboard → Settings → Database) when prompted.
    2. `npx supabase db push --linked` — applies every migration not yet in
       `supabase_migrations.schema_migrations`, including this phase's retry_queue-retirement
       migration and the revert-anon-grants migration. Confirm it reports success with no
       errors.
    3. Re-run `curl https://<your-railway-domain>/health` once more — must still be
       `{"ok": true}` (proves service_role wasn't affected by the anon revoke, as designed —
       service_role's grants were never touched by either migration).
    4. Run the backup/restore drill per the updated `docs/BACKUP_DRILL.md`: dump the live
       project (`db dump --linked` + `--role-only`), create a throwaway free-tier Supabase
       project, link to it, `db push` the schema, load the data dump with `psql` (install the
       PostgreSQL Windows client if needed), then run the Step 3 verification queries (row
       counts on products/orders match production, recent orders present, audit_logs present).
    5. Record a real row in `docs/BACKUP_DRILL.md`'s Drill Log table: date, performed by,
       result, notes.
    6. Delete the throwaway restore-target Supabase project once verified.
  </action>
  <verify>
    <human-check>User confirms all migrations applied cleanly, /health still ok=true, the restore drill's verification queries matched production, and reports the exact Drill Log row recorded.</human-check>
  </verify>
  <done>supabase_migrations.schema_migrations on the live project contains every file in supabase/migrations/; docs/BACKUP_DRILL.md's Drill Log has a new, truthful, non-N/A row recording a real backup+restore result.</done>
</task>

</tasks>

<verification>
After Task 4 is confirmed by the user: `/health` returns ok=true against the live deployment;
the worker's logs show `"Using SQLAlchemyJobStore"` and `apscheduler_jobs.next_run_time` for
`monthly_report` survived a real restart (Task 2); `supabase_migrations.schema_migrations` on
the live project contains every file in `supabase/migrations/`; `docs/BACKUP_DRILL.md`'s
Drill Log has a non-N/A row with a real date and result.
</verification>

<success_criteria>
1. DATABASE_URL is set on the Railway worker service with the Session Pooler connection
   string, and a real worker restart is proven not to lose scheduler state.
2. The app runs on service_role in both Railway and local dev, verified end-to-end before the
   revert migration shipped.
3. anon/authenticated can no longer execute run_query/run_exec.
4. Every migration file is applied to the live Supabase project.
5. A real backup/restore drill has been executed and recorded truthfully in the Drill Log.
</success_criteria>

<output>
After completion, create `.planning/phases/04-reliability-operations-completion/04-07-SUMMARY.md`
</output>
