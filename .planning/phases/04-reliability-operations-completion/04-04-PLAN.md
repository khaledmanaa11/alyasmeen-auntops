---
phase: 04-reliability-operations-completion
plan: 04
type: execute
wave: 2
depends_on: ["04-01"]
files_modified: [app/worker.py, CLAUDE.md, supabase/migrations/20260825000003_retire_retry_queue.sql]
autonomous: true
requirements: [REQ-prod-outbox]
must_haves:
  truths:
    - "retry_queue.py and retry_actions.py no longer exist in the codebase"
    - "app/worker.py no longer schedules the retry_queue job"
    - "The retirement of retry_queue is documented in a migration, without an irreversible DROP TABLE in the same phase that also touches grants/RLS"
  artifacts:
    - path: "app/worker.py"
      provides: "Scheduler wiring with no retry_queue import or job"
    - path: "supabase/migrations/20260825000003_retire_retry_queue.sql"
      provides: "Self-documenting migration recording retry_queue's retirement"
  key_links: []
---

<objective>
Delete `app/services/retry_queue.py` and `app/services/retry_actions.py` now that plan 04-01
built and tested their replacement (the outbox's `pdf_invoice` job kind + `queue_text` calls in
every former retry_queue caller). Unwire the 15-minute `retry_queue` scheduler job from
`app/worker.py`, delete the two test files that exclusively tested the deleted modules, and
ship a migration documenting the retirement. This completes Phase 4 decision 1.

Purpose: no dead code may claim to run — retry_queue's `enqueue()` was never actually called by
anything after the outbox migration, and gatekeeper-style unwired code is exactly what Phase 4
exists to eliminate.
Output: retry_queue.py/retry_actions.py deleted, worker.py no longer schedules them, a
migration recording the retirement.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-reliability-operations-completion/04-CONTEXT.md
@.planning/phases/04-reliability-operations-completion/04-RESEARCH.md
@.planning/phases/04-reliability-operations-completion/04-01-SUMMARY.md
@app/worker.py
@supabase/migrations/20260825000001_fix_rpc_grants.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete retry_queue/retry_actions and unwire worker.py</name>
  <files>app/worker.py, CLAUDE.md</files>
  <action>
    First, re-verify (grep the whole repo, not just trust prior research) that nothing besides
    `app/worker.py` still imports `retry_queue` or `retry_actions` after plan 04-01's migration
    landed — `grep -rn "retry_queue\|retry_actions" app/ tests/` should only show
    `app/worker.py` (production) and the two test files this task deletes.

    In `app/worker.py`: remove the line `from app.services.retry_queue import process_retries`
    and the line `scheduler.add_job(process_retries, "interval", minutes=15, id="retry_queue",
    replace_existing=True)`. Remove `"retry_queue"` from the `jobs=[...]` list in the
    `logger.info("Worker scheduler initialized", jobs=[...], ...)` call.

    Delete `app/services/retry_queue.py` and `app/services/retry_actions.py` entirely.

    Delete `tests/unit/test_retry.py` and `tests/unit/test_retry_actions_pdf.py` — their
    coverage was already ported to `tests/unit/test_processor.py`'s `TestPdfInvoiceJobKind`
    class in plan 04-01 (the pdf_invoice-specific assertions); the remaining
    retry_queue/retry_actions behaviors (`enqueue`, `process_retries`,
    `send_text_ready`/`send_text_done`/`send_text_delivered` actions) have no replacement need
    since that mechanism no longer exists — the outbox's own `attempts`/`max_attempts` +
    `status='failed'` already covers the same retry/backoff intent generically for every job
    kind, not per-action.

    In `CLAUDE.md`'s "Project Structure" tree under `app/services/`, remove the two lines
    listing `retry_queue.py` and `retry_actions.py` (they no longer exist — leaving them in the
    docs would be exactly the kind of "claims to run" staleness Phase 4 exists to eliminate).
  </action>
  <verify>`grep -rn "retry_queue\|retry_actions" app/ tests/` returns nothing. `python -m pytest -q` — full suite green with the two files removed and no import errors from worker.py.</verify>
  <done>retry_queue.py and retry_actions.py no longer exist; worker.py no longer references them; CLAUDE.md's file tree is accurate.</done>
</task>

<task type="auto">
  <name>Task 2: Retirement migration for the retry_queue table</name>
  <files>supabase/migrations/20260825000003_retire_retry_queue.sql</files>
  <action>
    Create `supabase/migrations/20260825000003_retire_retry_queue.sql` with a header comment
    matching the self-documenting style of `20260825000001_fix_rpc_grants.sql`, explaining:
    the `retry_queue` table (from `20260614000000_baseline.sql`) is retired as of Phase 4 —
    `outbox_jobs` (added in `20260614000001_durable_messaging.sql`) is now the single retry
    mechanism for every durable send, including the PDF invoice action retry_queue used to
    dispatch via `retry_actions.py`. This migration is intentionally NOT a `DROP TABLE`: the
    table is already RLS-locked with no anon/authenticated policies (deny-all per
    `20260614000003_security_rls.sql`), so leaving it in place with zero application code
    reading or writing it is zero functional risk, while `DROP TABLE` is irreversible and
    untestable against the live production database from this repo's current tooling. Add a
    single no-op-safe statement documenting the retirement in the database itself:
    ```sql
    COMMENT ON TABLE retry_queue IS
      'RETIRED as of Phase 4 (2026-08-25+) — superseded by outbox_jobs. No application code '
      'reads or writes this table anymore. Safe to DROP in a future migration once confirmed '
      'unused in production for a safe period; not dropped here to keep this phase''s '
      'migrations additive/reversible.';
    ```
  </action>
  <verify>Visual review of the SQL for syntax correctness (a `COMMENT ON TABLE` statement, no schema/data change). `python -m pytest -q` unaffected (migration file only, not applied to any live/test DB in this repo's test suite).</verify>
  <done>A migration file exists documenting retry_queue's retirement in the database itself, without an irreversible DROP TABLE.</done>
</task>

</tasks>

<verification>
`python -m pytest -q` full suite green. `git status`/`grep` confirm retry_queue.py,
retry_actions.py, and their test files are gone, with no dangling imports anywhere in
app/ or tests/.
</verification>

<success_criteria>
1. retry_queue.py and retry_actions.py are deleted from the repository.
2. app/worker.py no longer schedules or imports retry_queue.
3. A migration documents the table's retirement without an irreversible DROP TABLE.
4. Full test suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/04-reliability-operations-completion/04-04-SUMMARY.md`
</output>
