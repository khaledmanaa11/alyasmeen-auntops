---
phase: 04-reliability-operations-completion
plan: 06
type: execute
wave: 3
depends_on: ["04-05"]
files_modified: [app/routers/ui.py, app/templates/alerts.html, app/templates/orders.html, app/templates/dashboard.html, app/templates/products.html, app/templates/broadcast.html, tests/integration/test_ui_api.py]
autonomous: true
requirements: [REQ-prod-metrics]
must_haves:
  truths:
    - "An operator can open a dashboard tab that lists dead-lettered webhook_events and permanently-failed outbox_jobs and retry either with one click"
    - "The new tab is reachable from every existing dashboard page via the shared navbar, following the existing design system"
  artifacts:
    - path: "app/templates/alerts.html"
      provides: "Alerts dashboard page — lists dead-lettered items with retry buttons, follows the #006948/Material Symbols/RTL design system"
    - path: "app/routers/ui.py"
      provides: "GET /alerts page route"
  key_links:
    - from: "app/templates/alerts.html"
      to: "/api/alerts"
      via: "fetch on page load"
      pattern: "fetch\\('/api/alerts'\\)"
    - from: "app/templates/alerts.html"
      to: "/api/alerts/{table}/{id}/retry"
      via: "fetch POST on retry button click"
      pattern: "/retry"
---

<objective>
Build the operator-facing Alerts dashboard page that consumes plan 04-05's `/api/alerts` API —
a 5th nav tab ("تنبيهات") added consistently to every existing dashboard template, following
the exact design system already in place (glassmorphism navbar, `#006948` primary, Material
Symbols Outlined, Cairo font, Arabic RTL, card-based layout matching `products.html`'s closest
existing pattern). This completes Success Criterion 4.

Purpose: a stuck message must be visible and one-click recoverable by the operator, not a
silent, invisible loss.
Output: `app/templates/alerts.html`, `GET /alerts` route in `ui.py`, and a consistent nav-tab
addition across all 4 existing dashboard templates.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-reliability-operations-completion/04-CONTEXT.md
@.planning/phases/04-reliability-operations-completion/04-RESEARCH.md
@.planning/phases/04-reliability-operations-completion/04-05-SUMMARY.md
@app/routers/ui.py
@app/templates/products.html
@app/templates/orders.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Alerts page — route + template</name>
  <files>app/routers/ui.py, app/templates/alerts.html, app/templates/orders.html, app/templates/dashboard.html, app/templates/products.html, app/templates/broadcast.html</files>
  <action>
    In `app/routers/ui.py`, add a new page route following the exact pattern of
    `products_page()`:
    ```python
    @router.get("/alerts", response_class=HTMLResponse)
    async def alerts_page(request: Request):
        if not _is_authenticated(request):
            return RedirectResponse(url="/login", status_code=303)
        return templates.TemplateResponse(request, "alerts.html")
    ```

    Create `app/templates/alerts.html` using `app/templates/products.html` as the structural
    template (same `<head>` block: Cairo + Material Symbols Outlined font links, Tailwind CDN,
    Alpine.js CDN, identical `.nav-glass`/`.nav-active`/`.nav-link`/`.product-card`-equivalent
    CSS tokens — reuse the same `#006948`/`#004d33`/`#e6f3ee`/`#f0f7f4`/`#e8f3ee` design tokens
    and `20px` card border-radius / `1px solid #e8f3ee` border / Material Symbols icons). Build
    an Alpine.js `alertsApp()` root with:
    - On `init()`: `fetch('/api/alerts')`, store `webhookEvents` and `outboxJobs` arrays.
    - Two sections, each a list of cards (mirror `products.html`'s card + toast/loading/empty-
      state patterns): "رسائل واردة معلّقة" (webhook_events — show phone, a trimmed error
      string, attempts count, created_at) and "رسائل صادرة فشلت" (outbox_jobs — show phone,
      kind, last_error, attempts/max_attempts, created_at). Each row/card has a retry button
      (Material Symbols `refresh` icon, label "إعادة المحاولة") that POSTs to
      `/api/alerts/webhook_events/{id}/retry` or `/api/alerts/outbox_jobs/{id}/retry`
      respectively, then removes the row from the local list + shows a toast ("تم جدولة إعادة
      المحاولة ✅") on success — reuse `products.html`'s toast mechanism verbatim.
    - Empty state when BOTH lists are empty: "لا توجد رسائل معلّقة 🎉" (mirror
      `products.html`'s empty-state markup/icon-circle pattern).
    - Loading spinner while the initial fetch is in flight (mirror `products.html`'s loading
      block).
    - A short tooltip/caption near each retry button noting: "قد يكرر هذا بعض الإجراءات إذا
      فشلت الرسالة الأصلية جزئياً" (this may repeat some actions if the original message
      partially failed) — surfaces the partial-retry caveat documented in 04-RESEARCH.md /
      plan 04-05's SUMMARY so the operator isn't surprised.

    Add the 5th nav tab to `alerts.html`'s own navbar (marked `nav-active`) AND to the other 4
    templates' navbars (`orders.html`, `dashboard.html`, `products.html`, `broadcast.html`,
    each marked `nav-link` there), inserted after the `/broadcast` link and before `/logout`,
    following the exact existing markup shape:
    ```html
    <a href="/alerts" class="nav-link px-3 py-1.5 rounded-xl text-sm font-medium flex items-center gap-1">
      <span class="material-symbols-outlined" style="font-size:18px">warning</span>
      تنبيهات
    </a>
    ```
    (`login.html` has no navbar — do not touch it.)
  </action>
  <verify>Manual visual review of alerts.html's structure against products.html's; confirm all 5 templates now list the same 5 nav links in the same order with exactly one marked nav-active per page.</verify>
  <done>Visiting /alerts (authenticated) renders the alerts dashboard with both dead-letter lists and working retry buttons; every other dashboard page links to it via the shared navbar.</done>
</task>

<task type="auto">
  <name>Task 2: Page-route tests</name>
  <files>tests/integration/test_ui_api.py</files>
  <action>
    In `tests/integration/test_ui_api.py`'s existing `TestPageRoutes` class, add two tests
    following the exact pattern of `test_products_page_renders_when_authenticated` /
    `test_orders_page_redirects_to_login_unauthenticated`:
    - `test_alerts_page_redirects_to_login_unauthenticated`: `client.get("/alerts",
      follow_redirects=False)` → 303, `location` header contains `/login`.
    - `test_alerts_page_renders_when_authenticated`: `auth_client.get("/alerts")` → 200.
  </action>
  <verify>`python -m pytest tests/integration/test_ui_api.py -q` — all tests including the two new ones pass.</verify>
  <done>The /alerts route's auth guard and successful render are covered by automated tests, consistent with every other dashboard page.</done>
</task>

</tasks>

<verification>
`python -m pytest -q` full suite green. Grep all 5 templates for `href="/alerts"` — must appear
in all 5 (4 as nav-link, 1 as nav-active in alerts.html itself).
</verification>

<success_criteria>
1. /alerts renders a working dashboard listing dead-lettered webhook_events and
   permanently-failed outbox_jobs, each retryable with one click.
2. The design system (#006948, Material Symbols, Cairo, Arabic RTL, card layout) is followed
   exactly — no new visual pattern introduced.
3. All 4 existing dashboard templates link to the new tab.
4. Full test suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/04-reliability-operations-completion/04-06-SUMMARY.md`
</output>
