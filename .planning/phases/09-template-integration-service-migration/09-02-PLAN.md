---
phase: 09-template-integration-service-migration
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified: [app/services/followup.py, app/services/monthly_report.py]
autonomous: true
requirements: [REQ-M2-03-A, REQ-M2-03-B]
must_haves:
  truths:
    - "Scheduled follow-up jobs use the 'order_followup' template"
    - "Monthly report jobs use the 'monthly_summary' template"
    - "Both services queue jobs in the outbox with appropriate template parameters"
  artifacts:
    - path: "app/services/followup.py"
      provides: "Template-based follow-up logic"
    - path: "app/services/monthly_report.py"
      provides: "Template-based report delivery"
---

<objective>
Migrate the Follow-up and Monthly Report services to use WhatsApp Templates. This ensures that these business-initiated messages can be delivered even if the customer (or the Aunt) has not interacted with the bot in the last 24 hours.

Purpose: Fix out-of-window messaging risk for scheduled services.
Output: Services updated to use template payloads in the outbox.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-template-integration-service-migration/09-RESEARCH.md
@app/services/followup.py
@app/services/monthly_report.py
@app/routers/whatsapp_helpers.py
@.planning/phases/09-template-integration-service-migration/09-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update follow-up service to use templates</name>
  <files>app/services/followup.py</files>
  <action>
    1. Import `get_customer_name` from `app.routers.whatsapp_helpers`.
    2. In `send_followups`, change the `enqueue_outbox` call to use a template payload:
       ```python
       payload = {
           "type": "template",
           "template_name": "order_followup",
           "body_vars": [customer_name or ""]
       }
       ```
    3. Ensure `customer_name` is fetched for each phone number.
  </action>
  <verify>
    <automated>python -c "from app.services.followup import send_followups; print(send_followups())"</automated>
  </verify>
  <done>Follow-up service enqueues template jobs instead of raw text.</done>
</task>

<task type="auto">
  <name>Task 2: Update monthly report service to use templates</name>
  <files>app/services/monthly_report.py</files>
  <action>
    In `send_monthly_report`, change the `enqueue_outbox` call to use a template payload:
    ```python
    payload = {
        "type": "template",
        "template_name": "monthly_summary",
        "body_vars": [report_text]
    }
    ```
    Note: Ensure `report_text` (the built report) is passed as the first and only body variable.
  </action>
  <verify>
    <automated>python -c "from app.services.monthly_report import send_monthly_report; send_monthly_report()"</automated>
  </verify>
  <done>Monthly report service enqueues template jobs instead of raw text.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| DB → Outbox | Data extracted from DB for template variables |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Tampering | followup.py | mitigate | Validate that customer name doesn't contain malicious control characters |
</threat_model>

<success_criteria>
- Follow-up jobs in `outbox_jobs` have `template` type and `order_followup` name.
- Monthly report jobs in `outbox_jobs` have `template` type and `monthly_summary` name.
- Integration tests confirm successful queueing.
</success_criteria>

<output>
Create `.planning/phases/09-template-integration-service-migration/09-02-SUMMARY.md` when done
</output>
