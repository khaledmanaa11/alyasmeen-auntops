---
phase: 09-template-integration-service-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [app/services/whatsapp_meta.py, app/services/whatsapp_dev.py, worker.py, tests/unit/test_whatsapp_meta_template.py]
autonomous: true
requirements: [REQ-M2-03]
must_haves:
  truths:
    - "Meta transport can send structured templates to the Graph API"
    - "Dev transport prints template payloads for inspection"
    - "Worker outbox loop recognizes and executes template jobs"
  artifacts:
    - path: "app/services/whatsapp_meta.py"
      provides: "send_template function"
    - path: "worker.py"
      provides: "template job execution logic"
---

<objective>
Implement the transport layer for WhatsApp Templates in both the production Meta service and the development mock service. Update the worker to process template-type outbox jobs.

Purpose: Enable business-initiated contact outside the 24-hour window, which is mandatory for scheduled follow-ups and reports.
Output: Generic template sending capability in the messaging layer and worker.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-template-integration-service-migration/09-RESEARCH.md
@app/services/whatsapp_meta.py
@app/services/whatsapp_dev.py
@worker.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add send_template to WhatsApp services</name>
  <files>app/services/whatsapp_meta.py, app/services/whatsapp_dev.py</files>
  <action>
    1. In `app/services/whatsapp_meta.py`, implement `send_template(to, template_name, language_code="ar", body_vars=None)`.
       - Strip spaces from 'to'.
       - Construct the Meta 'template' payload with 'name', 'language', and 'components'.
       - Map 'body_vars' list to the 'body' component parameters (text type).
       - POST to Meta API (v19.0).
       - Reference Pattern 2 from 09-RESEARCH.md.
    2. In `app/services/whatsapp_dev.py`, implement a mock `send_template` that prints the payload to console.
  </action>
  <verify>
    <automated>python -c "from app.services import whatsapp_meta; print(whatsapp_meta.send_template('123', 'test', body_vars=['a']))" (Expect 401/400 but valid JSON construction)</automated>
  </verify>
  <done>send_template exists in both services and follows Meta schema.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement unit test for template payload</name>
  <files>tests/unit/test_whatsapp_meta_template.py</files>
  <behavior>
    - Ensure `send_template` produces the exact JSON structure required by Meta.
    - Verify 'to' number formatting.
    - Verify body variables are correctly nested in components.
  </behavior>
  <action>
    Create a unit test that mocks `requests.post` and asserts the payload structure for `send_template`.
  </action>
  <verify>
    <automated>pytest tests/unit/test_whatsapp_meta_template.py</automated>
  </verify>
  <done>Unit test passes and confirms payload structure.</done>
</task>

<task type="auto">
  <name>Task 3: Update worker to execute template jobs</name>
  <files>worker.py</files>
  <action>
    In `worker.py`, update `execute_outbox_job` function:
    1. Detect `msg_type == "template"` in the payload.
    2. Extract `template_name`, `language_code` (default "ar"), and `body_vars`.
    3. Call `whatsapp_meta.send_template` with these arguments.
  </action>
  <verify>
    <automated>python worker.py --test-outbox (with a manually inserted template job in DB)</automated>
  </verify>
  <done>Worker successfully dispatches template jobs from the outbox.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Worker → Meta API | Sensitive tokens and payloads sent to external service |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information Disclosure | whatsapp_meta.py | mitigate | Ensure tokens are loaded from Config (env) and not logged |
| T-09-02 | Tampering | worker.py | mitigate | Use strictly typed payload extraction from JSON to avoid injection |
</threat_model>

<success_criteria>
- `send_template` correctly formats Meta JSON payloads.
- Worker can process an outbox job of type `template`.
- Dev mode correctly intercepts and prints template intents.
</success_criteria>

<output>
Create `.planning/phases/09-template-integration-service-migration/09-01-SUMMARY.md` when done
</output>
