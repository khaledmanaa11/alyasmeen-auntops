---
phase: 05-operator-security-ux
plan: 10
type: execute
wave: 7
depends_on: ["05-07", "05-08", "05-09"]
files_modified: [docs/OPERATOR_ACCOUNTS.md, .planning/STATE.md]
autonomous: false
requirements: [REQ-prod-auth-mfa, REQ-prod-session-opaque, REQ-prod-csrf, REQ-prod-sec-headers, REQ-prod-handoff, REQ-dash-login]
user_setup:
  - service: supabase-auth
    why: "The two real operator accounts and the anon key can only come from the human"
    env_vars:
      - name: SUPABASE_ANON_KEY
        source: "Supabase Dashboard -> Project Settings -> API -> anon/public key (set locally in .env AND on both Railway services)"
      - name: ADMIN_PHONE
        source: "Khaled's WhatsApp number, 9725XXXXXXXX"
      - name: DASHBOARD_BASE_URL
        source: "https://alyasmeen.org"
    dashboard_config:
      - task: "Confirm the Email provider is enabled"
        location: "Supabase Dashboard -> Authentication -> Providers -> Email"
must_haves:
  truths:
    - "The operator_auth migration is applied to the live Supabase project"
    - "Two real Supabase Auth accounts exist: the aunt (role aunt) and Khaled (role admin)"
    - "The aunt has an active TOTP factor enrolled from a real authenticator app"
    - "Signing in on a new device requires the TOTP code and sends Khaled a WhatsApp alert; the same device does not re-challenge afterwards"
    - "The aunt can complete a full handoff cycle and a full alert-recovery cycle in the live UI without help"
    - "DASHBOARD_PASSWORD is no longer set anywhere and nothing breaks"
  artifacts:
    - path: "docs/OPERATOR_ACCOUNTS.md"
      provides: "Runbook updated with the real account emails (redacted), the live enrollment date and the recovery drill result"
  key_links:
    - from: "live Supabase project"
      to: "supabase/migrations/20260828000000_operator_auth.sql"
      via: "supabase db push"
      pattern: "operator_sessions"
---

<objective>
Take Phase 5 from "green on this branch" to "the aunt is actually using it": apply the auth
migration live, create the two real accounts, run assisted TOTP enrollment with her, and walk
the four success criteria end to end in the deployed dashboard.

This plan is deliberately separated from the implementation plans because everything in it
either needs a human decision (which email addresses), a human device (an authenticator app on
her phone) or a human judgement (does this page make sense to her).

Purpose: prove all four Phase 5 success criteria against the live system.
Output: a live, MFA-protected dashboard and an updated runbook.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-operator-security-ux/05-CONTEXT.md
@.planning/phases/05-operator-security-ux/05-02-SUMMARY.md
@.planning/phases/05-operator-security-ux/05-09-SUMMARY.md
@docs/OPERATOR_ACCOUNTS.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Provide the operator emails and the Supabase anon key</name>
  <action>
    Three things only the human can supply. Everything else in this plan is automated.

    1. **The two email addresses.** Real, working inboxes — password-reset links go there.
       - The aunt's email (role `aunt`)
       - Khaled's email (role `admin`)
       These are never guessable and must not be invented.

    2. **`SUPABASE_ANON_KEY`.** Supabase Dashboard -> Project Settings -> API -> "Project API
       keys" -> the `anon` / `public` key. Set it in the local `.env` AND on **both** Railway
       services (web and worker). It is a public-safe key; the existing `SUPABASE_KEY`
       (service_role) stays exactly as it is.

    3. **`ADMIN_PHONE`** (Khaled's WhatsApp number, `9725XXXXXXXX`) and
       **`DASHBOARD_BASE_URL`** (`https://alyasmeen.org`) — set locally and on Railway.

    Also confirm: `DASHBOARD_PASSWORD` can now be deleted from `.env` and from both Railway
    services. Nothing reads it anymore (plan 05-03 removed it from `Config`). Delete it rather
    than leaving a dead secret lying around.
  </action>
  <verify>`python -c "from app.services.config import Config; assert Config.SUPABASE_ANON_KEY and Config.ADMIN_PHONE; print('env ok')"` locally, and the same three variables visible on both Railway services.</verify>
  <done>Both operator email addresses are known, SUPABASE_ANON_KEY / ADMIN_PHONE / DASHBOARD_BASE_URL are set locally and on both Railway services, and DASHBOARD_PASSWORD is deleted everywhere.</done>
  <resume-signal>Reply with the two email addresses and confirm the three env vars are set on Railway (web + worker) and locally, and that DASHBOARD_PASSWORD has been removed.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Apply the migration live and create the two operator accounts</name>
  <files>docs/OPERATOR_ACCOUNTS.md</files>
  <action>
    1. **Apply the migration.** `supabase db push` (the CLI is already linked and authenticated
       — Phase 4 plan 04-07 pushed all 11 existing migrations this way). Only
       `20260828000000_operator_auth.sql` should be pending. If the CLI reports remote history
       drift, STOP and report rather than forcing — 04-07 already had to repair stale history
       once and that is not a step to improvise.
       Verify afterwards:
       `python -c "from app.db.database import query; print(query('SELECT count(*) FROM operator_sessions')); print(query('SELECT count(*) FROM trusted_devices')); print(query('SELECT count(*) FROM pending_logins'))"`
       — three zero counts means all three tables exist and are reachable with the shipped key.

    2. **Create the accounts** with the emails from Task 1:
       ```
       python scripts/manage_operators.py create --email <aunt> --role aunt
       python scripts/manage_operators.py create --email <khaled> --role admin
       ```
       Capture the two generated temporary passwords from stdout, hand them over in the
       checkpoint message, and do NOT write them into any file, commit or SUMMARY.
       Confirm with `python scripts/manage_operators.py list` that both users exist with the
       right `role` in `app_metadata` and zero MFA factors.

    3. **Deploy.** Push the branch so Railway rebuilds both services. Then verify against the
       live URL:
       - `curl -si https://alyasmeen.org/health` -> `{"ok": true, ...}`
       - `curl -si https://alyasmeen.org/orders | head -1` -> 303 to `/login`
       - `curl -si https://alyasmeen.org/login | grep -i "strict-transport-security"` -> present
         (production mode, so HSTS is on)
       - `curl -si https://alyasmeen.org/login | grep -i "content-security-policy"` -> present
       - `curl -si -X POST https://alyasmeen.org/api/products` -> 401 (no session, no CSRF
         needed to prove auth)

    4. **Update `docs/OPERATOR_ACCOUNTS.md`** with the live facts: both accounts created (emails
       redacted to `a***@domain`), the date, and a note that temporary passwords were delivered
       out of band and must be changed at first sign-in.
  </action>
  <verify>The three live table counts return 0; `manage_operators.py list` shows both users with the correct roles; the five live curl checks pass.</verify>
  <done>The migration is applied to the live project, both operator accounts exist with the right roles, and the deployed app enforces auth and ships the security headers.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Assisted TOTP enrollment and the full operator walkthrough</name>
  <action>
    Before pausing, make sure the deployed build is the one about to be verified: confirm the
    Railway web and worker services both rebuilt from the current branch head, run
    `curl -s https://alyasmeen.org/health`, and re-read `docs/OPERATOR_ACCOUNTS.md` so the
    assisted-enrollment steps you walk her through match the runbook. Then present the
    walkthrough below and wait.
  </action>
  <what-built>
    The complete Phase 5 dashboard, live: Supabase Auth login with TOTP, 30-day opaque sessions
    with remember-device, CSRF and security headers on every request, a محادثات handoffs tab
    with transcript and return-to-bot, a السجل audit trail, a حسابي account page with device
    management, and a reworked تنبيهات page with plain-Arabic action cards.
  </what-built>
  <how-to-verify>
    Do these in order, on the live site. Steps 1-3 are the assisted-enrollment ritual from
    `docs/OPERATOR_ACCOUNTS.md` — Khaled sits with the aunt (in person or on a call).

    **A. First sign-in and enrollment**
    1. The aunt opens https://alyasmeen.org/login, signs in with her email and the temporary
       password. She should reach /orders. Expected: no TOTP prompt yet (no factor enrolled).
    2. She opens حسابي (/account). The MFA card shows the amber "غير مفعّل" state.
    3. She clicks "فعّلي التحقق بخطوتين", enters her password, scans the QR with her
       authenticator app (Google Authenticator / Authy), and enters the 6-digit code.
       Expected: success state, MFA card turns green.
    4. On Khaled's laptop, sign in as the aunt in a private window using the same temporary
       password. Expected: it is REJECTED or requires the code — and any older session of hers
       elsewhere has been signed out by the enrollment.

    **B. MFA + remember-device**
    5. Sign her out (خروج) and sign back in on the SAME browser. Expected: the code page appears
       once; after entering a valid code she reaches /orders.
    6. Sign out and in again on that same browser. Expected: NO code page — remember-device.
    7. Sign in from a different browser or her phone. Expected: the code page appears, AND
       Khaled receives a WhatsApp message about a new-device sign-in. The aunt receives nothing.
    8. Enter a WRONG code once. Expected: an Arabic error and no access.

    **C. Sessions**
    9. On /account, الأجهزة المتصلة lists both devices, with "هذا الجهاز" on the current one.
       Revoke the other one; refresh that other browser. Expected: it is bounced to /login.
    10. On Khaled's admin account, /account shows جلسات المشغّلين listing the aunt's sessions
        with revoke buttons. On the aunt's account that card is absent.
    11. Click "تسجيل الخروج من كل الأجهزة" on the aunt's account. Expected: every device is
        signed out.

    **D. Handoffs**
    12. Open محادثات. If Phase 3 has not shipped yet, there will legitimately be no active
        handoffs — in that case verify via a status-change takeover instead: on /orders change
        a status for a customer the bot has recently replied to. Expected: the conflict dialog
        appears naming her. Choose "كمّلي أنتِ".
    13. Go back to محادثات. Expected: a handoff for that customer, with her name as the headline
        and the recent transcript when expanded, plus a working "افتحي واتساب" link.
    14. Click "أعد للبوت". Expected: it moves to محلولة, the red badge count drops, and the bot
        starts replying to that customer again.

    **E. Audit + alerts**
    15. Open السجل. Expected: readable Arabic sentences for the login, the status change, the
        conflict override and the handoff resolution — each attributed to the right email.
        Khaled's account sees the same trail.
    16. Open تنبيهات. If there is at least one stuck item, expected: a card reading like a
        sentence with the customer's NAME (not a job id), a primary "افتحي المحادثة" button, and
        technical detail hidden until "تفاصيل تقنية" is clicked. Test "إعادة المحاولة للكل".
        If the list is empty, temporarily create a failure (send to an invalid number) to
        produce one, and confirm Khaled ALSO receives a WhatsApp alert about it.

    **F. The real question**
    17. Ask the aunt, without prompting: looking at تنبيهات, what would you do next? If she
        cannot answer in one sentence, the phrasing needs another pass — report that instead of
        approving.

    Report any step that behaves differently, and especially any step where she hesitated.
  </how-to-verify>
  <verify>Every lettered section A-F above behaves as described on the live site, and step 17 gets a one-sentence answer.</verify>
  <done>All four Phase 5 success criteria are demonstrated live, the aunt has a working TOTP factor, and she can describe her next action from the تنبيهات page unprompted.</done>
  <resume-signal>Type "approved" if every step behaved as described, or list the step numbers that failed and what happened.</resume-signal>
</task>

</tasks>

<verification>
- All three live table counts exist and are reachable.
- Both operator accounts exist with the correct `app_metadata.role`.
- The aunt has exactly one verified TOTP factor
  (`python scripts/manage_operators.py list` shows factor count 1).
- HSTS and CSP headers present on the live site; `/orders` 303s and `/api/*` 401s when signed
  out.
- The human walkthrough is approved.
</verification>

<success_criteria>
1. Dashboard access requires Supabase Auth with TOTP MFA — verified live with a real
   authenticator app (success criterion 1).
2. The aunt can manage handoffs, see audit history and resolve a bot-vs-aunt conflict from the
   UI (success criterion 2).
3. All dashboard mutations are CSRF-protected and sessions are opaque, revocable and
   device-aware (success criterion 3).
4. The operator can see and recover from failed communications, and permanent failures reach her
   and Khaled on WhatsApp (success criterion 4).
5. `DASHBOARD_PASSWORD` is gone from every environment.
</success_criteria>

<output>
After completion, create `.planning/phases/05-operator-security-ux/05-10-SUMMARY.md` and update
`.planning/STATE.md`: mark Phase 5 complete with the four criteria verified, record the live
enrollment date, and carry forward any deferred debt (strict CSP / Tailwind precompile, and
whether Phase 3 had shipped a handoff producer by this point).
</output>
