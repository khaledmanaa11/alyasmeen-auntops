---
phase: 05-operator-security-ux
plan: 07
type: execute
wave: 6
depends_on: ["05-05", "05-06"]
files_modified: [app/templates/_nav.html, app/templates/handoffs.html, app/templates/audit.html, app/routers/ui.py, tests/integration/test_ui_pages.py]
autonomous: true
requirements: [REQ-prod-handoff, REQ-dash-login]
must_haves:
  truths:
    - "The dashboard nav shows a محادثات tab carrying a live count badge of active handoffs, and a سجل tab, on every page"
    - "The handoffs page lists active handoffs first and resolved ones below, each showing the customer's name and the reason"
    - "Opening a handoff shows the recent WhatsApp transcript and a wa.me link so she has context before messaging the customer"
    - "One أعد للبوت button resolves the handoff, the row moves to the resolved section, and the badge count drops"
    - "The سجل page shows a chronological list of operator actions in plain Arabic, naming who did what and when"
    - "Both pages match the premium design system (Cairo, #006948, glassmorphism nav, 20px cards, RTL) and are reachable only when signed in"
  artifacts:
    - path: "app/templates/handoffs.html"
      provides: "Handoffs tab: active/resolved lists, transcript drawer, return-to-bot action"
      min_lines: 180
    - path: "app/templates/audit.html"
      provides: "Operator audit trail page"
      min_lines: 100
    - path: "app/templates/_nav.html"
      provides: "Shared nav with the handoffs badge and the new tabs"
      contains: "/handoffs"
    - path: "app/routers/ui.py"
      provides: "GET /handoffs and GET /audit page routes"
      contains: "handoffs.html"
  key_links:
    - from: "app/templates/handoffs.html"
      to: "/api/handoffs"
      via: "fetch on load and after each resolve"
      pattern: "fetch\\('/api/handoffs"
    - from: "app/templates/handoffs.html"
      to: "/api/handoffs/{id}/resolve"
      via: "the أعد للبوت button"
      pattern: "resolve"
    - from: "app/templates/_nav.html"
      to: "/api/handoffs/count"
      via: "badge polling on the shared nav"
      pattern: "api/handoffs/count"
    - from: "app/templates/audit.html"
      to: "/api/audit"
      via: "fetch on load"
      pattern: "fetch\\('/api/audit"
---

<objective>
Give the aunt the operator surface for success criterion 2: a dedicated handoffs tab where she
can see which conversations need her, read enough context to act, and hand each one back to the
bot with one button — plus a chronological who-did-what trail.

Plan 05-05 already built and tested the JSON API; this plan is the UI that consumes it, plus
the two new nav entries in the shared `_nav.html` partial that plan 05-04 extracted.

Purpose: REQ-prod-handoff (operator inbox) and the audit-visibility decision.
Output: `handoffs.html`, `audit.html`, two page routes, an updated shared nav with a live badge.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-operator-security-ux/05-CONTEXT.md
@.planning/phases/05-operator-security-ux/05-05-SUMMARY.md
@app/templates/_nav.html
@app/templates/alerts.html
@app/templates/orders.html
@app/routers/ui.py
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared nav — handoffs tab with live badge, audit tab, account link</name>
  <files>app/templates/_nav.html, app/routers/ui.py</files>
  <action>
    Plan 05-04 extracted the duplicated `<nav class="nav-glass">` block into
    `app/templates/_nav.html`, included by every page. Edit that ONE file (never re-edit the six
    page templates — that duplication is exactly what the partial removed).

    Add three entries after the existing `تنبيهات` link, keeping the identical
    `nav-link` / `nav-active` classes, sizes and spacing so nothing shifts visually:
    - `محادثات` -> `/handoffs`, icon `forum`, with a count badge element
      `<span id="handoffCount" class="hidden ...">` styled as a small red pill
      (`background:#ef4444; color:white`) — red, not the brand green, because it means "needs
      you now".
    - `السجل` -> `/audit`, icon `history`.
    - `حسابي` -> `/account`, icon `account_circle`, placed just before the existing `خروج`
      link. (Plan 05-09 built that page in the previous wave; this is the link to it.)

    Give each new link the same self-highlighting expression the partial already uses —
    `class="{{ 'nav-active' if _p == '/handoffs' else 'nav-link' }} ..."` against the
    `{% set _p = request.url.path %}` that plan 05-04 put at the top of the file. **Do not add a
    `{% set active = ... %}` variable and do not reopen any page template**: the whole point of
    05-04's self-highlighting partial is that adding a tab is a one-file change. Editing the
    page templates here would also collide with plan 05-08, which owns `orders.html` and
    `alerts.html` in this same wave.

    Add a small inline script at the bottom of `_nav.html` that polls
    `GET /api/handoffs/count` every 30 seconds (and once immediately), showing the badge with
    the number when the returned `active` count is greater than 0 and hiding it otherwise. Wrap the fetch in try/catch and fail
    silently — a badge that throws must never break the page it sits on. 30s matches the
    dashboard's existing auto-refresh feel; define it as a single `const BADGE_POLL_MS = 30000`.

    In `app/routers/ui.py` add the two page routes next to the existing ones (the router-level
    `require_operator_page` dependency from 05-03 already guards them):
    `@router.get("/handoffs")` -> `templates.TemplateResponse(request, "handoffs.html")` and
    `@router.get("/audit")` -> `"audit.html"`.
  </action>
  <verify>`grep -c "handoffCount" app/templates/_nav.html` returns at least 1; `git diff --name-only` for this task lists ONLY `app/templates/_nav.html` and `app/routers/ui.py`; `grep -c 'set active' app/templates/*.html` returns 0 everywhere; start the app in mock mode and confirm `GET /orders`, `/dashboard`, `/products`, `/broadcast`, `/alerts`, `/handoffs`, `/audit` all render with the new tabs and each highlights its own; `curl -si localhost:8000/handoffs | head -1` returns 303 when signed out.</verify>
  <done>The shared nav carries محادثات (with badge), السجل and حسابي; each page highlights its own tab via `request.url.path`; no page template was modified; `/handoffs` and `/audit` page routes exist and are auth-guarded.</done>
</task>

<task type="auto">
  <name>Task 2: handoffs.html — active/resolved lists, transcript, return-to-bot</name>
  <files>app/templates/handoffs.html</files>
  <action>
    Create `app/templates/handoffs.html`. Model it structurally on `alerts.html` (Alpine.js
    single-file page, `x-data="handoffsApp()"`, `x-cloak`, toast pattern) and keep the design
    system exactly: `<html lang="ar" dir="rtl">`, Cairo font, `#006948` primary / `#004d33`
    hover / `#e6f3ee` tint, `#f0f7f4` page background, cards at `border-radius: 20px` with
    `1px solid #e8f3ee` and `0 2px 12px rgba(0,0,0,0.04)`, Material Symbols icons, the
    `.nav-glass` partial via `{% include %}`. Do NOT introduce `#059669` or DaisyUI classes.
    Include `<script src="/static/js/csrf.js"></script>` in `<head>` after the Tailwind CDN tag
    (plan 05-04's rule — without it the resolve POST is rejected with 403).

    Layout:
    - Header: `محادثات` + subtitle `"محادثات تحتاج تدخلك — رديّ على الزبونة في واتساب، وبعدين رجّعيها للبوت"`.
    - A two-state filter (`نشطة` / `محلولة`) as pill buttons; default `نشطة`. Resolved handoffs
      stay visible as recent history (locked in CONTEXT) — never hide them permanently.
    - Loading spinner and an empty state (`"ما في محادثات محوّلة إلك 🎉"`), matching alerts.html's.
    - One card per handoff:
      - Headline = the customer's NAME in bold (fall back to the phone number when empty) —
        the same "name first, not phone first" hierarchy the orders page already uses.
      - The reason as a small tinted pill (`background:#e6f3ee; color:#006948`), plus relative
        time (`منذ ...`).
      - A green WhatsApp button linking to `item.wa_link` with `target="_blank"` —
        `"افتحي واتساب"`.
      - A `"شوفي المحادثة"` toggle that expands the transcript inline.
      - For active handoffs only: a primary `#006948` button `"أعد للبوت"` with icon
        `smart_toy`.
    - Transcript: on expand, `fetch('/api/handoffs/' + id)` and render the returned `transcript`
      as chat bubbles — customer turns (`role == 'user'`) right-aligned on white with a border,
      bot turns (`role == 'assistant'`) left-aligned on `#e6f3ee`. Cap the visible height at
      ~320px with `overflow-y:auto`. Show a small `"آخر ٢٠ رسالة"` caption.
    - `أعد للبوت` -> `POST /api/handoffs/{id}/resolve`; on 200 show the toast
      `"رجّعت المحادثة للبوت ✅"`, move the item into the resolved list locally, and refresh the
      nav badge (call the same count endpoint). On 409 show `"هاي المحادثة محلولة من قبل"` and
      refresh the list.

    **Escape every value that comes from the database.** `orders.html` had a stored-XSS bug
    fixed during the hardening session; use Alpine's `x-text` (never `x-html`) for
    `customer_name`, `reason`, transcript `content` and any error text.
  </action>
  <verify>Start the app in mock mode, `curl -X POST localhost:8000/dev/test_handoff` to seed one, sign in, open `/handoffs`: the card shows the seeded customer's name, expanding shows the three seeded turns, and clicking أعد للبوت moves it to the resolved filter and drops the badge to 0. Also `python -c "import pathlib; s=pathlib.Path('app/templates/handoffs.html').read_text(encoding='utf-8'); assert 'x-html' not in s and '#059669' not in s and 'csrf.js' in s; print('ok')"`.</verify>
  <done>The handoffs page lists active and resolved handoffs with names and reasons, expands a real transcript, links to WhatsApp, and resolves with one button that also unpauses the bot (via 05-05's API).</done>
</task>

<task type="auto">
  <name>Task 3: audit.html — operator action trail</name>
  <files>app/templates/audit.html, tests/integration/test_ui_pages.py</files>
  <action>
    Create `app/templates/audit.html` — same design system, the same bare
    `{% include "_nav.html" %}` (it highlights itself from the path — pass nothing), same
    csrf.js tag. It is read-only: one `fetch('/api/audit')` on load, no mutations.

    Render a single chronological timeline (newest first), one compact row per entry:
    - A Material Symbols icon chosen per action family (login/logout -> `login`, order ->
      `receipt_long`, product -> `inventory_2`, broadcast -> `campaign`, handoff -> `forum`,
      alert -> `warning`, mfa/session -> `shield`).
    - A plain-Arabic sentence built client-side from `action` + `details` via a
      `describe(entry)` function with an explicit `switch`/map — e.g.
      `order_status_changed` -> `"غيّرت حالة الطلب #{order_id} إلى {to}"`,
      `product_updated` -> `"عدّلت المنتج {name}"`,
      `login_success` -> `"سجّلت الدخول"`,
      `login_failed` -> `"محاولة دخول فاشلة"`,
      `handoff_resolved` -> `"رجّعت محادثة {phone} للبوت"`,
      `order_status_conflict_override` -> `"تجاوزت تنبيه البوت وغيّرت حالة الطلب #{order_id}"`,
      `broadcast_sent` -> `"أرسلت رسالة جماعية لـ {sent} زبونة"`.
      Any unmapped action falls back to showing the raw action string — never render a blank row.
    - The actor's email in muted text and a localised timestamp (`toLocaleString('ar-EG')`).
    - A `التفاصيل` toggle per row revealing the raw `details` JSON in a `<pre>` — same
      "technical detail collapsed behind a toggle" principle as the alerts page.
    - A simple client-side text filter box over actor + sentence, and an empty state.

    Use `x-text` everywhere; never `x-html`.

    **`tests/integration/test_ui_pages.py`** — NEW, small. It needs no local fixtures: plan
    05-03 promoted `client` into `tests/conftest.py` alongside `operator_client` and
    `admin_client`, so just request them as parameters (do NOT re-declare a local
    `TestClient(app)` fixture — that per-file duplication is what 05-03 removed). With
    `operator_client`, assert
    `GET /handoffs`, `GET /audit`, `GET /orders` and `GET /alerts` return 200 and their HTML
    contains the shared nav markers (`/handoffs`, `/audit`, `handoffCount`); with the plain
    `client` fixture assert `GET /handoffs` and `GET /audit` return 303 with
    `location: /login`. Monkeypatch whatever `query` the page routes need — the page routes
    render static templates, so no DB stubbing should be required; if it is, patch the bound
    name in `app.routers.ui`.
  </action>
  <verify>`python -m pytest tests/integration/test_ui_pages.py -q` passes; `python -m pytest -q` full suite green; open `/audit` in a browser after performing a product edit and confirm the row reads as a sentence naming the operator.</verify>
  <done>`/audit` renders the operator trail as readable Arabic sentences with expandable raw details, is visible to both accounts, and the page-level auth tests pass.</done>
</task>

</tasks>

<verification>
- `python -m pytest -q` — full suite green.
- Browser walkthrough (mock mode): seed with `POST /dev/test_handoff`; the nav badge shows 1 on
  every page; `/handoffs` shows the card with the customer's name; the transcript expands;
  `أعد للبوت` resolves it, the badge clears, and the item appears under `محلولة`.
- `/audit` shows `handoff_resolved` for that action, attributed to the signed-in operator's
  email.
- `grep -rn "#059669\|x-html" app/templates/handoffs.html app/templates/audit.html` returns
  nothing (design system + XSS rules).
</verification>

<success_criteria>
1. `محادثات` and `السجل` tabs exist on every dashboard page, with a live active-handoff badge.
2. `/handoffs` lists active and resolved handoffs with customer names, reasons, transcripts and
   wa.me links.
3. A single `أعد للبوت` button resolves a handoff and clears it from the active list.
4. `/audit` renders the operator trail as plain Arabic sentences with raw details behind a
   toggle.
5. Both pages match the premium design system and are auth-guarded.
</success_criteria>

<output>
After completion, create `.planning/phases/05-operator-security-ux/05-07-SUMMARY.md`.
Record the final `_nav.html` link list and the badge-polling interval, and confirm that adding
the three tabs required editing no page template (the self-highlighting partial from 05-04 held
up).
</output>
