---
phase: 01-database-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified: [tests/integration/test_database_reliability.py, docs/BACKUP_DRILL.md]
autonomous: true
requirements: [REQ-prod-backup-restore, REQ-prod-atomic-orders, REQ-prod-data-foundation]
must_haves:
  truths:
    - "Backup/Restore process is verified and documented"
    - "Atomic order creation is covered by tests"
    - "Outbox pattern is functionally verified"
  artifacts:
    - path: "tests/integration/test_database_reliability.py"
      provides: "Reliability and atomicity tests"
    - path: "docs/BACKUP_DRILL.md"
      provides: "Documented disaster recovery procedure"
---

<objective>
Verify the database foundation through rigorous testing and establish a disaster recovery procedure.

Purpose: Guarantee that the new data foundation is reliable, atomic, and recoverable.
Output: Integration test suite for database operations and a documented backup/restore drill.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-database-foundation/01-02-SUMMARY.md
@.planning/research/RESEARCH-1.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unit & Integration Tests for Atomic Operations</name>
  <files>tests/integration/test_database_reliability.py</files>
  <action>
    - Create a test file to verify the `create_order_atomic` RPC.
    - Test success path: verify customer, order, and lines are all created correctly.
    - Test failure path: pass invalid JSON or data that triggers a constraint violation; verify that NO records (customer/order/lines) are created (roll-back).
    - Verify that session persistence works through the new database access layer.
  </action>
  <verify>
    <automated>pytest tests/integration/test_database_reliability.py</automated>
  </verify>
  <done>Business logic atomicity is verified by automated tests.</done>
</task>

<task type="auto">
  <name>Task 2: Integration Test for Durable Outbox Pattern</name>
  <files>tests/integration/test_database_reliability.py</files>
  <action>
    - Extend tests to verify that `outbox_jobs` entries are created alongside order status updates (simulate the pattern if worker isn't fully built yet).
    - Ensure jobs can be claimed using `FOR UPDATE SKIP LOCKED`.
    - Verify that `webhook_events` are durably stored before any processing logic runs.
  </action>
  <verify>
    <automated>pytest tests/integration/test_database_reliability.py</automated>
  </verify>
  <done>Durable messaging schema and access patterns are verified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Backup/Restore Verification Drill</name>
  <files>docs/BACKUP_DRILL.md</files>
  <action>
    - Execute a manual backup using `npx supabase db dump --local > backup.sql`.
    - Drop the local database: `npx supabase db reset --local`.
    - Restore from the backup file: `psql -f backup.sql` (or equivalent CLI command).
    - Verify data integrity after restore.
    - Document the exact steps in `docs/BACKUP_DRILL.md`.
  </action>
  <verify>
    <human-check>User confirms that the restore succeeded and the documentation is clear.</human-check>
  </verify>
  <done>Disaster recovery procedure is verified and documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backup Storage | Backup files contain PII (customer info); must be secured. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Information Disclosure | backup.sql | mitigate | Document that backups must be encrypted/restricted in production. |
</threat_model>

<verification>
Run the full test suite and confirm `docs/BACKUP_DRILL.md` contains successful timestamps and output logs from the drill.
</verification>

<success_criteria>
1. All reliability tests pass.
2. Atomicity is proven through failure-injection tests.
3. Backup/Restore drill is completed and documented.
</success_criteria>

<output>
Create `.planning/phases/01-database-foundation/01-03-SUMMARY.md` when done
</output>
