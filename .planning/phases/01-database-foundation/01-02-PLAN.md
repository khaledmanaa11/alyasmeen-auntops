---
phase: 01-database-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [supabase/migrations/20260614000002_atomic_logic.sql, supabase/migrations/20260614000003_security_hardening.sql, app/db/database.py]
autonomous: true
requirements: [REQ-prod-atomic-orders, REQ-bot-session-persist]
must_haves:
  truths:
    - "Orders are created atomically via pgSQL function"
    - "Generic run_query/run_exec RPCs are disabled or restricted"
    - "RLS is enabled on all tables"
  artifacts:
    - path: "supabase/migrations/20260614000002_atomic_logic.sql"
      provides: "Atomic business logic functions"
    - path: "supabase/migrations/20260614000003_security_hardening.sql"
      provides: "RLS policies and security lockdown"
---

<objective>
Implement atomic business logic and secure the database using Row-Level Security (RLS).

Purpose: Ensure data integrity during order creation and prevent unauthorized database access.
Output: Atomic pgSQL functions for orders, RLS policies for all tables, and updated application database layer.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-database-foundation/01-01-SUMMARY.md
@app/db/database.py
@.planning/research/RESEARCH-1.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Atomic Order Creation Function</name>
  <files>supabase/migrations/20260614000002_atomic_logic.sql</files>
  <action>
    Create a pgSQL function `create_order_atomic` that:
    - Takes customer info (phone, fulfillment, address) and items (JSONB array).
    - Inserts/Updates the customer record.
    - Inserts the order.
    - Inserts order lines from the JSONB array.
    - Updates the order total.
    - All within a single transaction (automatic in pgSQL functions).
    Reference the example in RESEARCH-1.md.
  </action>
  <verify>
    <automated>npx supabase db reset --local</automated>
  </verify>
  <done>Atomic function exists in the database.</done>
</task>

<task type="auto">
  <name>Task 2: Enable RLS & Define Security Policies</name>
  <files>supabase/migrations/20260614000003_security_hardening.sql</files>
  <action>
    - Enable RLS on all tables: `ALTER TABLE ... ENABLE ROW LEVEL SECURITY`.
    - Define policies for the `anon` role (read-only for products, no access for others).
    - Define policies for the `authenticated` role (limited dashboard access).
    - Ensure the application service (using `service_role` or a dedicated role) can still function.
    - Decommission/Drop `run_query` and `run_exec` RPC functions (or restrict them to `service_role` only).
  </action>
  <verify>
    <automated>npx supabase db reset --local</automated>
  </verify>
  <done>RLS is active and generic RPCs are disabled/restricted.</done>
</task>

<task type="auto">
  <name>Task 3: Update database.py for Specific RPCs</name>
  <files>app/db/database.py</files>
  <action>
    - Modify `app/db/database.py` to remove generic `query`/`execute` patterns where possible.
    - Implement specific methods that call the new pgSQL functions using `client.rpc()`.
    - If `run_query` must remain for development, ensure it is only used by the `service_role` key and logged.
    - Per D-01: keep using `supabase-py` and the current connection pattern.
  </action>
  <verify>
    <automated>pytest tests/unit/test_database.py</automated>
  </verify>
  <done>Application database layer uses secure, specific access patterns.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client→API→RPC | Untrusted inputs from WhatsApp/Dashboard must be validated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | Elevation of Privilege | run_query RPC | mitigate | Restrict to service_role or drop function; enable RLS. |
| T-01-03 | Tampering | SQL Injection | mitigate | Use parameterized pgSQL functions (RPC) exclusively. |
</threat_model>

<verification>
Check that `anon` key cannot call `run_query` and that `create_order_atomic` correctly handles partial failures (atomicity).
</verification>

<success_criteria>
1. Orders are created via a single RPC call.
2. RLS blocks unauthorized queries.
3. `database.py` no longer exposes generic execution to unprivileged roles.
</success_criteria>

<output>
Create `.planning/phases/01-database-foundation/01-02-SUMMARY.md` when done
</output>
