---
phase: 01-database-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [supabase/migrations/20260614000000_baseline.sql, supabase/migrations/20260614000001_durable_messaging.sql, supabase/config.toml]
autonomous: true
requirements: [REQ-prod-migrations, REQ-nfr-data]
must_haves:
  truths:
    - "Database can be recreated from scratch using migrations"
    - "Schema includes webhook_events, outbox_jobs, and audit_logs"
  artifacts:
    - path: "supabase/migrations/20260614000000_baseline.sql"
      provides: "Existing schema baseline"
    - path: "supabase/migrations/20260614000001_durable_messaging.sql"
      provides: "New durable schema"
---

<objective>
Initialize the Supabase migration environment and establish the durable messaging schema.

Purpose: Move from manual schema management to versioned migrations and add tables for reliability.
Output: Supabase CLI configured, baseline migration created, and new messaging tables added.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@app/db/schema.sql
@.planning/research/RESEARCH-1.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Supabase CLI Initialization & Baseline Migration</name>
  <files>supabase/config.toml, supabase/migrations/20260614000000_baseline.sql</files>
  <action>
    - Initialize Supabase CLI in the project root: `npx supabase init`.
    - Create a baseline migration file `supabase/migrations/20260614000000_baseline.sql`.
    - Copy the content of `app/db/schema.sql` into the baseline migration.
    - Cleanup the baseline: ensure it uses `CREATE TABLE IF NOT EXISTS` or standard idempotent patterns.
  </action>
  <verify>
    <automated>npx supabase db reset --local</automated>
  </verify>
  <done>Local Supabase instance is running with the baseline schema.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Durable Messaging & Audit Schema</name>
  <files>supabase/migrations/20260614000001_durable_messaging.sql</files>
  <action>
    Create a new migration file for the following tables as per RESEARCH-1.md:
    - `webhook_events`: phone (TEXT), payload (JSONB), processed (BOOLEAN), created_at.
    - `outbox_jobs`: kind (TEXT), payload (JSONB), status (TEXT: 'pending','sent','failed'), created_at, updated_at.
    - `audit_logs`: actor (TEXT), action (TEXT), details (JSONB), created_at.
    - `handoffs`: phone (TEXT), reason (TEXT), status (TEXT: 'active','resolved'), created_at.
    Add indexes for common lookups (processed, status, phone).
  </action>
  <verify>
    <automated>npx supabase db reset --local</automated>
  </verify>
  <done>New tables are present in the local database schema.</done>
</task>

<task type="auto">
  <name>Task 3: Migration Integrity Verification</name>
  <files>supabase/migrations/*.sql</files>
  <action>
    - Run `npx supabase db lint` (if available) or verify all migrations apply cleanly to a fresh database.
    - Check that no manual edits are required to reach the desired state.
  </action>
  <verify>
    <automated>npx supabase db reset --local</automated>
  </verify>
  <done>Database schema is fully reproducible from migrations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI→Database | Migrations modify system-of-record schema. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Migration files | mitigate | Checksums enforced by Supabase CLI; code review required. |
| T-01-SC | Tampering | npm installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED] packages. |
</threat_model>

<verification>
Verify that `npx supabase db reset --local` succeeds and all tables are listed in `\dt` via `psql` or `supabase db query`.
</verification>

<success_criteria>
1. Supabase CLI is initialized.
2. Baseline migration contains the current schema.
3. Durable messaging tables exist in the schema.
4. `db reset` succeeds.
</success_criteria>

<output>
Create `.planning/phases/01-database-foundation/01-01-SUMMARY.md` when done
</output>
