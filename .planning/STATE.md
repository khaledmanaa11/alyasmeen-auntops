# Project State: ALYASMEEN AuntOps Production Readiness

## Project Reference
**Core Value**: Customers can reliably place and manage real orders through WhatsApp, while the aunt retains clear control over exceptions and can operate the business without technical assistance.
**Current Focus**: Phase 4 (Reliability & Ops Completion) is COMPLETE (7/7 plans). Phase 3 (Agent Dependability & Safety) is IN PROGRESS — waves 1-4 complete plus 03-07 of wave 5 (03-01 through 03-07 done, 7/8 plans; 03-08, the other wave-5 plan, running in parallel).

## Current Position
- **Phase**: 3 (Agent Dependability & Safety) — IN PROGRESS (wave 1: 03-01/03-02/03-03 done in parallel; wave 2: 03-04 done, sequential, depends on wave 1; wave 3: 03-05 done, sequential, depends on 03-03+03-04; wave 4: 03-06 done, sequential; wave 5: 03-07 done (this plan), 03-08 running in parallel; 7/8 plans). Phase 5 (Operator Security & UX) — IN PROGRESS (waves 1-6 of 7 COMPLETE — 05-01 through 05-09 all done; 9/10 plans; next: the 05-10 checkpoint plan, human-gated live rollout). Phase 4 is COMPLETE (7/7 plans).
- **Status**: 🟢 Phase 4 done — live rollout (service_role, migrations, worker persistence, backup drill) verified against production. 🟢 Phase 5 waves 1-4 complete: 05-01 (app/services/sessions.py + operator_sessions/trusted_devices/pending_logins migration, not yet applied live), 05-02 (app/services/auth.py Supabase Auth wrapper + scripts/manage_operators.py), 05-03 (auth_deps.py + auth_routes.py wire the two together into real email+password+TOTP login, replacing the shared-password cookie everywhere), 05-04 (CSRFMiddleware scoped to the session cookie + SecurityHeadersMiddleware + the single-file `_nav.html` partial), and 05-05 (app/services/handoff.py resolve()/active_count()/bot_recently_active() + app/services/audit.py best-effort operator trail + app/routers/operator_api.py's 5 endpoints + POST /dev/test_handoff seed) all done. 🟢 05-06 done: every dashboard mutation is audit-logged, api_update_status has a bot-vs-aunt conflict guard, /api/alerts speaks plain Arabic + retry_all, processor.py proactively WhatsApps permanent failures. 🟢 05-09 done (parallel with 05-06): /account page (MFA enrollment with QR + manual secret, revoke-on-enroll, session list/revoke, logout-everywhere, admin cross-account session view), PKCE password-reset landing (/login/reset-request + /login/reset + reset_password.html), and the new-device WhatsApp alert to ADMIN_PHONE only — wave 5 is fully complete. 🟢 Wave 6 (05-07 + 05-08, run in parallel in the same working directory) is fully complete: 05-08 reworked `app/templates/alerts.html` into severity-grouped plain-Arabic action cards consuming 05-06's `{alerts, counts}` payload (customer-facing failures lead with a WhatsApp-open action, internal ones with retry, both behind a collapsed technical-details toggle) plus a confirmed retry-all button, and gave `app/templates/orders.html`'s status-change flow a bot-vs-aunt conflict modal on a 409 response (template-only, no backend changes). 05-07 added a محادثات nav tab with a live red badge polling `GET /api/handoffs/count` every 30s, `app/templates/handoffs.html` (active/resolved lists, expandable transcript, one-button أعد للبوت resolve), `app/templates/audit.html` (read-only operator trail, all 18 OPERATOR_ACTIONS rendered as plain-Arabic sentences), a السجل + حسابي nav entry (closing the gap 05-09 flagged — /account was reachable only by direct URL until now), and `GET /handoffs`/`GET /audit` page routes — zero page templates touched beyond `_nav.html` itself. 🟢 Phase 3 wave 1 (03-01/03-02/03-03, run in parallel) done: `handoff.trigger()` (idempotent handoff creation + session pause + aunt notification), `app/services/policy.py` (deterministic tool-call gate + `detect_handoff_keyword()`), and `AIUnavailableError` + a 5th `request_human_handoff` Claude tool. 🟢 Phase 3 wave 2 (03-04, depends on wave 1) done: `sessions.paused` is now read on every inbound message (silences the bot, still records the customer's message); keyword-triggered and unsupported-media handoffs are wired into `handle_message`/`process_event`; `tests/conftest.py`'s autouse `mock_db` now patches `app.services.handoff` too. 🟢 Phase 3 wave 3 (03-05, depends on 03-03+03-04) done: `policy.validate()` now gates every AI tool call inside `_make_tool_executor`'s closure (deny before dispatch, Arabic denial text handed back to Claude as the tool result); `request_human_handoff` is a working tool (real handoff, session pause, exactly one deterministic ack — never the model's own phrasing); `AIUnavailableError`/any `generate_reply` exception now opens an `ai_failure` handoff on top of the existing Arabic fallback reply, with `handoff.trigger()`'s idempotency + the paused gate together preventing an alert storm during one outage. All five handoff triggers this phase owed are now live and tested. Full suite: 464 passed/3 skipped.
- **Progress**: [█████████-] ~90% (27/30 plans across all phases; Phase 3: 7/8, Phase 5: 9/10)

## Accomplishments (2026-08-25 hardening session — outside GSD, branch `fix/production-hardening`)
A full multi-agent code review rated the implementation 5/10 and found 64 issues; the verified
ones were fixed in two Sonnet waves + follow-ups. All on branch `fix/production-hardening`
(9+ commits, NOT merged to main, NOT pushed). Suite: 250+ green.
- [x] Webhook poison-pill events dead-letter after 3 attempts (`webhook_events.attempts`, migration `20260825000000`).
- [x] Malformed webhook payloads persist instead of crashing; global handler no longer masks /whatsapp/ failures as 200.
- [x] **Outbox wired as the single send path**: `queue_text`/`queue_buttons` in `processor.py` → `outbox_jobs` → poller with bounded attempts. Only `process_job` (and standalone scheduler services) call senders directly.
- [x] `whatsapp_meta.py` senders raise `WhatsAppSendError` on non-2xx (failures were silently swallowed).
- [x] Arabic confirm (تأكيد/اكد/أكد/تم) + restored clear command; order-number unwrap; qty clamp; AI-failure Arabic fallback; address-persistence bug fixed (delivery orders were losing their address).
- [x] Retired model default → `claude-haiku-4-5-20251001`; hardcoded credential fallbacks removed (fail-fast); Anthropic 30s timeout; catalog 60s TTL (worker staleness).
- [x] Dashboard: stored-XSS escaped, broadcast send fixed + authenticated, Secure cookie.
- [x] Invoice fully Arabic (Amiri font + reshaper/bidi, render-verified); RLS grant fix + `monthly_snapshots` migrations.
- [x] Test suite repaired: collection fixed, conftest rewritten for processor architecture, 5 stale files ported, debug router gated to dev.

## Phase 3 execution status
Re-verified and re-planned (`03-RESEARCH.md` + fresh `03-01` through `03-08` plans) before
execution, superseding the earlier stale-plan warning. Wave 1 (`03-01`/`03-02`/`03-03`, run in
parallel), wave 2 (`03-04`, sequential, depends on wave 1), and wave 3 (`03-05`, sequential,
depends on 03-03+03-04) are all done — see Session Continuity below for what each shipped.
🟢 Wave 4 (03-06) done: opt-in real-Claude-API eval harness in `tests/eval/` (double opt-in: `eval` marker excluded from default addopts AND `RUN_AGENT_EVAL=1` required; TTL-immune catalog injection via monkeypatched `retriever._load_catalog`; `EVAL_SAMPLES` cost control; `.last_run.json` gitignored) + measured full-dataset baseline recorded in `03-EVAL-BASELINE.md`: overall 85.1% (63/74), critical 75.0%, handoff 68.8%, informational 100.0% — a MEASUREMENT, not a target; 03-07 derives thresholds from it. Executor dropped twice on ECONNRESET after its final commit; orchestrator verified deliverables and wrote the SUMMARY. Default suite: 470 passed, 3 skipped, 1 deselected.
🟢 Wave 5, `03-07` done (ran in parallel with `03-08` in the same working directory/session; owned `tests/eval/test_agent_eval.py` + `docs/EVAL_GATE.md` only): the eval harness is now an actual regression gate, not just a measurement tool — `TOLERANCE`/`MIN_SAMPLE_FOR_TIER_GATE` constants next to `BASELINE_*`, `test_agent_eval` fails on overall/per-tier regression below baseline-minus-tolerance (tiers under 5 sampled cases reported, not gated), on any case with no customer reply (absolute floor), or on any pipeline exception; failure output is a built per-tier table + regressed-case list, printed on success too. `docs/EVAL_GATE.md` (197 lines) is the documented human pre-release command. See `03-07-SUMMARY.md` for the full writeup, including a real finding worth knowing: the deterministic 12-case default sample always draws 2 already-known baseline-failing cases (ids 31, 55), so a red 12-case smoke run needs an `EVAL_SAMPLES=all` re-run before concluding a real regression — documented in `EVAL_GATE.md` section 5(d), not treated as a bug. 03-08 (Phase 3's last plan — `tests/unit/test_phase3_requirements.py`, `ALYASMEEN/wiki/*`, `CLAUDE.md`) still in progress.

## Technical Debt / Risks
- ~~`retry_queue.py` (enqueue never called) is dead code claiming to run~~ → **Phase 4 criterion 1 resolved** (plan 04-04): `retry_queue.py`/`retry_actions.py` deleted entirely, the 15-minute scheduler job removed from `app/worker.py`, retirement documented in `supabase/migrations/20260825000003_retire_retry_queue.sql` (COMMENT ON TABLE, not DROP — table is already RLS-locked deny-all). `gatekeeper.py` is **no longer dead code** either — rewritten synchronous (plan 04-02) and wired into every outbound Claude call (`ai_service.py`) and every real-mode WhatsApp send (`whatsapp_meta.py`).
- ~~Migrations not yet applied to live Supabase; anon-vs-service_role key decision open~~ → **Phase 4 criterion 2 resolved** (plan 04-07, live): `SUPABASE_KEY=service_role` on Railway (web+worker) and local `.env`, verified end-to-end before `20260825000004_revert_anon_grants.sql` shipped; all 11 migrations applied live via `supabase db push` after repairing stale remote migration history and non-destructively renaming aside two drifted tables from the retired June architecture (`webhook_events_oldjune`/`outbox_jobs_oldjune`, 8 stale rows preserved — recommend dropping once confident).
- ~~Worker job store falls back to MemoryJobStore without `DATABASE_URL`~~ → **Phase 4 criterion 3 resolved** (plan 04-07, live): a Railway **worker** service now exists (Procfile `worker:` alone didn't auto-provision one — created manually with `python -m app.worker`, fixing a broken `python app/worker.py` invocation along the way) with `DATABASE_URL` set; `apscheduler_jobs.next_run_time` for `monthly_report` verified identical before/after a real worker restart. Cosmetic gap: structlog "Using SQLAlchemyJobStore" line not visible in Railway's log viewer (persistence itself independently confirmed via the live table; logging config fix deferred).
- ~~No dashboard visibility for dead-lettered events / failed outbox jobs~~ → **Phase 4 criterion 4 resolved** (plans 04-05 + 04-06): `GET /api/alerts` + retry endpoints in `ui_api.py`, consumed by `app/templates/alerts.html` (`/alerts` page, 5th nav tab "تنبيهات" wired into all 5 dashboard templates) — operator can see and one-click retry both dead-lettered `webhook_events` and permanently-failed `outbox_jobs`.
- ~~Backup/restore drill never actually executed~~ → **Phase 4 criterion 5 resolved** (plan 04-07, live): real drill run with native `pg_dump -n public` (Docker unavailable for `supabase db dump`) restored into a throwaway project via `psql`; verification counts matched production exactly; row recorded in `docs/BACKUP_DRILL.md`'s Drill Log; throwaway project deleted.
- Async latency: outbox adds ≤2s to replies (poll interval) on top of the 3s inbox poll; acceptable at current volume, monitor in pilot.
- Railway build now pinned to Nixpacks via `railway.json` (Railpack's default builder failed on an unrelated mise python-attestation error) — revisit if Railway fixes Railpack upstream.
- `webhook_events_oldjune`/`outbox_jobs_oldjune` exist live with no corresponding migration file (necessarily a one-off live remediation, not a repeatable schema change) — operator should drop them once confident the 8 old rows aren't needed, or a future light migration can formalize the drop.

## Todos & Blockers
- [ ] **TODO**: Merge `fix/production-hardening`'s final 2 commits (`f8d4bf7`, `780649e`) to `main` — everything through `0f9af1c` is already merged via PRs #5/#6.
- [ ] **TODO**: Continue `/gsd:execute-phase 3` — waves 1-5's `03-07` done (03-01 through 03-07, 7/8 plans); only `03-08` remains to close out Phase 3.
- [ ] **TODO** (deploy): Update `WA_META_TOKEN` (new system user token). `SECRET_KEY`/`DATABASE_URL`/`SUPABASE_KEY` are already set live on Railway (confirmed in plan 04-07); `DASHBOARD_PASSWORD` was retired by plan 05-03 (dashboard auth is now per-operator email+password + TOTP) — `SUPABASE_ANON_KEY` must be set live instead before 05-09's rollout (not yet configured anywhere, including locally — see 05-02/05-03 SUMMARYs).
- [ ] **BLOCKER**: Meta WABA registration still pending.

## Session Continuity
- **Last Action**: Executed plan `05-06-PLAN.md` (wave 5, depends on 05-05, ran in parallel with `05-09` in the same working directory/session). Owns `ui_api.py`/`processor.py` for the operator-facing API semantics. Task 1: every one of the 8 mutating `ui_api.py` endpoints now takes `op: Operator = Depends(require_operator)` and calls `audit.log_action(op.email, "<action>", {...})` after success; `api_update_status` calls `handoff.bot_recently_active(phone)` BEFORE any write and returns `409 {"conflict":true,"reason":"bot_active","customer_name","phone","last_activity","requested_status","message"}` unless the body carries `"force": true`, in which case it pauses the session, opens an `operator_takeover` handoff row, and logs `order_status_conflict_override` in addition to `order_status_changed`. Task 2: `GET /api/alerts` reworked via a new `_frame_alert(row, source)` helper into `{"alerts":[{id,source,phone,customer_name,severity,headline,what_happened,what_to_do,wa_link,kind,attempts,max_attempts,error,payload,created_at}],"counts":{total,customer_facing,internal}}` — LEFT JOINs `customers`, falls back to the phone number when the name is empty; new `POST /api/alerts/retry_all` bulk-resets both tables in two UPDATEs, audited `alert_retry_all`. Task 3: `app/services/processor.py` gained `notify_permanent_failure(source, phone, kind, error)` — queues a plain-Arabic alert via `queue_text` to `AUNT_PHONE` (customer-facing kinds or any dead-lettered webhook event) and `ADMIN_PHONE` (everything), with a mandatory loop guard (never alert a failure whose own phone IS the aunt's/admin's); `process_job` gained an optional `max_attempts: int = 3` param, called only on the job's last attempt. **Found and fixed a live production-data leak**: `audit.log_action`/`handoff.bot_recently_active` are real, unmocked calls unless a test explicitly monkeypatches them — this sandbox has real Supabase credentials + real network access, so an unmocked test run wrote 5 real rows to production `audit_logs` (confirmed via direct query, deleted immediately). Fixed by mocking in every affected test file, including `tests/integration/test_ui_api.py` which is **not** in this plan's declared `files_modified` but is directly exercised by Task 1's product/broadcast changes (documented as a Rule 2 deviation in `05-06-SUMMARY.md`). 22 new tests (13 integration `test_orders_api.py`+`test_alerts_api.py`, 5 unit `test_processor.py`, plus rewrites). Full suite: 343 passed/3 skipped. Commits: `7e7d229`, `a8423db`, `a32c237`. Full interface (409 payload, alerts item shape, `process_job` signature) recorded in `05-06-SUMMARY.md`.
- **Last Action (wave 6)**: Executed plan `05-08-PLAN.md` (wave 6, depends on 05-06, ran in parallel with `05-07` in the same working directory/session). Template-only, no backend changes needed — 05-06 had already shipped and tested the exact payload shapes this plan consumes. `app/templates/alerts.html`: rewrote the body against `GET /api/alerts`'s `{alerts, counts}` shape — Alpine getters `customerFacingAlerts`/`internalAlerts` split by `severity`; customer-facing cards lead with a green "افتحي المحادثة" WhatsApp link (`:href="item.wa_link"`, `rel="noopener"`) plus secondary retry, internal cards lead with retry only; both show `item.what_happened`/`item.what_to_do` via `x-text` and hide `kind`/`attempts`/`error`/`payload` behind a per-card collapsed "تفاصيل تقنية" toggle; header gained a `{{counts.total}} عنصر يحتاج مراجعة` line and a confirmed retry-all button wired to `POST /api/alerts/retry_all`. `app/templates/orders.html`: `updateStatus()` now branches on `res.status === 409` to open a conflict modal (built via `textContent`, never `innerHTML` concatenation, for the untrusted `customer_name`) instead of a generic error — "كمّلي أنتِ" re-POSTs with `force: true` and toasts the takeover copy, "اتركي البوت" / backdrop-click / Escape all just close it. Full suite unaffected: 351 passed/3 skipped, identical to before this plan. Commits: `ff01cdc`, `c32464b`. No deviations — both `<verify>` commands passed on the first attempt.
- **Last Action (wave 6, 05-07)**: Executed plan `05-07-PLAN.md` (wave 6, depends on 05-05/05-06, ran in parallel with `05-08` in the same working directory/session; owned `_nav.html`, `handoffs.html`, `audit.html`, `ui.py`, `tests/integration/test_ui_pages.py` — never touched `alerts.html`/`orders.html`, 05-08's files). `_nav.html`: three new entries after تنبيهات — محادثات (`/handoffs`, `#handoffCount` red badge polling `GET /api/handoffs/count` every `BADGE_POLL_MS=30000` + once immediately, hidden at 0, try/catch-silent), السجل (`/audit`), حسابي (`/account`, before خروج — closes the gap 05-09 flagged: `/account` was reachable only by direct URL until now). Same `_p == request.url.path` self-highlighting pattern reused, zero page templates touched (`grep -c "set active" app/templates/*.html` = 0 everywhere). `ui.py`: `GET /handoffs` -> `handoffs.html`, `GET /audit` -> `audit.html`, both under the existing `require_operator_page` guard. `app/templates/handoffs.html` (265 lines, new): Alpine page modeled on `alerts.html` — active/resolved filter pills, customer-name-first cards with a reason pill + relative time + WhatsApp link, a lazy transcript fetch (`GET /api/handoffs/{id}`, last 20 turns as RTL chat bubbles), and `أعد للبوت` -> `POST /api/handoffs/{id}/resolve` (moves the card locally, toasts, calls the nav's own `pollHandoffBadge()` to refresh the badge immediately; 409 toasts distinctly and reloads). `app/templates/audit.html` (202 lines, new): read-only, single `fetch('/api/audit')`, `describe(entry)` switch mapping all 18 `OPERATOR_ACTIONS` to plain-Arabic sentences with a raw-string fallback for anything unmapped, per-action icon, actor+sentence client-side filter, راw-JSON details toggle. `x-text` everywhere in both new templates — no `x-html`, no `#059669` (verified by grep). `tests/integration/test_ui_pages.py` (new, 2 tests, uses the shared `client`/`operator_client` fixtures): 200 + shared-nav markers signed-in on `/handoffs`/`/audit`/`/orders`/`/alerts`; 303->`/login` signed-out on `/handoffs`/`/audit`. Full suite: 353 passed/3 skipped. Commits: `be16ea8`, `6f2173a`, `c26d0ff`. **Additional live-DB verification** (same technique 05-05 used, since `SUPABASE_ANON_KEY` still isn't configured locally so a real browser login can't be tested): `TestClient` + `app.dependency_overrides` bypassing only the auth dependency, seeded a real handoff via `POST /dev/test_handoff`, walked seed -> list -> transcript(3 turns) -> resolve(200) -> repeat-resolve(409) -> badge 1->0 -> `handoff_resolved` row in `/api/audit`, then deleted every seeded row and reconfirmed 0 rows remaining for that phone/actor. No deviations.
- **Next Step**: Phase 5 wave 6 is fully complete (05-07 + 05-08 both have SUMMARYs and commits); the 05-10 checkpoint plan (human-gated: operator emails, `SUPABASE_ANON_KEY`, live rollout) is still the last plan in Phase 5, not yet run. Any plan writing a new kind of audit row must add its action string to `OPERATOR_ACTIONS` in `app/services/audit.py` first, or `list_operator_actions()` will silently exclude it (and `audit.html`'s `describe()` should get a matching case too, or the row falls back to the raw action string — still safe, just less readable). **Load-bearing test convention (05-06/05-09, now also 03-04)**: any test file exercising a mutating `ui_api.py`/`auth_routes.py` endpoint must monkeypatch `app.services.audit.log_action` (and, for `api_update_status`, `app.services.handoff.bot_recently_active`); `tests/conftest.py`'s shared autouse `mock_db` now covers `app.services.handoff`'s `query`/`execute`/`execute_returning` too, so most processor-level tests no longer need their own local patch — both are real, unmocked, live-DB calls otherwise; this sandbox has real Supabase credentials AND real network egress, so skipping this WILL write to production. `_nav.html` carries all seven page tabs plus حسابي and خروج. **Phase 3 is now IN PROGRESS, not stale**: wave 1 (03-01/02/03), wave 2 (03-04), and wave 3 (03-05) are all done — see the Phase 3 bullets below. Next: `03-06`/`03-07`/`03-08`.
- **Last Action (Phase 3, wave 1, 03-02)**: Executed `03-02-PLAN.md` (wave 1, no depends_on, ran in parallel with `03-01` and `03-03` in the same working directory/session; owned `app/services/policy.py` + `tests/unit/test_policy.py` only). Built `app/services/policy.py` — a pure, zero-I/O deterministic gate: `validate(tool_name, args, context) -> PolicyDecision` covers all 5 AI tools (`add_to_cart`, `show_menu`, `get_order_status`, `save_address`, `request_human_handoff`) in fixed rule order (session_paused → unknown_tool → order_not_mutable → per-tool arg rules, first denial wins); `TOOL_SCOPES` allowlist with `"order"` scope intentionally empty (order creation is confirm-only, status progression is operator-only — pinned via a monkeypatched synthetic order-scoped tool test since no real tool has this scope); `resolve_product()` extracts `_tool_add_to_cart`'s exact-then-substring catalog lookup into one shared definition; `add_to_cart` never honours a hallucinated `price` arg. `detect_handoff_keyword(text) -> str | None` returns `explicit_human`/`complaint`/`refund`/`damaged`/`privacy`/`None` using two-tier matching (multi-word phrases substring-matched, single tokens whole-word-only) to avoid `"بشر"` colliding with `"بشرتي"` and `"حنان"` never bare-word-matching; `opt_out_of_messages` (dataset id 54) intentionally returns `None` — no consent/suppression mechanism exists in this codebase, documented as out of scope for Phase 3. 63 new tests in `tests/unit/test_policy.py`, all green; the known wave-1 interaction (`test_tool_scopes_match_ai_service_tools` racing 03-03's 5th-tool addition) resolved favourably — 03-03 had already landed `request_human_handoff` by the time this test ran, so it passed first try. Full interface (context keys, `PolicyDecision` fields, group list) recorded in `03-02-SUMMARY.md` for 03-04/03-05 to consume. **Process note**: a shared-git-index race with sibling 03-01 (concurrent `git add`/`git commit` in one working directory) transiently swept this plan's Task 2 diff into 03-01's first commit; 03-01 self-corrected by amending, and this plan then committed its own Task 2 work cleanly — no data loss, final state verified via `git diff HEAD` (empty) and a full test re-run. Also logged (not fixed, out of scope) a full-suite test-pollution issue: `app/db/database.py`'s module-level circuit-breaker globals leak across test files within one pytest session, causing 5 `test_database.py` failures only in full-suite runs, never in isolation — see `.planning/phases/03-agent-dependability-safety/deferred-items.md` (03-01 later added a follow-up note there too). Commits: `2d23143`, `15eb191`, `7f0609c`. No code deviations from plan spec.
- **Last Action (Phase 3, wave 1, 03-03)**: Executed `03-03-PLAN.md` (wave 1, no depends_on, ran in parallel with `03-01` and `03-02` in the same working directory/session; owned `app/services/ai_service.py` + `tests/unit/test_ai_service.py` only). Task 1: added `AIUnavailableError(RuntimeError)` and replaced all three swallow points in `generate_reply()` — missing/empty `CLAUDE_API_KEY` now raises instead of returning a hard-coded Arabic string, a real Anthropic call failure raises with the original exception chained (`raise AIUnavailableError(str(exc)) from exc`), and an empty joined completion raises instead of shipping the literal `"(no reply)"` to a customer. The inner per-tool try/except (a single failing tool becomes a `tool_result` string Claude talks around) was deliberately left unchanged, with a comment explaining it's a separate, still-correct contract. `processor.AI_FALLBACK_REPLY` is now the only AI fallback string in the codebase — the `ai_service.py`-side duplicate (`"عذرًا، صار خلل مؤقت..."`) is gone (`grep -rn "صار خلل مؤقت" app/` returns nothing). Task 2: added the fifth Claude tool `request_human_handoff` (required `reason: string`) to `_TOOLS`, plus an `<escalation_rules>` system-prompt block (escalate on anger/refund demands/damaged-or-missing items/privacy concerns/explicit human requests; never claim an escalation or promise a cancel/refund/order-edit without actually calling a tool) and a worked example modeled on dataset case id 57. Tool *execution* (the real `handoff.trigger()` call) is explicitly left for 03-05 per the one-AI-file rule / ADR-005 — `ai_service.py` has no phone/session context. 9 new/rewritten tests in `tests/unit/test_ai_service.py` (raises on no key / API failure with chained cause / empty completion; failing tool still yields a normal reply; exact 5-tool name set; `reason` required; no tool exposes a `status`-mutating surface; prompt carries the escalation block + anti-fabrication rule; `tool_executor` receives the new tool's name+args through the existing 2-call loop). **Found and fixed the circuit-breaker test-pollution bug both 03-01 and 03-02 had already independently logged in `deferred-items.md` against files they don't own**: `generate_reply()`'s unmocked `app.ai.retriever._catalog()` call was burning real retries against the `block_live_db` guard on every `test_ai_service.py` test with a valid API key, and this plan's own new tests pushed the process-global circuit breaker over its failure threshold, breaking unrelated `test_database.py` tests in full-suite runs. Fixed (test-only, Rule 1/3) with a file-scoped autouse `mock_catalog` fixture patching `app.ai.retriever._catalog`; full suite went from 356 passed/5 failed to 431 passed/3 skipped, and `test_ai_service.py` itself dropped from 8.75s to 1.24s. Updated `deferred-items.md` to record the resolution for both siblings. Commits: `f9685ac`, `934d08f`. Full interface (`AIUnavailableError` import path + raise conditions, `request_human_handoff` schema) recorded in `03-03-SUMMARY.md` for 03-05 to consume.
- **Last Action (Phase 3, wave 1, 03-01)**: Executed `03-01-PLAN.md` (wave 1, no depends_on, ran in parallel with `03-02` and `03-03` in the same working directory/session; owned `app/services/handoff.py` + `app/services/audit.py` + `app/templates/audit.html` + `tests/unit/test_handoff_trigger.py` only). Built `handoff.trigger(phone, reason, metadata=None, assigned_to="aunt", notify=True) -> str` — the producer half of the handoff system Phase 5 had deliberately left as a stub for this phase. Idempotent per phone (SELECT-before-INSERT on active status; a repeat call re-heals `sessions.paused` but opens no second row/audit entry/notification — the only thing preventing an alert storm); `sessions.paused` is set via `INSERT ... ON CONFLICT (phone) DO UPDATE` rather than a bare `UPDATE`, correct even for a brand-new customer with no `sessions` row yet. `_notify_aunt()` queues a plain-Arabic alert through `processor.queue_text` (the durable outbox, never a direct send), loop-guarded against `AUNT_PHONE`/`ADMIN_PHONE`, modeled 1:1 on `processor.notify_permanent_failure()`. `REASON_LABELS` maps 6 caller reason codes (`keyword_request`, `unsupported_media`, `ai_failure`, `ai_requested`, `policy_denied`, `operator_takeover`) to Arabic sentences — the error contract is two-tier: the durable state transition (steps 1-3) is allowed to raise, the audit write + notification (steps 4-5) are best-effort and never raise past `trigger()`. Added `"handoff_triggered"` to `audit.OPERATOR_ACTIONS` (without this, `list_operator_actions()`/`/audit` would have silently excluded it — the exact trap 05-07's own note already flagged) and a matching `describe()` case in `audit.html`. 7 new tests in `tests/unit/test_handoff_trigger.py` (local `FakeHandoffDB`, `handoff.py` is not in conftest's shared `mock_db` patch list). **Process note**: Task 1's first commit attempt briefly swept sibling 03-02's then-unstaged `policy.py` changes into the same commit (a `git add`/`git commit`-without-pathspec race in this shared working directory) — self-corrected immediately via `git reset --soft HEAD~1` + selective unstage + recommit with an explicit pathspec, before 03-02 ever touched its own file again; no data loss, all subsequent commits in this plan used explicit `-- <path>` pathspecs. Also updated `tests/unit/test_audit.py`'s hardcoded `OPERATOR_ACTIONS` count (19→20, in scope since `audit.py` belongs to this plan) and logged/later-confirmed-resolved the shared circuit-breaker test-pollution issue in `deferred-items.md` (root-caused and fixed by 03-03). Commits: `c4790dc`, `aa67faa`, `3cf7696`, `d782978`. Full interface (signature, `REASON_LABELS` keys, error contract) recorded in `03-01-SUMMARY.md` for 03-04/03-05 to consume.
- **Last Action (Phase 3, wave 2, 03-04)**: Executed `03-04-PLAN.md` (wave 2, depends on 03-01+03-02, single sequential executor; owned `tests/conftest.py`, `app/routers/whatsapp_helpers.py`, `app/services/processor.py`, `tests/unit/test_processor_safety.py` — new file). Task 1: `tests/conftest.py`'s autouse `mock_db` now patches `app.services.handoff` (`query`/`execute`/`execute_returning`) alongside `wh`/`processor`/`audit` — the exact gap `03-RESEARCH.md` flagged; `FakeDB` gained a `handoffs` list (serving `trigger()`'s idempotency SELECT + INSERT-RETURNING) and its session-insert branch now preserves an existing `paused` flag instead of resetting it (mirroring `save_session()`'s real SQL, which never lists that column); a `last_handoff(fake_db, phone)` helper sits next to `drain_outbox_jobs()`. Task 2: `load_session()` (`whatsapp_helpers.py`) now `SELECT`s and returns `paused`; `handle_message()` gained two gates before any hard command — a **paused gate** (records the customer's message to `chat_history`, sends nothing) and a **keyword-handoff gate** (`policy.detect_handoff_keyword()` non-`None` → `_open_handoff()` → one deterministic `HANDOFF_ACK_REPLY`, never model-generated). `_open_handoff(phone, reason, metadata) -> bool` wraps `handoff.trigger()` so a durable-write failure falls back to `AI_FALLBACK_REPLY` instead of promising an escalation that didn't happen. Task 3: `process_event()`'s previously-silent `else` branch (audio/image/sticker/document/video/location/contacts) now calls the new `handle_unsupported_media(phone, msg_type, name)` — upserts the customer (mandatory, `handoffs.phone` has a live FK), records a `[رسالة <type>]` placeholder, queues `UNSUPPORTED_MEDIA_REPLY` before opening an `unsupported_media` handoff (reply-before-handoff ordering), and re-checks the paused flag itself so repeated media during one active handoff sends at most one apology. The webhook event is still marked `processed = TRUE` either way. 20 new tests in `tests/unit/test_processor_safety.py` (`TestPausedGate`, `TestKeywordHandoff`, `TestUnsupportedMedia`, including a 6-way media-type parametrization and a handoff-write-failure fallback test). `app/routers/whatsapp.py` correctly untouched (verified via `git diff --stat` against the pre-plan commit) — no code deviations from plan spec. Full suite: 451 passed/3 skipped (431 baseline + 20 new, zero regressions). Commits: `63cade3`, `90e74fb`, `78e15ce`. Full interface (`_open_handoff` signature, `HANDOFF_ACK_REPLY`/`UNSUPPORTED_MEDIA_REPLY` constants, exact gate order inside `handle_message`) recorded in `03-04-SUMMARY.md` for 03-05 to consume — that plan edits the same function to add AI-failure and tool-call-escalation handoff sites.
- **Last Action (Phase 3, wave 3, 03-05)**: Executed `03-05-PLAN.md` (wave 3, depends on 03-03+03-04, single sequential executor; owned `app/services/processor.py`, `tests/unit/test_processor_policy.py` — new file). Task 1: `_make_tool_executor`'s closure now calls `policy.validate(name, args, {phone, paused, cart, catalog(), order_status_provider})` as its first action, before any dispatch — `order_status_provider` is a lazy lambda so no DB read happens for today's five (none order-scoped) tools; a denial returns `decision.message` as the tool result (Claude phrases the Arabic sentence around it) and opens a `policy_denied` handoff when `decision.escalate` is set. `processor.py`'s local `MIN_CART_QTY`/`MAX_CART_QTY` literals are gone, replaced by `from app.services.policy import MAX_CART_QTY, MIN_CART_QTY` — `processor.MIN_CART_QTY`/`MAX_CART_QTY` still resolve (`1 50`); `_tool_add_to_cart`'s own clamp stays (numeric menu-pick path bypasses the AI/gate entirely). Task 2: `_tool_request_human_handoff(phone, st, reason)` opens a real handoff via `_open_handoff()`, only setting `st["handoff_pending"] = True` on success (never on a failed write); `handle_message()` consumes that flag via `st.pop()` right after `generate_reply()` returns and before the final `queue_text`, replacing the model's own reply with `HANDOFF_ACK_REPLY` so the customer gets exactly ONE deterministically-worded message, never the model's own (potentially over-promising) phrasing. Task 3: `generate_reply`'s except block now has a dedicated `AIUnavailableError` branch plus a defence-in-depth generic `Exception` branch — both send `AI_FALLBACK_REPLY` and open an `ai_failure` handoff via `_open_handoff()` (result discarded, `handoff_pending` never set on this path, so the reply is always the fallback string, never the ack). `handoff.trigger()`'s per-phone idempotency + the 03-04 paused gate together guarantee exactly one handoff/one aunt notification per customer per outage — verified with a two-message test. 13 new tests in `tests/unit/test_processor_policy.py` (276 lines): `TestPolicyGateBlocksBadToolCalls` (6 — hallucinated product, qty clamp, short address, canonical name resolution, unknown-tool escalation, no extra DB read for non-order tools), `TestRequestHumanHandoffTool` (3 — opens handoff + pauses, exactly-one-message double-message regression guard, failed-write does not promise an escalation), `TestAIFailureEscalation` (3 — `AIUnavailableError`, generic exception, no alert storm on a second message during one outage), `TestPausedSessionBlocksTools` (1). All five handoff triggers this phase owed are now live and tested: keyword (03-04), unsupported media (03-04), AI failure (03-05), the AI's own tool (03-05), and policy denial (03-05) — plus the paused gate that makes every one of them stick. `app/routers/whatsapp.py` confirmed untouched (`git diff --stat` against the pre-plan commit is empty). No code deviations — one verification-script artifact only (the plan's own Task 2 `<verify>` used `hm.index()` for an ordering check that collided with 03-04's earlier, identical `queue_text(phone, reply)` literal in the keyword-handoff gate; re-verified correctly with `hm.rindex()`, no code change needed). Full suite: 464 passed/3 skipped (451 baseline + 13 new, zero regressions). Commits: `876ea70`, `2f98133`, `044e07f`. Full interface (executor context dict, `handoff_pending` mechanism, complete live handoff reason-code list including `operator_takeover` from 05-06) recorded in `03-05-SUMMARY.md` for `03-06` to consume.
- **Last Action (Phase 3, wave 5, 03-07)**: Executed `03-07-PLAN.md` (wave 5, depends on 03-06, ran in parallel with `03-08` in the same working directory/session; owned `tests/eval/test_agent_eval.py` + `docs/EVAL_GATE.md` only). Task 1: added `TOLERANCE` (critical/handoff 0.05, informational 0.15, overall 0.08) and `MIN_SAMPLE_FOR_TIER_GATE=5` next to the `BASELINE_*` constants 03-06 recorded; `test_agent_eval` now asserts overall accuracy, per-tier accuracy (a tier sampled below the minimum is reported, never gated — a 12-case run can draw 1-2 cases from a tier), an absolute (non-baseline-relative) floor that every sampled case produced a non-empty customer reply, and that no case raised an exception. `_build_regression_report()` builds the per-tier baseline/measured/tolerance/floor/n/verdict table plus a regressed-case list (`id | intent | expected | observed | raw_input`) BEFORE the assertion, so it prints on success too under `-s`, and is reused verbatim as the `pytest.fail()` message on failure — no bare `assert 0.71 >= 0.79`. Added `test_gate_constants_are_consistent` (default-suite, unmarked, no fixtures — checks `TOLERANCE`/`BASELINE_BY_TIER` stay in sync and every tolerance is sane). Task 2: `docs/EVAL_GATE.md` (197 lines) — what the gate checks (explicit contrast with `tests/data/eval_intent.py`, which is NOT the release gate), when to run it, bash+PowerShell commands, how to read the table, a 4-branch failure playbook, the re-baselining procedure (update both `test_agent_eval.py` and `03-EVAL-BASELINE.md` in one commit, never quietly widen a tolerance), and known limitations. No CI workflow invented (no `.github/` in this repo). **[Rule 3 - Blocking] fix**: `tests/eval/conftest.py`'s autouse `_eval_guard` was skipping EVERY test under `tests/eval/` (not just `eval`-marked ones) unless `RUN_AGENT_EVAL=1` — this silently skipped the plan's own required default-suite `test_gate_constants_are_consistent`; scoped the guard to `request.node.get_closest_marker("eval")` so only real API-spending tests are gated. **Real finding, documented not "fixed"**: two consecutive real `EVAL_SAMPLES=12` runs against an unmodified codebase both failed the overall gate (75.0%, then 66.7%, vs a 77.1% floor) — the deterministic default sample structurally always includes 2 already-known baseline-failing cases (ids 31, 55) plus 1-2 more from live-model variance; `TOLERANCE`/`DEFAULT_SEED`/`MIN_SAMPLE_FOR_TIER_GATE` were deliberately left untouched (adjusting them would be inventing a threshold) and this is instead documented in `EVAL_GATE.md` section 5(d) as the expected reason to re-run `EVAL_SAMPLES=all` before concluding a real regression. Failure path verified twice: organically (the two real runs above) and synthetically (`TOLERANCE["overall"]` temporarily set to `-1.0`, ran with `EVAL_SAMPLES=3` to minimize cost, confirmed `pytest.fail` with the regressed-case list, then reverted). Full suite green and eval-free throughout: 471→483 passed (03-08 landed concurrently in between), 3 skipped, 1 deselected. Commits: `e1d1897`, `a8c1463`. Full interface (`TOLERANCE`/`MIN_SAMPLE_FOR_TIER_GATE` constants, report format) recorded in `03-07-SUMMARY.md`.
