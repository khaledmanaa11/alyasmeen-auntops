# Log

**Summary**: Append-only record of all operations on this vault.

---

| Date | Source | What changed |
|------|--------|--------------|
| 2026-08-28 | GSD phase 03-08 (execute-plan) | Created [[agent-safety]] — the durable-knowledge page for Phase 3's agent-dependability-safety work, which had shipped (waves 1-4, plans 03-01 through 03-06) with no wiki coverage at all. Documents `app/services/policy.py` (pure, zero-I/O deterministic tool-call gate), the five handoff-trigger reason codes (`keyword_request`, `unsupported_media`, `ai_failure`, `ai_requested`, `policy_denied`) and the separate paused-gate mechanism that makes each one stick, the `trigger()`/`resolve()` handoff lifecycle, the order-mutation boundary (now regression-tested by `tests/unit/test_phase3_requirements.py`), and a pointer to `docs/EVAL_GATE.md` (plan 03-07, without restating its thresholds). Every claim cites `file:line` verified against the code this session — corrected one internal inconsistency in `03-08-PLAN.md`'s own task description along the way, which listed "the paused gate" as one of "the five handoff triggers"; the paused gate does not itself open a handoff, it silences the bot once one is already active, so the page documents it as a separate, complementary mechanism instead of folding it into the trigger list. Flagged [[whatsapp-bot-brain]] and [[ai-service]] as still stale (predate the 2026-08-25 `processor.py` rewrite) with an explicit warning banner rather than fixing them — out of scope for this plan. Added to index.md. |
| 2026-08-28 | GSD phase 05-03 (execute-plan) | Rewrote dashboard auth end to end: the shared-password cookie (`sha256(SECRET_KEY:DASHBOARD_PASSWORD)`, checked by a `_is_authenticated()` copy-pasted in ui.py/ui_api.py/broadcast.py) is retired. New flow: `app/routers/auth_routes.py` (email+password via `app/services/auth.py`'s `sign_in()`, TOTP challenge at `/login/mfa`, `/logout` + `/logout-all`) mints an opaque session via `app/services/sessions.py`'s `create_session()`; every other router is now guarded by a single FastAPI dependency (`app/routers/auth_deps.py`'s `require_operator`/`require_operator_page`/`require_admin`) instead of a per-handler check. `Config.DASHBOARD_PASSWORD` removed entirely (grepped clean across the repo, `ALYASMEEN/raw/` intentionally left untouched — immutable snapshot, not re-synced this session). Updated [[web-dashboard]] accordingly. |
| 2026-08-25 | outbox wiring session | Wired the durable outbox for real: all customer-facing sends in `processor.py` now enqueue into `outbox_jobs` via new `queue_text`/`queue_buttons` helpers; only the poller's `process_job` (and standalone scheduler services) call `send_text`/`send_buttons` directly. A send failure now retries bounded in the outbox (max_attempts=3 → status='failed') instead of failing and replaying the whole webhook event. All four `whatsapp_meta.py` senders now raise `WhatsAppSendError` on non-2xx instead of silently returning — send failures were previously invisible. Tradeoff: replies gain ≤2s latency (outbox poll interval). GSD prep: inserted Phase 4 "Reliability & Ops Completion" into `.planning/ROADMAP.md` (old 4/5 → 5/6), rewrote `.planning/STATE.md` to current truth, flagged Phase 3 plans as stale (written pre-hardening). [[whatsapp-bot-brain]] and [[whatsapp-meta-integration]] need a review pass. |
| 2026-08-25 | bug-fix session (multi-agent review → Sonnet fix waves, 245 tests green) | Fixed the verified bugs from the full-project review. **Bot pipeline** (processor.py, whatsapp.py, main.py): webhook_events poison-pill events now dead-letter after 3 attempts (new migration `20260825000000`); malformed webhook payloads can no longer crash before persistence; global handler no longer masks /whatsapp/ failures with HTTP 200; Arabic confirm (تأكيد/اكد/أكد/تم) and the missing clear command (clear/فرغ/مسح) added; `create_order_atomic` result properly unwrapped (order numbers were garbled); AI-call failure now sends an Arabic fallback instead of poisoning the event; cart qty clamped 1–50; `_tool_show_menu` mutates the live session (stale-snapshot clobber fixed); **address-stage replies were never persisted** (missing `save_session`) — delivery orders were created with an empty address; now fixed + confirm blocks delivery-without-address. **AI/config**: CLAUDE_MODEL default was retired `claude-3-haiku-20240307` → `claude-haiku-4-5-20251001`; removed `admin123`/`change-me` credential fallbacks (fail-fast at import, pytest exempt); Anthropic client timeout 30s; raw exception text no longer sent to customers; catalog cache now 60s TTL (worker saw stale prices forever); second tool round forced to text via `tool_choice: none`. **Dashboard**: stored-XSS escaping in orders.html; broadcast send URL/field mismatch fixed + auth added to /broadcast/improve; session cookie Secure in prod. **Invoice**: fully translated Hebrew→Arabic (pdf_invoice.py, ui_api.py, retry_actions.py) with Amiri font + reshaper/bidi, render-verified. **DB**: migration `20260825000001` re-grants run_query/run_exec (RLS migration had revoked the app's own access); `20260825000002` creates the phantom `monthly_snapshots` table. **Tests**: debug.py stale import fixed (was killing pytest collection) + debug router gated behind USE_MOCK_WHATSAPP; conftest rewritten for the processor architecture; 5 stale-architecture test files ported to tests/unit/test_processor.py; pytest.ini merged into pyproject.toml. Suite: 245 passed / 3 skipped (Docker) / 0 failed. NOT committed — all in working tree. Wiki pages [[whatsapp-bot-brain]], [[ai-service]], [[supabase-data-layer]], [[web-dashboard]] need a review pass against these changes. Still open: outbox_jobs wiring (sends still direct), retry_queue.enqueue never called, gatekeeper.py unwired, Phase 3 (policy gate/handoff/eval), CI/CD, backup drill. |
| 2026-06-14 | — | Vault initialized: CLAUDE.md, wiki/index.md, wiki/log.md created |
| 2026-06-14 | repo-wide scrub | Removed all stale "Wave" references now that invoicing is confirmed PDF-only: CLAUDE.md (Done list, file tree, tables ×2, env-var rows), README.md (env vars), .env.example, app/db/schema.sql (action enum comment), docs/TODO.md, agents/prompts.py, and the generated .planning/codebase/{INTEGRATIONS,ARCHITECTURE}.md. Verified no project file presents Wave as a live integration; only provenance/resolution records retain the word. |
| 2026-06-14 | live code (ui_api.py, pdf_invoice.py, retry_actions.py) | Settled invoicing PDF-vs-Wave discrepancy: **PDF only** (no wave_invoice.py exists; fires on done in ui_api.py:147). Corrected scheduler-jobs.md (Wave→PDF) and design-decisions.md (open question→resolved). Reconciled .planning: SYNTHESIS.md status AWAITING USER → READY TO ROUTE; recorded resolutions in INGEST-CONFLICTS.md. Flagged stale Wave refs in TODO.md + project CLAUDE.md for correction. |
| 2026-06-14 | repo cleanup (Khaled) | Deleted the `agents/` multi-agent pipeline (pipeline.py, frontend_pipeline.py, prompts.py, __init__.py, output/) — it was only a test; sole worker is Khaled via Claude Code. Not imported by any app code; verified app/ still parses clean. Removed the Multi-Agent Pipeline section from project CLAUDE.md and the `agents/output/` rules from .gitignore. |
| 2026-06-14 | repo cleanup (Khaled) | Retired `catalog.json` for real. Replaced the file-reading `_CATALOG` in whatsapp_helpers.py with a `catalog()` adapter over the live Supabase loader (`retriever._catalog`), mapping sku/price/description → id/list_price/description_sale. Updated call sites (whatsapp.py `info N`, debug.py `/dev/test_order`) and both tests (test_debug.py, test_whatsapp_info.py) to use `catalog()`. Deleted app/data/catalog.json + stray root catalog_editor.html & catalog_template.md. Fixed CLAUDE.md (removed catalog.json from file tree, corrected the "knowledge/ is EMPTY" claim — it has 6 .md files). Verified import wiring + adapter mapping with venv python (pytest couldn't run: venv pip is OneDrive-corrupted, system py3.13 has incompatible fastapi). |
| 2026-06-14 | .planning/intel/ (gsd-ingest-docs) | Created wiki/design-decisions.md — distilled the 9 accepted decisions (ADR-001..005, ADR-PE-001..004), the 2 user-resolved competing-variant conflicts (catalog grounding, token budget), and the open PDF-vs-Wave invoicing question from `.planning/intel/decisions.md` + `INGEST-CONFLICTS.md`. Cross-linked to ai-service, supabase-data-layer, whatsapp-bot-brain, scheduler-jobs, database-tables. Added to index.md. |
| 2026-06-14 | project CLAUDE.md, graphify GRAPH_REPORT.md | Added Layer 2/3 architecture to CLAUDE.md. Snapshotted both sources into raw/. Generated graph/graph-overview.md (787 nodes · 1211 edges · 58 communities · top 10 god nodes). Ingested CLAUDE.md into 7 wiki concept pages: alyasmeen-auntops, whatsapp-bot-brain, ai-service, supabase-data-layer, database-tables, web-dashboard, scheduler-jobs, whatsapp-meta-integration. Cross-linked wiki ↔ graph. Rebuilt index.md as master MOC. Removed default Welcome.md. |
| 2026-08-28 | docs (orchestrator) | Added docs/OPERATOR_GUIDE_AR.md — the aunt's Arabic day-to-day dashboard guide (10 sections, every button label verified verbatim against app/templates/*.html, no invented business facts; closes the "no Arabic leave-behind doc" gap found by 06-RESEARCH.md). Updated docs/OPERATOR_ACCOUNTS.md status header: identity stack built (05-03/04/09), operator_auth migration applied LIVE and verified, local .env prepared (anon key + base URL set, DASHBOARD_PASSWORD removed); remaining 05-10 steps are human-gated. |
| 2026-08-30 | production recovery (orchestrator + Khaled) | WhatsApp bot resurrected after ~2.5-month outage. FIVE stacked failures, peeled one by one: (1) Meta webhook pointed at the suspended alyasmeen.org domain — re-pointed to web-production-2ee0f.up.railway.app/whatsapp/webhook; (2) WA_META_TOKEN was a 60-day temp token, dead since ~June 15 — replaced with a never-expiring System User token (whatsapp_business_messaging + management); (3) inbound POSTs 403d because WA_META_APP_SECRET was never set on Railway (fail-closed HMAC by design) — Khaled added it; (4) CLAUDE_MODEL env var on Railway pinned a retired model (the June "Error code: 400" in chat_history) — set to claude-haiku-4-5-20251001 via MCP; (5) unpinned anthropic>=0.25.0 resolved to breaking 1.2.0 on the fresh build ("unexpected keyword argument temperature", captured in ai_failure handoff metadata) — pinned <1.0 in commit 5f14a75. Remaining: Railway CLAUDE_API_KEY is invalid (401) — Khaled replacing. Phase 3 safety net worked as designed throughout: every AI failure produced the Arabic fallback + an ai_failure handoff + session pause (and Meta duplicate deliveries were deduped by idempotency). Diagnosis chain is reproducible: webhook_events counts -> Railway http logs -> handoffs.metadata error field. |
