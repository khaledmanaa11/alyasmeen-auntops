# Log

**Summary**: Append-only record of all operations on this vault.

---

| Date | Source | What changed |
|------|--------|--------------|
| 2026-06-14 | — | Vault initialized: CLAUDE.md, wiki/index.md, wiki/log.md created |
| 2026-06-14 | repo-wide scrub | Removed all stale "Wave" references now that invoicing is confirmed PDF-only: CLAUDE.md (Done list, file tree, tables ×2, env-var rows), README.md (env vars), .env.example, app/db/schema.sql (action enum comment), docs/TODO.md, agents/prompts.py, and the generated .planning/codebase/{INTEGRATIONS,ARCHITECTURE}.md. Verified no project file presents Wave as a live integration; only provenance/resolution records retain the word. |
| 2026-06-14 | live code (ui_api.py, pdf_invoice.py, retry_actions.py) | Settled invoicing PDF-vs-Wave discrepancy: **PDF only** (no wave_invoice.py exists; fires on done in ui_api.py:147). Corrected scheduler-jobs.md (Wave→PDF) and design-decisions.md (open question→resolved). Reconciled .planning: SYNTHESIS.md status AWAITING USER → READY TO ROUTE; recorded resolutions in INGEST-CONFLICTS.md. Flagged stale Wave refs in TODO.md + project CLAUDE.md for correction. |
| 2026-06-14 | repo cleanup (Khaled) | Deleted the `agents/` multi-agent pipeline (pipeline.py, frontend_pipeline.py, prompts.py, __init__.py, output/) — it was only a test; sole worker is Khaled via Claude Code. Not imported by any app code; verified app/ still parses clean. Removed the Multi-Agent Pipeline section from project CLAUDE.md and the `agents/output/` rules from .gitignore. |
| 2026-06-14 | repo cleanup (Khaled) | Retired `catalog.json` for real. Replaced the file-reading `_CATALOG` in whatsapp_helpers.py with a `catalog()` adapter over the live Supabase loader (`retriever._catalog`), mapping sku/price/description → id/list_price/description_sale. Updated call sites (whatsapp.py `info N`, debug.py `/dev/test_order`) and both tests (test_debug.py, test_whatsapp_info.py) to use `catalog()`. Deleted app/data/catalog.json + stray root catalog_editor.html & catalog_template.md. Fixed CLAUDE.md (removed catalog.json from file tree, corrected the "knowledge/ is EMPTY" claim — it has 6 .md files). Verified import wiring + adapter mapping with venv python (pytest couldn't run: venv pip is OneDrive-corrupted, system py3.13 has incompatible fastapi). |
| 2026-06-14 | .planning/intel/ (gsd-ingest-docs) | Created wiki/design-decisions.md — distilled the 9 accepted decisions (ADR-001..005, ADR-PE-001..004), the 2 user-resolved competing-variant conflicts (catalog grounding, token budget), and the open PDF-vs-Wave invoicing question from `.planning/intel/decisions.md` + `INGEST-CONFLICTS.md`. Cross-linked to ai-service, supabase-data-layer, whatsapp-bot-brain, scheduler-jobs, database-tables. Added to index.md. |
| 2026-06-15 | live code (whatsapp.py, main.py) | Fixed inbound-logging crash on non-UTF-8 consoles: replaced the bare `print(...)` at the top of `webhook_post` with the module logger (`log.info`, %s args), and reconfigured `sys.stdout`/`sys.stderr` to utf-8/errors="replace" at startup in main.py (reusing the pattern already in scripts/). On Windows cp1255 the print of Arabic raised UnicodeEncodeError before any processing, dropping the message; Linux/Railway stdout is UTF-8 so it only bit local testing. Consolidated a redundant adjacent `log.info` into the one readable line. Added a self-contained regression test (tests/integration/test_bot_flow.py) that simulates a cp1255 stdout and asserts an Arabic message still gets a reply. Updated wiki/whatsapp-bot-brain.md. **Discovered (not fixed, out of scope):** the `client` fixtures in tests/unit/test_whatsapp.py and tests/integration/test_bot_flow.py still monkeypatch the dead `wa._CATALOG` (removed in the 2026-06-14 catalog refactor), so ~24 tests error at setup; several DB tests also need live Supabase creds. Baseline and post-change suites both show 10 failed / 24 errors — my change adds +1 passing test and zero new failures. |
| 2026-06-14 | project CLAUDE.md, graphify GRAPH_REPORT.md | Added Layer 2/3 architecture to CLAUDE.md. Snapshotted both sources into raw/. Generated graph/graph-overview.md (787 nodes · 1211 edges · 58 communities · top 10 god nodes). Ingested CLAUDE.md into 7 wiki concept pages: alyasmeen-auntops, whatsapp-bot-brain, ai-service, supabase-data-layer, database-tables, web-dashboard, scheduler-jobs, whatsapp-meta-integration. Cross-linked wiki ↔ graph. Rebuilt index.md as master MOC. Removed default Welcome.md. |
